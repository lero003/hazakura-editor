## 読みやすい見開き

朝の光が机の上に届く。文章を読み返す時間には、言葉の間にも余白がほしい。

```text
読みやすさ
= 適度な文字の大きさ
+ 本文のまわりの余白
+ ページの中央の間隔
+ 落ち着いた行間
```

> 本文を変えずに、自分が読みやすい大きさで読む。

1. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

2. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

3. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

4. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

5. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

6. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

7. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

8. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

9. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

10. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

11. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

12. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

13. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

14. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

15. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

16. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

17. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

18. 文章をゆっくり読み返す。長い段落とコードの枠があっても、隣のページとの間に十分な余白が残る。

## 次の章

読んだ場所に戻り、必要なところだけを書き直す。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

段落の長さが変わっても、前後のページへ移動できる。

## 短い章

小さな余白を残して、本を閉じる。
