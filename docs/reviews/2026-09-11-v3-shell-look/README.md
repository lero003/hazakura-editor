# 実機フィードバック（第2弾・11件）の確認と調整 — 2026-09-11

実機で触っていて見つかった11件を、**実アプリをブラウザに描いて測って**直した記録。
`fixture.html` + `fixture.tsx` は `App`（AppShell + useAppShellController）をそのまま描き、
ネイティブ境界（`__TAURI_INTERNALS__.invoke`）だけスタブする。UIは1行も作り直していないので、
実機と同じ DOM・同じ CSS を測れる。

```
npm run dev:vite
# 総合: 実アプリそのもの（?theme=light / ?doc=long）
open http://127.0.0.1:1420/docs/reviews/2026-09-11-v3-shell-look/fixture.html?theme=light
# サイドバー下端だけ（実コンポーネントを225px幅で）
open http://127.0.0.1:1420/docs/reviews/2026-09-11-v3-shell-look/fixture-sidebar.html?theme=light
```

## 指摘 → 原因（実測） → 直し

| # | 指摘 | 実測した原因 | 直し |
|---|------|--------------|------|
| ① | 右上に「書き出す」がない | `.primary-export` が存在しない（`exportBtn:false`）。保存ボタンは `[1262,15,57,35]` | `AppPrimaryToolbar` に「書き出す」を追加（`[1330,15,90,35]`）。**既存の書き出しダイアログ（形式ナビ付き）を開くだけで、新しい経路は作らない** |
| ② | 「書く」「読む」「確認」のアイコンが文字とずれる | アイコンの `span` が **14px の SVG なのに高さ 20px**（インラインの行ボックス）で、文字のベースラインに乗っていた：`[635,23,14,20]` | `display:inline-flex` + `line-height:0` で中央揃えに。実測 `[635,26,14,14]`（SVGと同寸） |
| ③ | 信号機が左上固定（モックは縦中央） | `trafficLightPosition {x:9,y:18}`。バーは 66px なので中心は y=33、ライト中心は y=24 で **9px 上に寄っていた** | `y:27` へ（12px のライトを 66px の中央に） |
| ④ | ペイン見出しの説明が説明的 | 「プレビュー」＋「縦スクロールで見た目を確認」の2段。モックは `PREVIEW / 表示のみ` の2〜3語 | 説明文は `purposeTitle`（ホバー）へ回し、見出しに残すのは**情報だけ**（アウトラインの注意件数・参照の「読み取り専用」）。参照ペインは見出しをファイル名に |
| ⑤ | 罫線・スクロールバーが異様に多い | タブ領域に**横線3本**（`.tabs-row` の強い線＋`.tab-list` の線＋アクティブタブの2px下線）＋読み取り操作5ボタンを囲む独立した枠。スクロールバーは `::-webkit-scrollbar` の自前定義で、macOSのオーバーレイが無効になり常時表示 | `.tab-list` の下線を撤去、`.pane-toggles` の囲み枠を撤去、自前スクロールバー定義を撤去（OS既定＝スクロール中だけ出る） |
| ⑥ | えるモードの背景が黄色い必要はない | `--bg:#faf6ee`（温かいクリーム）＋セピアのアクセント | モック準拠のニュートラルな紙へ：`--bg:#fffefb` / インク `#24362d` / アクセント `#356b50` |
| ⑦ | 「読む」で見開きにならないケースがある | **(a)** 前付/導入の章が `max-width:min(620px,100%)` で、見開き閾値 1090px に永久に届かない。**(b)** 本文が1列にしか満ちない章では紙だけ見開き幅になり**右半分が空く** | 上限を撤去。紙は**実際に使う列数**に合わせる（`data-spread="one"` で455px／`"two"` で996px）。**縮めるのは表示幅だけで、flow の幅は見開きのまま**（列組み・折り返し・ページ計算を変えないので本文が切れない） |
| ⑧ | 差分モードで下の方だけ比較、上部の空白 | 空の `.reference-editor-host` が **119.5px** を占めていた（グリッドの auto 行が2つあり、中身の無いホストにも高さが配られる） | 差分面と本文ホストを**同じセルに重ねる**（`grid-area:1/1`）。差分は上寄せ（中央寄せをやめる） |
| ⑨ | 差分モードのデザインがモックと違う | セットアップ面が**入れ子の枠3段**（カード＋2スロット＋ボタン）。結果面は対象カード・符号・注意書きを持っている | 外側の破線カードを廃止して枠を1段に。凡例（＋/−）と対象カードは既存。**行内の語単位ハイライト（モックの `del`/`ins`）は未実装**＝残件 |
| ⑩ | フォルダ内検索・ゴミ箱のところに並列ボタン | フッターはゴミ箱だけ | 「フォルダ内を検索」を並列に追加（モック準拠）。実測 225px 幅で `search[8,261,170,32]` + `trash[182,261,34,32]`、重なりなし |
| ⑪ | 全般的にモックのスッキリ感がない | ②⑤⑨⑩の積み重ね | 上記の合算。タブ内の二重線・操作群の囲み枠・差分の入れ子枠・常時スクロールバーを落として、境界は「タブ群→本文」「本文→ステータス」に寄せた |

## 実測に使った数字

- ナビのアイコン：修正前 `span[635,23,14,20]` → 修正後 `[635,26,14,14]`（ボタンは `[622,15,85,35]` で中心 y=32.5）
- 上部バー：`[0,0,1440,66]`、保存 `[1262,15,57,35]`、書き出す `[1330,15,90,35]`
- 差分：修正前は空ホスト 119.5px → 修正後は行 `657px` のみ（ヘッダは y=158 から）、セットアップカード `[453,235,820,259]`（上寄せ）
- 見開き：長い章 `sheet=996 / flow=916 / 列 x=262,723,1184… / Page 1 / 10`、1列分の章 `sheet=455 / flow=916（据え置き） / data-spread="one"`
- えるモード：`--bg #fffefb / --text #24362d / --accent #356b50`

## 実機で見てほしいところ（自動化の代替にしない）

1. 信号機の縦位置（ネイティブ描画。CSSでは動かない）
2. スクロールバーがスクロール中だけ出るか（ヘッドレスの Chromium では再現しない）
3. 「読む」→見開き→「書く」往復、章をまたぐページ送りが空白にならないこと
4. えるモードの紙が「白い紙」に見えるか（`#fffefb` はごく僅かに暖色）
5. 差分の行内ハイライト（未実装の残件）をどうするか

## 検証

- `npm run typecheck` ✓
- `npx vitest run` → **278ファイル / 2,429件** ✓
- `npm run build:vite` ✓
- `npm run smoke:app-store-surface` → **10ファイル / 120件** ✓
- Rust 無変更（`cargo test` は未実行）
