# v3 UI 外部レビュー依頼 — 江戸彼岸の明色化 / スライスC・D・E

Status: Ready for external review
Implementation scope: `5d2962fc..5861b872`（実装とその資料。内訳は下表）
Review packet: 本文書とブックキーピングのコミット。**実装の差分は上の範囲を見ればよい**
Branch: `codex/v3`
**最終レビュー対応**: `docs/reviews/2026-09-11-v3-slice-f1/README.md`（R1〜R5＋適用の実経路テスト＋画面16/04）
Authority: Evidence（ローカル検証・実描画・DOM実測。CIの主張はしない）
Date: 2026-09-11


## 0. この依頼の前提（モック起点の再点検）

これまでの棚卸しが**自分の作業ログ起点**でモックの要求体系を通していなかったため、
`docs/reviews/2026-09-11-v3-mock-recheck/README.md`（`c4dfdc7c`）で24画面をモック起点に引き直した。
その結果、未着手だった **スライスC・D・E** が残っていた。今回はそのうち C・D・E-1 と
**江戸彼岸（画面20）の明暗**を実装した。

### 実装範囲（このリクエストの7コミット）

| commit | 内容 |
| --- | --- |
| `c4dfdc7c` | モック起点の再点検（資料のみ） |
| `76d4a148` | **江戸彼岸をモックの明色へ**（テーマ・えるモード・装飾・えるモードの分割バー） |
| `f16274c1` | その資料 |
| `619d1102` | **スライスD**: 保存衝突の2版カード（14）・画像プレビューの倍率（24） |
| `f8e75c74` | その資料 |
| `d2993881` | **スライスC**: Local Assist の状態・文言・失敗（06/07/21/22） |
| `9c18b5b6` | **スライスE-1**: 検索（09）・構造のヒント（10）・書き出し（11） |
| `3b678de8` | **スライスE-2**: 見開きの紙面と進捗バー（04）・設定の境界説明（17）・LIVE PREVIEW（16） |
| `06925974` | **レビューP2**: R1〜R5 の修正と、適用（Apply）の実経路テスト |
| `746ce8da` | **スライスF-1**: 文字サイズの確認面に4設定（16）・読書面の下部ページ操作帯（04） |
| `06925974`〜`29871895` | **レビューP2/P3対応**: R1〜R5・Applyの実経路テスト・09の4096バイト境界（backend snippet）・R4の文字表示と文書identity |
| `242518b7` | **スライス07**: 差分レビューを主編集領域で読む（本文面化） |
| `5861b872` | **スライス11**: 書き出しの形式ナビを共通ダイアログ枠へ |

### 再現コマンド

```bash
git log --oneline 5d2962fc..3b678de8
git diff --stat 5d2962fc..3b678de8
npm run typecheck && npm test
npm run smoke:app-store-surface
npm run build:vite
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo test --manifest-path src-tauri/Cargo.toml   # 検索の wire 形式を変更したため必須
```

## 1. 特に見てほしい点（実装側の自己申告リスク）

1. **江戸彼岸の明色化**（`edohigan-theme.css` / `lMode.css` / `EdohiganShaderOverlay.tsx`）
   - モックが定義するテーマは `:root`・`.dark`・`.edohigan` の3つだけで、**モックの江戸彼岸は明色**
     （紙 `#fffcf8` / 地 `#fbf4f2` / インク `#453735` / アクセント `#975b68`）。実アプリは暗色だった。
   - **モックの罫線値（`#e6d8d0` 1.36 / `#c9b6ae` 1.90）は既存の階層契約（1.45〜1.7 / ≥2.1）を満たさない**ため、
     色相を保って `#dccac0`(1.55) / `#bfa8a0`(2.20) へ置き直した（B案の前例に従う意図的な差）。
   - えるモードの暗色グループから edohigan を外し、**机 `#fbf4f2` の上に紙 `#fffcf8`**という構造にした
     （モック03の紙はライトテーマの紙で、「えるモードはテーマの紙を使う」という構造に合わせた）。
   - **シェーダーは加算から「桜色へ寄せる」方式へ変更**（明色では加算が白飛びする）。花びらは左右の余白のみ。
     **WebGL の中身はヘッドレスで撮影できない**ため、装飾の見た目は実機の目視が未受入。
   - 前景オーバーレイの z を `--z-crt-top`(1200) → `--z-base`(1) へ下げ、**モーダル/パレットより手前へ出ない**
     ようにした（モック20の要求）。
   - **えるモードの分割バー**が窓の約38%を `--border` で覆っていた既存不具合を修正
     （1440×850 で323px。ライトテーマでも同色帯で出ていた = テーマ非依存）。

2. **画面14（保存衝突）の版カード**
   - ディスク側は**本文を読まず** `get_file_metadata` のバイト数と最終更新だけを出す
     （文字数は本文を読まないと分からないため**出さない**）。読めない場合は数字を出さない。
   - 読み取りは新しいフック `useDiskFileMetadata` に分離（読み取り専用・pathless/非Tauri では何もしない・
     遅延応答は破棄）。**保存・比較・戻るの経路は既存のまま**（上書きへ進むボタンは置いていない）。
   - `.close-dialog` の `max-width: 420px` を上書きするためセレクタを一段強くしている
     （`.save-conflict-backdrop .save-conflict-dialog`）。**この上書きの是非**を見てほしい。

3. **画面24（画像プレビュー）の倍率**
   - **100% = 画像1px が CSS1px**（devicePixelRatio では変えない）。**fit は 100% と別物**で、
     小さい画像は 1 を上限に拡大しない。表示変換のみ（再圧縮・保存なし）。
   - 拡大時は `overflow: auto` + `place-items: safe center` で端へ到達できる。
   - **実機での巨大・破損・透明・不在・権限案内は未確認**。

4. **画面07の挙動変更（意図的）**
   - 反映後は**反映ボタンを消し**、「文書へ反映しました（未保存）。⌘Zで戻せます。」へ置き換える
     （同じ案をもう一度「採用」させない）。既存テストの期待を**反転**させた：
     「反映後も Applied ボタンがある」→「反映後はボタンが消える」。この判断の妥当性を見てほしい。
   - 追加・削除の凡例を追加（記号は `DiffBody` と同じ `-` / `+`）。
   - **本文面化（右下フローティング→本文領域）は未実装**のまま（別スライス相当）。

5. **画面22の失敗表示**
   - 別窓の未知エラーは**生の内部文字列を出さない**（3言語）。既存テストの期待を反転させた。
   - サイドバーは内部メッセージを `classifyLocalAssistError` で分類し、**理由別の短い案内**を出す。
   - 「もう一度試す」は**既存の送信経路**（`submit()`）を呼ぶだけで、新しい生成経路を作っていない。

6. **検索の wire 形式変更（要確認）**
   - Rust の `WorkspaceSearchResult` に **`total_files_matched` を追加**した（走査数とは別）。
     TS 側の型・hook・表示を同時に更新しているが、**他にこの構造体を読む経路がないか**を確認してほしい。
   - 一致の着色は backend の `column`（1始まり・文字位置）を使い、**query は trim して長さを揃えている**。

7. **書き出しのラベル変更（ユーザー可視）**
   - EPUB/PDF の主操作を「書き出す」→「**書き出し先を選ぶ**」へ統一（HTML と揃えた）。
   - HTML に「「本全体」は PDF と EPUB で書き出せます（HTMLは1文書ずつ）。」を追加。
   - **形式ナビの統合（1モーダル化）は未実装**（3コマンド分離のまま。D02 は native 別窓の新設を必須としていない）。

8. **見開きの紙面と進捗バー（画面04）**
   - `.ebook-page-sheet` に**不透明な紙 + 1px罫線 + `--shadow-sm` + 角丸3px**を与えた（実測 389×395、
     背景 `rgb(255,254,251)` / 罫線 `rgb(199,210,197)`）。
   - 見開き中央のガターを **`clamp(28px,5vw,44px)` → 6px**（モックは4〜6px）。
     **既存のCSS契約テストがこの値を固定していたため、期待値をモック準拠へ更新**した（この判断を見てほしい）。
   - 進捗バーは `role="progressbar"` + `aria-valuenow/max`。**ページ数が未計測の間は 0%・max=1** として
     「不明」を正直に出し、推測の進捗を描かない。
   - 未実装: running header（実metadataがある場合のみ表示する想定）、下部のページ操作帯、単文書面と本全体面の統合。

9. **設定の境界説明（画面17）と LIVE PREVIEW（画面16）**
   - 可用性の**平文2行を削除**してカード1枚に一本化（同じ状態文言を二度出さない）。
     `role="status"` + `aria-label` は維持し、a11y テストの参照先を壊していない。
   - 境界4枚は**新しい状態機械を作らず**、既存 `appleAssistStatus` をそのまま表示するだけ。
     **設定を開いただけで生成しない**（従来どおり probe の結果のみ）。
   - LIVE PREVIEW は**保存済みの値だけ**を使い、ここから本文も設定も書き換えない。
     行間は独立した設定が無い（D06）ため**プレビュー既定の1.9固定**（新しい設定を発明していない）。
   - 途中で1件落ちた: 設定本文に見出しタグ（`h3`/`h4`）を足したことで**カテゴリ位置の計算対象がずれた**。
     見出しは設定のカテゴリ見出しだけに残し、カード内は通常要素＋クラスへ変更して解消（意味的にも正しい）。
   - **画面18（使い方の4カード面）は実装していない**: 既存のHelp面は「英語のみ・App Store提出準備」という
     2026-06-09 の**明示的な決定**を持ち、`smoke:app-store-surface`(117件) の対象でもある。
     日本語の使い方面を足すかは**ご判断ください**（勝手に寄せていません）。

## 1.5 レビュー（P2 5件）への対応 — 2026-09-11

外部レビューの P2 5件と受入範囲の指摘に対応した。詳細と実測は
`docs/reviews/2026-09-11-v3-slice-f1/README.md`。

| # | 指摘 | 対応 | 回帰テスト |
| --- | --- | --- | --- |
| R1 | 絵文字の後で一致強調がずれる（コードポイント vs UTF-16） | 切り詰めと着色を**コードポイント単位**へ統一 | 絵文字・補助面漢字の後で `mark` が一致2文字のみ／サロゲート片なし |
| R2 | fit が25%未満で「縮小」が拡大になる | 下の段が無いときは**現在値を維持** | 14.4% → 縮小しても増えない |
| R3 | 縦長画像で fit が全体を収めない（5%下限） | fit から**下限を撤去**（手動倍率と分離） | 1000×20000 → **2.88%**（表示3%）で高さが収まる |
| R4 | 進捗の未計測と ARIA の不一致 | 章番号で計測済みを判定。未計測は `aria-valuenow` を**出さない**／計測済みは `min=0,max=総,now=現在+1` | 計測前は値を主張しない／視覚25%＝ARIA25% |
| R5 | 生成案の上限を「入力対象が4,000文字超」と誤案内 | 分類と文言を**3つに分離**（入力対象／生成案／継続上限）し3言語 | 誤ったまとめを固定していたテストを反転 |
| 09 | 長い行の後方一致が見えない（行頭240文字で切る） | **一致位置を中心**に窓を作る（前60文字・切った側に `…`） | 400文字目の一致で `mark` が見え、表示が240文字以内 |
| 09 / P2 | **4096バイトより後ろ**の一致は payload から落ちていた | backend が**一致を含む snippet** と `column`/`snippet_start`/`match_length`/`line_length` を返す契約へ。frontend はその契約だけで着色と `…` を描く | Rust: 4096バイト超の一致が snippet に入る／絵文字後の `column` が文字単位。UI: 行頭と行末が切れた長い行で一致だけが `mark` |
| R4 / P3 | 未計測でも文字が `1 / 1` を主張 | 表示と ARIA の契約を `ebookProgress.ts` に集約（未計測は値を出さない） | **実測1ページの章**が `1 / 1`・`aria-valuenow=1/max=1`・塗り100%／未計測側は純関数テスト |

- **受入範囲**: `onApply` が成功を返すだけの fixture では本番の提案消費を証明できないため、
  **本番の単一ライタとストアを通す統合テスト**を追加した（`LocalAssistApply.integration.test.tsx`:
  1回だけ書く／提案がストアから消える／レビュー面が消える／サイドバーに完了通知／2回目は拒否）。
  **消費済み提案を残して通知する修正はしていない**。証跡の範囲もそのように訂正した。
- **併せて**: 画面16（中央面が4設定を映す）と画面04（下部のページ操作帯）を実装（`746ce8da`）。
- **フレーキー修正**: `LModeActionRail` の焦点復帰テストが3回に1回落ちていたため、
  role/name で引き直して待つよう安定化（5回連続で安定）。
- 意図的に更新した既存テスト: 目次・「編集に戻る」の所在（モック04の下部帯へ移動）、
  読書面のガター値（6px）、ガター値を固定していたCSS契約テスト。

## 2. ローカル検証（証跡として提示）

| 種別 | 結果 |
| --- | --- |
| `npm run typecheck` | 成功 |
| `npm test` | **274ファイル / 2,393件** 成功 |
| `cargo test` | **385 passed, 2 ignored**（検索の snippet 契約を変更） |
| `npm run smoke:app-store-surface` | 117件 成功 |
| `npm run build:vite` | 成功（chunk サイズ警告は既存） |
| `cargo fmt --check` / `cargo test` | OK / **385 passed, 2 ignored**（検索の snippet 契約を変更したため） |

**テスト件数の成功は「モック適合」の主張ではない**（レビュー V2）。件数は実装が壊れていないことの証跡であり、
モックとの差は「未受入・残り」に分けて記録している。また「実機受入」と「ローカル検証」も別物として扱う。

実描画の証跡（いずれも実コンポーネント。ネイティブ境界のみスタブ）:

- 江戸彼岸: `docs/reviews/2026-09-11-v3-edohigan-light/`（紙 `#fffcf8` 33.6% / ナビ `#f4e9e6` 15.0% /
  chrome `#fbf4f2` 9.6% / アクセント `#975b68` 1.0%、えるモードは机49.1%・紙46.3%）
- スライスD: `docs/reviews/2026-09-11-v3-slice-d/`（ダイアログ 680×581・カード各299px、
  2560×1708 の画像が fit 43% → 150% で3840px・スクロール可）
- スライスC: `docs/reviews/2026-09-11-v3-slice-c/`（凡例、反映後にボタンが消えること）
- スライスE-1: `docs/reviews/2026-09-11-v3-slice-e1/`（`余白|余白|余白` の一致着色、
  `2 ファイルに一致 · 3 件（走査 7 ファイル）`、`該当箇所へ`、`書き出し先を選ぶ`）
- スライスE-2: `docs/reviews/2026-09-11-v3-slice-e2/`（見開きの紙 389×395・ガター6px・進捗 `1/4` 25%、
  設定カード 834×105 と境界4枚、LIVE PREVIEW 834×112・本文15px）

## 3. CI について

GitHub の HEAD に combined status が付いていないため、**「CI 合格」とは主張しない**。
上記はすべてローカルの実行結果である。

## 4. 未受入・残り（次のスライス候補）

### 実装が要るもの（新規面）

- **画面05（本の構成の専用面）**: 現状はサイドバーのパネル。モックは「書名・表紙・本として読む・書き出す」を
  持つ独立面を要求する（**表紙はモック自身が別タスクと注記**）。章行の状態のうち「現在開いている」「欠落」は
  既存で出ているが、**「未保存の変更」は dirty 情報がパネルへ渡っていない**ため未実装。
- **画面12（Import の専用2ペイン）**: 既存は確認ダイアログ→下書きタブ→参照ペイン。モックの専用面は
  **D04 が未決として残している**項目であり、勝手に新設していない。今回触っていない。
- **画面13（復元の3領域）**: 復元ダイアログの構造変更（左に候補・中央に本文・右にメモ）。**要判断の大型改修**。

### 構造差が残るもの（レビューの高優先）

- **画面07 本文面化**: **実装済み**（`242518b7`）。右下フローティングをやめ、本文領域いっぱいの面にした
  （実測 1440×850 で面=1440×850・offset 0,0、差分は面の内側だけスクロール、操作は下端固定、
  生成経路は不変・単一ライタのまま）。詳細と実測は `docs/reviews/2026-09-11-v3-slice-07/README.md`。
- **画面11 形式ナビ**: **実装済み**（`5861b872`）。共通のダイアログ枠に形式ナビを置き、切替は
  「いまのを閉じて、選んだ形式の**既存の準備処理**を呼ぶ」だけ。3コマンドは残置、native 別窓は新設していない。
  実アプリで PDF→EPUB→HTML の切替を実測（`role=dialog` は常に1つ、HTMLの本全体非対応の理由は維持）。
  詳細は `docs/reviews/2026-09-11-v3-slice-11/README.md`。

### 意図差として分類したもの（レビュー 15・03 の指摘への回答）

| 項目 | 分類 | 理由 |
| --- | --- | --- |
| 設定のカテゴリ数（アプリ4分類 vs モック5分類） | **意図差**（今回は戻さない） | 分類は情報設計ではなく実装の都合（画面の並び）で、増やすと業務ロジック（設定の保存単位・説明文）まで波及する。モックの見た目だけを理由に増やさない |
| テーマ名（アプリの7テーマ vs モックの3テーマ） | **意図差** | モックが定義するのは `:root` / `.dark` / `.edohigan` の3つだけで、他の4つはアプリ独自。名前を寄せるとテーマの実体（トークン群）と食い違う |
| えるモードの全テーマ方針 | **判断待ち** | 江戸彼岸は明色で確定済み（今回）。他のテーマで「テーマの紙を使うか、独立した読書面（セピア）を残すか」は**全テーマのモックが存在しない**ため、勝手に決めない |
| 設定間隔（モックの間隔値） | **意図差** | 既存の余白スケールを使う。数値だけを合わせると他の面とリズムが崩れる |

### 判断待ち（実装ではなく意思決定）

- **画面18（使い方の4カード面）**: 既存Help面は**英語のみ**が明示決定（2026-06-09）で App Store 提出準備の対象。
  日本語の使い方面を足すか、既存に寄せるかは**ご判断ください**。
- **画面04の残り**: running header（実metadataがある場合のみ）と、単文書面・本全体面の統合。
  下部ページ操作帯は実装済み（`746ce8da`）。
- **画面16の行間**: 行間は独立した設定が無く（D06）、今回はプレビュー既定の1.9固定。
- **画面07の本文面化**、**書き出しの形式ナビ統合**、**えるモードの全テーマ方針**（テーマの紙を使うか）

### 実機でないと確認できないもの

- **native 受入と横断シナリオ T01〜T10**（実機。停止→停止処理中、外部変更からの衝突到達、画像の巨大/破損/権限 等）
- **`src-tauri/tauri.conf.appstore.json` は App Store 別作業の未コミット変更**（bundleVersion 126）。
  このレビューの対象外であり、触っていない。
