# v3 モックとの差 — 棚卸しと詰め方

Status: Audit; 段階1（面トークン）完了
Scope: `20260909_hazakura-ui-implementation` の24画面・共通ルールとの差の実測と実行順
Authority: Medium
Last reviewed: 2026-09-10

## 何を測ったか

モック側は `01-shared-system.md`（C02/C04/C05の基準値）、`reference/design-spec.md`、`reference/design-tokens.css`。
現行側は実アプリをブラウザーで起動（`hazakura-note-theme=light` / `hazakura-note-menu-language=ja`、1440×850、等倍）し、
DOMの実寸と計算済みトークンを読んだ。画面01は実画面と `reference/screens/01-welcome.png` を並べた。

**先に結論**：数値系の骨格（上部バー・ステータス・ボタン・角丸・UI文字）は既にモックと一致している。
全画面で差として効いているのは **①面の配色（紙面の暖かさ／ナビ面の色）②開始画面の構造 ③設定の外枠寸法** の3つ。

## 1. 骨格（既に一致しているもの）

| 要素 | モック基準 | 現行（実測） | 判定 |
|---|---|---|---|
| メイン上部バー | 高さ66px | 本文領域が y=66 から始まる＝66px | 一致 |
| ステータス | 高さ35px | `.status-bar` 高さ35px・文字12px | 一致 |
| ボタン | 高さ35px前後／角丸7px | 35px／radius 7px／文字13px | 一致 |
| UI文字 | 13–14px／補助12px前後 | 13px（補助11.5–12px） | ほぼ一致 |
| 余白 | 4/8/12/16/24/32 | `--radius-sm/md/lg` 4/8/12、余白は部品ごと | ほぼ一致 |
| 文書タブ43px・表示ツールバー48px | 参考値 | **未測定**（文書を開いた状態で要実測） | 未確認 |
| サイドバー225px | 初期参考値 | **未測定**（保存済み幅を優先する方針） | 未確認 |

## 2. 配色（全画面に効く差）

現行ライトテーマと `design-tokens.css` の対応表。数値は実測（現行）と定義（モック）。

| 役割 | モック | 現行 | 差 |
|---|---|---|---|
| アプリ背景 | `#f7f8f5` | `#fafbfa`（`--bg`） | わずかに冷たい |
| 本文の紙面 | `#fffefb` | **無し**（`--surface` `#ffffff` を兼用） | 暖かい紙面の概念がない |
| 操作部／ナビ背景 | `#eef2ec` | **無し**（サイドバーは `--surface-muted` `#f1f3f2`） | ニュートラル灰で、緑みの面がない |
| 汎用surface | `#ffffff` | `#ffffff` | 一致 |
| 控えめな背景 | `#f2f4ef` | `--surface-muted` `#f1f3f2` | ほぼ一致 |
| 本文 | `#24362d` | `--text` `#1a1f1d` | 現行のほうが暗く、緑みが少ない |
| 補助文字 | `#606f64` | `--text-muted` `#6b7670` | 近い |
| 主要操作 | `#356b50` | `--accent` `#2e6b4f` | 近いが、モックは比較案と明記 |
| 淡い選択 | `#e1ecdf` | `--accent-soft` `#eef3f0` | モックのほうが緑が濃い |
| 境界線 | `#dce2d9` | `--border` `#e4e7e5` | 現行は中立灰 |
| 強い境界線 | `#b9c6b8` | `--border-strong` `#c8cfc9` | 現行は中立灰 |
| 追加／削除の背景 | `#e8f1e6` / `#f8e9e7` | 既存Diffトークン | 未突合せ |

`--surface-paper` / ナビ面の意味トークンは現行に存在しない（C04が「必要なら追加」と明示している新設候補名）。
**「面の分け方」が無いことが、全画面の印象差の主因**という見立て。

## 3. 文字（既定値は設定であり、変更は別決定）

| 対象 | モック目安 | 現行の既定 | 扱い |
|---|---|---|---|
| 通常ソース | 15px / 行高1.95 | `editorFontSize` 14 | C05: 既存設定を尊重。既定変更は別決定として記録 |
| プレビュー | 明朝17px / 行高2.1 | `previewFontSize` 15 | 同上 |
| えるモード | 21px / 行高2.15 / 最大幅724px | `lModeFontSize` 15 | 同上 |
| UI | 13–14px | 13px | 一致 |

つまり読み面の「大きさ」は**利用者設定の既定値問題**で、見た目の刷新とは分けて判断する（現行の判断を踏襲）。

## 4. 画面ごとの差（確認できたもの）

| 画面 | 差 | 規模 |
| --- | --- | --- |
| 01 はじめの画面 | ~~モックは2ペイン（左＝淡いセージ面に明朝44–46pxの大見出し＋主/副ボタン、右＝「続きを書く」一覧＋カード）~~ → **UI-G4で2ペイン化済み**（残差は補足カードの採否と押下の手応え） | 済 |
| 15 設定・テーマ | 左ナビ幅200px（現行180px）・アイコンなし・現在地の見せ方が違う（G2で実装、残差は `reviews/2026-09-10-v3-ui-g2/README.md`） | 小〜中 |
| 設定の外枠 | 参考1100×752 | 現行は幅 min(920px) で 920×760 | 小 |
| 16 文字と表示 | モックは書体・行間スライダーを含む（現行は4つの文字サイズ＋静的な見本） | C05/D05–D07で**新設定は別判断** | 保留 |
| 05 本の構成 | モックは専用面。現行はサイドバー/Book Scopeの既存導線 | 新規面 | 保留 |
| 23 狭いウィンドウ | 1024px幅でサイドバーを畳む案 | 実測は `v3-ui-b1/b2` の証跡あり | 要再比較 |
| 24 画像プレビュー | 倍率・全体表示 | D14で**別機能として保留** | 保留 |
| その他（02/03/04/06–14/17–22） | 各スライスで合評済み。未突合せの残差は個別に再比較が要る | — | 次の作業 |

C09の手順（同じウィンドウ・テーマ・文字サイズで撮影し、修正する差と意図した差に分ける）で
画面ごとに潰す。24画面の突合せ表はこの文書に追記していく。

## 5. 実行順（提案）

1. ~~**共通面トークン**~~ **完了**：[UI-G3](reviews/2026-09-10-v3-theme-paper/README.md) で
   `--surface-paper` / `--nav-surface` を新設し、7テーマへ適用（モック値＋テーマ別の導出）。
   読む・書く面は不透明な紙面、サイドバーはナビ面。紙面のコントラストは全テーマ自動検査
   （不透明ナビ5テーマも自動、半透明のedohigan/shinkaiは実描画ピクセルで確認）。
2. ~~**01 開始画面**~~ **完了**：[UI-G4](reviews/2026-09-10-v3-ui-g4/README.md) で2ペイン化
   （左＝ナビ面のブランド＋45px明朝コピー＋開始操作、右＝紙面の「続きから」一覧＋履歴0件の案内）。
3. ~~**段階2（文字色・アクセント・境界線・chrome面）**~~ **完了（第二調整を残す）**：
   [段階2](reviews/2026-09-10-v3-theme-stage2/README.md) でlight/darkにモック値を反映、
   `--chrome-surface` を全7テーマへ新設（ツールバー・タブ・ステータス）。
   実測で面の分離は1.06〜1.13:1しかなく、モック自身のC08（重要な境界3:1目安）と衝突するため、
   **第二調整（B案）で罫線だけを一段強めた**（[境界線](reviews/2026-09-10-v3-border-hierarchy/README.md)）。
   面の構造はモックのまま、chrome/紙面の境目だけは未決。
4. ~~**focus ring・ガター境界・領域境界**~~ **完了**：[focus/境界](reviews/2026-09-10-v3-focus-and-boundaries/README.md)
   でリングを `--focus-ring` に（薄めを全廃）、`--cm-gutter-border` を `var(--border)` へ、
   タブ下・ステータス上だけ `--border-strong`。
5. ~~**設定の外枠**~~ **完了**：[設定外枠](reviews/2026-09-10-v3-settings-frame/README.md) で1100×752・レール200px・
   アイコンなし。**Help導線は未決**（パレットを任意カテゴリで開くAPIが無く、モック18の使い方画面は新設画面のため）。
6. ~~**23 狭い窓**~~ **完了**：[狭幅](reviews/2026-09-11-v3-compact-sidebar/README.md) で
   ≤1100pxのとき表示上だけ畳む（保存設定なし・利用者の開閉は幅が変わるまで優先）。
   1440=開く／1024=畳む／トグルで開く／戻すと既定へ、を実測。
7. モックに等価定義がない5テーマ（yakou/shokou/edohigan/shinkai/crt）の文字色は据え置き（罫線は第二調整で実施済み）。

## 6. 残課題（2026-09-11 時点・実測つき）

モック対応は**1〜6まで完了**。残りは次の8件で、いずれも「実装」ではなく**判断または実機**が要る。

| # | 課題 | 現在地（実測・根拠） | 必要な作業 | 種別 |
| --- | --- | --- | --- | --- |
| A | 1024で本文が564pxに留まる | サイドバーを畳むとカラムが1024pxになり、`@container document-workspace (max-width: 780px)` の単一ペイン判定を外れる（エディタ564px＋プレビュー460px）。モックの本文目安は630px | 閾値を780px→約880pxへ（1行）。広い窓の保存済み分割へ戻る既存受入に触れるため**オーナー判断** | 判断 |
| B | 設定の「本文書体」「行間」 | 4つの文字サイズ設定（editor 12–22 / preview 12–24 / workspace 10–18 / L Mode 12–24）は既存。書体・行間の**独立した永続設定は無い** | モック自身が「別タスク」と明記。追加するなら設定の正本とバリデータの設計から | 別タスク |
| C | 画面24 画像プレビューの寸法 | `ImagePreviewPane` は既存。モックの参考寸法（上部59px・情報列54px）との差が**未計測** | 実機でワークスペースの画像を1枚開いて計測（ブラウザ検証では画像ペインに到達できない） | 実機 |
| D | Help導線（レール最下部の「使い方」） | アプリのHelpはコマンドパレットの `help.*`（ローカルデータの説明・サポート診断・プライバシー・OSS・アプリについて）に既存。**任意カテゴリでパレットを開くAPIが無い** | レールからパレットを開く最小導線を足すか、モック18の「使い方」画面を新設するか | 判断 |
| E | Diff行の背景 | モックは追加 `#E8F1E6` / 削除 `#F8E9E7` を挙げるが、既存の `--diff-*-bg` は alpha 合成で近似（実測で近い） | 既存トークンへ寄せるかは別スライス | 別スライス |
| F | L Mode の紙と文字の既定値 | モックの 15/17/21px と行間を既定へ入れるかは未決定（利用者設定を尊重する原則） | 既定値変更は表示刷新と別の決定として記録 | 判断 |
| G | native 受入 | VoiceOver / 文字200% / 実機の開始画面・編集画面 / 半透明ナビ＋実シェーダー / 再起動後のテーマ・文字サイズ / 別窓同期 / 実IME→Reader→章編集→Undo | 実機で実施（自動化で代替しない） | 実機 |
| H | CI | GitHub の HEAD に combined status が付かない | ローカルの数値（typecheck / 2,326件 / App Store surface 117 / Vite build）だけを根拠として扱う | 運用 |

## 7. 決定が要る点（履歴）

- **配色の強さ**：オーナー判断により「全7テーマをモックの方向へ作り直す」で確定（2026-09-10）。
  段階1で面（紙面／ナビ面）、段階2で文字色・アクセント、第二調整で罫線の階層を適用済み。
- edohigan / shinkai のエディタ面は段階1で不透明な紙面へ統一（C08）。水流の透過は操作面とガターへ寄せた。
- 面の分離（chrome vs 紙面 1.06:1）は「B案＝罫線だけ一段強める」で決着（2026-09-10）。
