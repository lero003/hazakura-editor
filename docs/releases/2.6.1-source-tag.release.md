# Hazakura Editor 2.6.1 Source / Local App Candidate

Status: Local source and App Store package candidate; tag and publication pending
Scope: v2.6 Local Assist baseline plus theme / Preview reading and selection polish
Authority: Medium
Last reviewed: 2026-08-27

`2.6.1` is the next local candidate after the merged `2.6.0` A-1–A-4 source
work. It is not a source tag, TestFlight upload, App Review, GitHub Release,
or publication claim. Physical Assist validation remains a separate gate.

## What changed since 2.6.0

- Theme contrast and reading-surface polish: right Preview, e-book pages, and
  whole-book Reader keep opaque paper on joke themes; shell shaders, scanlines,
  boot, and ambient particles remain.
- Preview paint stability: image loads no longer rebuild the Markdown tree.
  Resolved images are cached and reapplied before paint.
- Long-document Preview selection: drag-to-edge auto-scroll, pause of
  editor↔Preview scroll sync during the pointer gesture, and no link follow
  when a click ends a text selection.

Local Assist conversation / Diff apply, save, and mutation boundaries are
unchanged from `2.6.0`.

## Store delta since published `2.4.0`

The App Store What's New draft for this package is
`docs/releases/2.6.1-app-store-release-notes.md`. It restates the unpublished
v2.5 workspace-control line, the v2.6 Local Assist conversation / Diff apply
line, and the 2.6.1 Preview / theme polish. It is copy for App Store Connect,
not a publication record.

## Release boundary

This is a local candidate. A generated App Store `.pkg` is a TestFlight-shaped
artifact only. It is not an upload or review claim. Hazakura Local Assist
remains an on-device preview companion with no network fallback, auto-save,
tool calling, or autonomous apply.

Build locally with:

```bash
npm ci
npm run build
```

The generated app is a local validation artifact. It is ad-hoc signed and not
Developer ID signed or notarized.

## Known limits

- Detached-window Assist physical smoke (narrow layout, keyboard/focus,
  VoiceOver, streaming/cancel) is still pending. The live helper on this host
  answered `availability: { kind: "available" }`; that is not the UI gate.
- Source tag, App Store Connect upload, App Review, and GitHub Release need a
  separate explicit approval.
- Published Mac App Store remains `2.4.0` (hotfix only).
- Full built-app smoke-checklist (IME, dirty close, save conflict, workspace
  images) was not re-run for `2.6.1`; latest-HEAD evidence here is the local
  gates, helper probes, distribution probe, and native window smoke.

## Local candidate evidence

Evidence is from `main` at `6ff22dad` (2.6.1 plus App Store `bundleVersion`
119), plus this notes pass:

- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` — pass.
- `cargo test --manifest-path src-tauri/Cargo.toml` — 368 passed / 2 ignored.
- `npm test` — 213 files / 1,819 tests pass.
- `npm run build:vite` — pass; existing large-chunk warning only.
- `npm run smoke:app-store-surface` — 10 files / 111 tests pass.
- `npm audit` — 0 vulnerabilities.
- `cargo audit --file src-tauri/Cargo.lock` — no high/critical vulnerability
  reported; 18 transitive GTK / parser / Unicode / GLib warnings remain
  allowed and documented by the current dependency boundary.
- `npm run build:apple-assist-helper:fixture` — fixture smoke ok.
- `npm run build:apple-assist-helper:live` — probe
  `availability: { kind: "available" }`.
- `npm run build` — helper-enabled App Store preview app built as `2.6.1` /
  `dev.hazakura.editor`; existing large-chunk and expected local
  no-notarization warnings remain.
- `npm run probe:macos-distribution` — `LICENSE` and `THIRD_PARTY_NOTICES.md`
  present; helpers executable; ad-hoc signature verifies; `spctl` rejected
  with Insufficient Context (expected for this lane).
- `SKIP_BUILD=1 npm run smoke:macos-window -- "<preview app>"` — native window
  `1280x820` opened and quit cleanly.
- `git diff --check` — pass.
- `docs/release-pre-check.md` hygiene on `v2.3.0..6ff22dad` — added-path hits
  limited to test fixtures such as `/tmp/note.md`; public `lero003` repository
  links; CSS `token` helper and diagnostics redaction tests. Zero tracked files
  under `src-helpers/apple-assist/.build/`, `src-tauri/target/`, or
  `node_modules/`.
- A local App Store / TestFlight `.pkg` already exists; provenance is in
  ignored `docs/internal/app-store-candidates/latest.json`. That is not an
  upload claim.

## Related

- `docs/releases/2.6.1-app-store-release-notes.md`
- `docs/releases/2.6.0-source-tag.release.md`
- `docs/v2.6-plan.md`
- `docs/smoke-checklist.md` items 14–15
