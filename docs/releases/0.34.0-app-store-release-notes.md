# Hazakura Editor 0.34.0 App Store Release Notes

Status: Superseded draft
Scope: User-facing App Store / TestFlight release-note copy for `0.34.0`
Authority: Medium
Last reviewed: 2026-06-26

> Superseded by `0.35.0`: the local native-print smoke passed, but the
> TestFlight build still showed macOS' "This application does not support
> printing" alert. Do not use this draft for release copy.

This note is the public-facing release-note draft for the `0.34.0` App
Store / TestFlight candidate. It does not by itself claim App Store
Connect upload, Apple processing, TestFlight distribution, App Review
approval, or public Mac App Store release.

Local package evidence is tracked separately in
[`0.34.0-app-store-submission-candidate.release.md`](0.34.0-app-store-submission-candidate.release.md).

## App Store Connect Copy

PDF印刷と、長文表示・編集の安定性を改善しました。

- PDF印刷が外部ブラウザ経由ではなく、アプリ内のネイティブ印刷処理で開くようになりました。
- サンドボックス環境でも印刷ダイアログへ進みやすくなるよう、印刷用の一時HTML処理を整理しました。
- プレビューや電子書籍モードの長文表示が重くなりにくいよう、描画とページ計測を調整しました。
- プレビューの慣性スクロールや、エディタのスクロールバー操作後の挙動を改善しました。
- Local Assistの提案を適用するとき、対象タブをより確実に扱うようにしました。
- ワークスペース画像が電子書籍モードで透明のまま表示されることがある問題を修正しました。
- EPUB書き出し、ワークスペースの開いているファイル表示、右クリックからのMarkdown挿入など、前回までの改善も含まれています。

## Longer Release Note

`0.34.0` focuses on making PDF print usable in the Mac App Store /
TestFlight lane and tightening the v1 long-form reading / editing path.
The previous print path wrote temporary HTML and handed it to the OS
default browser. That was fragile under the App Sandbox, so PDF print now
stays inside the app boundary and uses an app-owned native print webview.

Main changes:

- PDF print no longer depends on opening a temporary HTML file through
  the OS default browser.
- The app invokes the system print panel from an app-owned native print
  webview.
- Rust-side validation still requires the main window, non-empty HTML,
  and a plain `.html` file name.
- Stale print-browser handoff code and tests were removed.
- Preview scroll synchronization avoids fighting inertial scrolling.
- E-book rendering now debounces repeated Markdown sanitization after the
  initial render and coalesces page measurement into animation frames.
- Markdown rendering performs fewer HTML parse passes.
- Editor scrollbar and Go to Line focus behavior was hardened.
- Local Assist applies generated text to the intended tab more
  explicitly.
- Workspace images that could stay transparent in e-book Mode are
  remeasured more reliably.
- The v0.33 EPUB Export v1 Polish and small workspace / editor
  fit-and-finish items remain included in this candidate.

## Boundary

This release does not add a generic browser launcher, shell input,
Preview DOM editing, automatic rewriting, auto-save, Git integration,
LSP, terminal, arbitrary command execution, external AI/API calls, or
Agent Workbench to the App Store lane.

PDF print remains an explicit user action. Hazakura Local Assist remains
only an explicit, on-device, availability-gated writing companion whose
result stays unsaved until accepted and reviewable through Diff /
Discard.

## Verification

Source/local proof passed with focused PDF print / export hook tests,
full `npm run test` (118 files / 1045 tests), full
`cargo test --manifest-path src-tauri/Cargo.toml` (298 tests), App Store
surface smoke, signed app distribution probe, strict codesign
verification, package signature check, sandbox preview smoke, Info.plist
version/build checks, package SHA check, and `git diff --check`.

The user reported the manual native PDF print flow as working before the
`0.34.0` build `46` package candidate was generated. App Store Connect
upload, Apple processing, TestFlight install / launch, and App Review
remain outside this repository state until separately recorded.
