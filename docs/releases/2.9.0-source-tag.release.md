# Hazakura Editor 2.9.0 Release Candidate

Status: App Store published (owner-reported); candidate evidence retained
Scope: Daily-use quality and System-only Local Assist
Authority: Medium
Last reviewed: 2026-09-09

## 公開報告 — 2026-09-09

オーナーがv2.9の審査通過・公開を報告。公開build/source対応と実際の提出文面は独立未確認。
以下の候補・未実施・配布前確認は準備時点の記録として保持し、個別試験を合格へ書き換えない。
現行の次の作業は[v3準備](../current-work.md)。

## 変更

保存中の編集保持、独立した下書き復旧、保存して閉じる、Unicode検索、文字コード再読込、
workspace操作の部分成功、PDF/HTML画像欠落通知を修正した。
10 MiB保存上限、ファイル属性保持、Save As bookmarkと校正のみの構造検査を補強。
対応表・再現試験・設計上の限界は [品質レビュー](../reviews/2026-09-08-v2.9-quality-hardening.md)。
既存v2.9 Local Assistの上限・生成元・エラー分類とR2-cを含む。

## ソースと検証

npm/Tauri/Cargoの版数は2.9.0。製品runtimeの依存変更はない。クリーンCIで不足が再現したため、開発依存の
`@types/node 24.13.3`（および型定義の依存）を固定追加した。Cargo.lockは自アプリ版数のみ。
署名pkgは下記のクリーンソースから作成した。既存のv2.8候補の証跡を流用しない。

- 同一source `190e854d` の [frontend/macOS CI](https://github.com/lero003/hazakura-editor/actions/runs/34271531452) が成功。
- TypeScript / 全Vitest **234 files・2,061 tests** / Vite build成功。
- App Store surface **10 files・111 tests** 成功。
- Rust fmt / **382 passed・2 ignored**、Swift **5 tests**、評価判定Node **4 tests** 成功。
- Swiftは書込可能なmodule cacheと `--disable-sandbox` を指定して再実行。
  通常sandbox下のcache書込失敗を試験成功へ数えていない。

- npm audit: 指摘0件。
- cargo audit: vulnerability分類0件。保守停止・unsound・yankedの警告あり。
  今回はunmaintained 7・unsound 1・yanked 1の9警告。
  macOS target treeにglib/proc-macro-errorは無く、`ttf-parser`、`unic-*`、`chacha20 0.10.1`は残る。
  上流の保守追従を継続する。警告ゼロとはしない。
- Viteの大きいchunk警告、Local Assistの意味/日本語品質、長文性能は残る。

## 署名済みローカル候補

2026-09-09の外部再レビューN1/N2修正後に、未送信のbuild 125を再作成。修正前pkgは別保存。

- **2.9.0 / build 125**、universal arm64/x86_64。
- source: `190e854d`、build直前の `sourceDirty: false`。
- SHA-256: `c3adf108f7c3775021977754a0d4e57acd5b27afd7c736b899f5070bb51424b5`。
- 独立worktreeの既存clean installで型検査・全2,061 testsを確認し、通常のcandidateスクリプトで作成。
  既存のユーザーのbuild番号変更は元worktreeに保持。旧pkgは削除していない。
- App/helperのdeep/strict署名、universal構成、App Sandbox・user-selected read-write・
  app-scope bookmark・helper inheritを確認。LICENSEとTHIRD_PARTY_NOTICESが同梱されている。
- hostのpkgutilはApple発行証明書による署名を確認。
  `spctl` は今回のローカルMASアプリを `rejected` と判定。
  Developer ID配布やAppleの処理成功を示す証跡ではない。
- ローカルパスと詳細はignored `docs/internal/app-store-candidates/latest.json`。
  現物SHAと記録を再照合済み。外部差分再レビュー通過後、PR #45をmainへマージ。
  最新レビューhead 21c431e5のCIも成功。マージ1a97a697と候補sourceの差は文書のみ。

## 配布前の残項目

以下のpreview/ad-hoc追試は修正前候補の履歴。今回の190e854d署名候補の実機試験へ転用しない。
A1の完成HTMLは画像・CSS込み10 MiB上限を維持し、超過時は保存前に画像縮小等を案内する。

Apple署名候補でのIME・VoiceOver・旧OS・再起動、
Local Assistの長文・失敗応答・Sandboxでの追加受け入れ。
短文の別窓→生成→追加依頼取消→前案保持→Diff→明示反映→Undoはローカルpreviewで確認済み。
PDFの代表fixture全ページと、未保存章を含む本全体8ページを目視済み。HTML全体の目視はローカルfile URLに対するブラウザーの安全制限で未実施。
2026-09-09にローカルpreviewで文字コード・Undo・保存・Reader・HTML/PDFを追試。
PDFの末尾空白を修正し再出力で確認。詳細は品質レビューの追試欄。
ad-hoc App Sandbox診断では単独ファイルのSave As→再起動→復元→再保存を確認。
7テーマの代表編集画面と105k文字の追記・Undo・スクロールはローカルpreviewで目視/実操作済み。
IME・VoiceOver・旧OS・Apple署名候補/TestFlightでの確認は残る。

署名済みローカルpkgとSHA・sourceはignored `docs/internal/app-store-candidates/latest.json`へ記録する。
Apple upload・処理・TestFlight配布・App Review・公開は未実施。公開タグ・Releaseは作成しない。
コード修正が入れば候補を再構築し、build番号の未使用をApp Store Connectで確認してから送信する。
