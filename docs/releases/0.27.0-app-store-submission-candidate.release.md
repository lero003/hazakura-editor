# Hazakura Editor 0.27.0 App Store Submission Candidate

> **Helper-free App Store lane package candidate**

Status: Local package candidate generated on 2026-06-20. App Store
Connect upload, processing, TestFlight, App Review, and release handling
are performed outside this repository unless separately recorded.

`0.27.0` was prepared as the next App Store update after the reported
`0.26.0` App Store release and the `v0.27.0` source / local-app tag.
The App Store package was generated after the source tag so the package
build counter can advance without moving the source tag. Build `22`
supersedes the `0.26.0` build `21` package.

The release boundary stays unchanged: Markdown source remains canonical,
Safe Editor remains primary, and the App Store lane omits Agent
Workbench, CLI Agent launch, dev mode, arbitrary command execution,
Hazakura Local Assist helper, external AI/API calls, Git, LSP, terminal,
plugins, workspace-wide indexing, auto-apply, and auto-commit behavior.

## Candidate Identity

- User-visible version: `0.27.0`
- App Store build counter: `22`
- Bundle identifier: `dev.hazakura.editor`
- App name: `Hazakura Editor`
- Lane: helper-free App Store submission build
- Source commit: `9e86d95a`
- Local package:
  `src-tauri/target/universal-apple-darwin/release/bundle/pkg/HazakuraEditor-0.27.0-build22-mas.pkg`

## Local Package Snapshot

2026-06-20 local packaging smoke:

- `npm run candidate:app-store-pkg` passed.
- The wrapper generated ignored local candidate metadata at
  `docs/internal/app-store-candidates/HazakuraEditor-0.27.0-build22.json`.
- The wrapper reported App Store surface smoke as passed.
- `npm run build:app-store-pkg` advanced the App Store build counter
  from `21` to `22`.
- The submit-lane bundle reported `CFBundleShortVersionString=0.27.0`,
  `CFBundleVersion=22`, `CFBundleIdentifier=dev.hazakura.editor`, and
  `LSMinimumSystemVersion=26.0`.
- The submit-lane bundle is universal (`x86_64` / `arm64`) and signed
  with the Apple Distribution identity for team `8BNUB2R9C8`.
- The submit-lane bundle had the expected App Sandbox,
  user-selected read/write, app-scoped bookmark, and network-client
  entitlements.
- The submit-lane bundle omitted the Hazakura Local Assist helper and
  included bundled `LICENSE` / `THIRD_PARTY_NOTICES.md` resources.
- `REQUIRE_APP_STORE_ENTITLEMENTS=1 npm run probe:macos-distribution -- <app>`
  passed for bundle metadata, helper absence, bundled notices,
  codesign verification, and required App Store entitlements.
- `pkgutil --check-signature` passed for the generated package.
- Package SHA-256:
  `3cf8a09dcf4b3fd81d50ad330d552c0e7de30ec56713b2fc4b4f2a62ae913ff7`.
- `spctl` rejected or returned inconclusive local assessment for the
  signed app, which is treated as local trust-policy noise for this lane
  rather than App Store Connect validation evidence.

## Pre-Submission Checklist

- Upload `HazakuraEditor-0.27.0-build22-mas.pkg` through Transporter.
  Handled outside this repository.
- Confirm App Store Connect processing / validation has no untriaged
  warnings. Handled outside this repository.
- Run signed TestFlight smoke on the resulting `0.27.0` build,
  including launch, basic open/edit/save, Preview / e-book / Diff /
  Review right-pane controls, large-document startup behavior, central
  editor manual scrolling, dirty close behavior, Move to Trash, and
  App Store-lane omission of developer / agent / Hazakura Local Assist
  surfaces.
- Finalize App Store Connect metadata, screenshots, support URL,
  reviewer notes, privacy-policy URL, and update scheduling outside the
  tracked repository.

## Not Claimed By This Note

This note does not preserve raw proof for:

- uploaded to App Store Connect,
- processed or accepted by Apple validation,
- distributed through TestFlight,
- submitted to App Review,
- approved by App Review,
- released on the Mac App Store,
- a Developer ID signed or notarized DMG, or
- a public GitHub binary release.
