# Hazakura Editor 2.0.0 App Store Release Notes

Status: Published on the Mac App Store (user-reported 2026-07-21)
Scope: User-facing App Store / TestFlight release notes for package `2.0.0`
Authority: Medium
Last reviewed: 2026-07-21

This note boxes the **v2 Book Scope / structured Markdown** line shipped as
package/app version **`2.0.0`**. Source tag boundary:
`docs/releases/2.0.0-source-tag.release.md`. Local package provenance lives in
ignored `docs/internal/app-store-candidates/latest.json`.

Historical: `1.13.0` remains the prior published store baseline in docs history.
`1.14.0` was an intermediate source / submission line; the live listing is now
`2.0.0`. Do not rewrite published tags.

## App Store Connect Copy

Hazakura Editor v2.0は、選んだ Markdown を一冊として読み・書き出すための
「本」機能を追加し、知識フォルダ（OKF）との明示的な連携を整えたリリースです。

・サイドバーの「本」で、章にする Markdown を明示選択し、まとまりと並べ順を保存できます（ディスクのファイル並びは変えません）。上下移動は同じまとまりの中に留まります。
・ワークスペースから章候補を作れます。index.md の見出しと、そこからつながる下位 index.md の章リンク順を候補のまとまりに反映し、保存するまで候補のままです。
・「本全体を読む」で、保存した章順の読み取り専用スクロール読書ができます。
・PDF / EPUB 書き出しで「現在のファイル」か「本全体」を選べます。本全体は章順と未保存タブを反映し、利用できない章があるときは書き出しを止めます。EPUBでは保存した「本」のまとまりを階層目次に反映し、相対リンクや `/` から始まる本内リンクを読める形で保持します。
・知識フォルダ（OKF）の点検とひな形作成はこれまで通り明示操作のみ。自動修正や起動時スキャンはありません。
・ヘルプに「本と知識フォルダ」を追加し、操作の要点だけを短く説明します。

Markdown sourceを正本とし、外部AIへのnetwork fallback、auto-save、tool calling、workspace-wide indexingを行わない境界は変わりません。

## Keep Inventory (shipped in `2.0.0`)

| Theme | Keep (in `2.0.0`) |
|---|---|
| Book Scope | Files/Book sidebar; explicit chapter tree; same-group ordering; unavailable retention; one edit buffer |
| Suggestions | On-demand draft tree from index headings/links + readable `.md`; save only on confirm |
| Reading | Whole-book read-only reader; live dirty buffers; per-chapter image bases |
| Export | Current file / Whole book PDF·EPUB; bounded preflight; saved-tree navigation + packaged chapter links |
| OKF | Explicit review + starter scaffold (v1.11 / v1.12 contracts) |
| UX quieting | Settled Book view: read/edit primary; suggest/recheck progressive; selection Save/Cancel restores focus |
| Help | Native Help menu + Command Palette route to Books and knowledge folders; About / diagnostics derive package version 2.0.0 |

## Boundary

- Safe Editor remains primary; Markdown/text source remains canonical per file.
- Book tree is app-private; not a workspace manifest and not OKF book semantics.
- No background indexing, auto-apply, auto-save, or second editable buffer.
- App Store lane omits Agent Workbench, CLI Agent, arbitrary command execution,
  and external AI/API providers.
- Hazakura Local Assist remains on-device, explicit, unsaved, and reviewable.

## Explicit Non-goals

- vertical writing (縦書き)
- whole-workspace Quick Open / background index
- Git, LSP, terminal, plugins, provider-add UI
- OKF auto-repair or silent multi-file rewrite

## Product Promise

Markdownで書き、本として読み、ローカルAIで整える — いまは「本」を明示選択した
ファイル群に広げられる段階です。説明は Help に置き、画面に機能を並べすぎません。
