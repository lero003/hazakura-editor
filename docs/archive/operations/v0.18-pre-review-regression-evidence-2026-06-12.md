# v0.18 Pre-Review Regression Evidence

Status: Operational
Scope: Local release-readiness gate evidence before App Review
Authority: Medium
Last reviewed: 2026-06-12

## Summary

This records the local regression evidence captured for the v0.18
pre-review automation Order 9 slice. It is not a signing,
Transporter, TestFlight, or App Review approval record.

## Environment

- Date: 2026-06-12
- Project version: `0.18.0`
- Node: `v26.0.0`
- npm: `11.12.1`
- Cargo: `cargo 1.95.0`
- Rust: `rustc 1.95.0`

## Commands

| Command | Result | Notes |
|---|---|---|
| `npm ci` | Pass | 138 packages installed from lockfile; 0 vulnerabilities reported. |
| `npm run build:vite` | Pass | Vite build completed; existing dynamic-import and chunk-size warnings remain. |
| `npm test` | Pass | 90 files / 745 tests passed. |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | Pass | No formatting diff reported. |
| `cargo test --manifest-path src-tauri/Cargo.toml` | Pass after rerun | Initial full run failed once in `agent_workbench_real_runtime_passes_app_search_path_to_provider_process`; focused rerun passed, then full rerun passed with 277 Rust tests. Treat as a transient test-run signal, not an accepted product failure, unless it reproduces again. |
| `npm audit` | Pass | 0 vulnerabilities reported. |
| `cargo audit -f src-tauri/Cargo.lock` | Pass with warnings | 17 allowed RustSec warnings reported, mainly inherited GTK3 / related unmaintained advisories through Tauri Linux-side dependencies plus `glib` and `unic-*`; command exited 0. |
| `npm run build` | Pass | Helper-free App Store preview bundle built at `src-tauri/target/release/bundle/macos/Hazakura Editor.app`; ad-hoc signing and not-notarized warning are expected for local preview. |
| `npm run probe:macos-distribution -- "src-tauri/target/release/bundle/macos/Hazakura Editor.app"` | Pass | Bundle metadata probed; `LICENSE` and `THIRD_PARTY_NOTICES.md` present; helper absent; codesign valid; App Sandbox entitlements intentionally absent for launchable local preview. |

## Smoke Boundary

The built app was not launched for manual UI smoke during this slice.
This run only archives local regression gate evidence. Signed TestFlight
manual smoke remains separate.

## Follow-Up

- If the Agent Workbench PATH propagation Rust test fails again on a
  full run, investigate test isolation around the fake `node` shim and
  process `PATH` handling.
- Continue with the next open automation slice in `docs/current-work.md`.
