# v1.2 Review Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the three review findings in the v1.2 polish slice without expanding the Safe Editor boundary.

**Architecture:** The command palette will derive Markdown command behavior from the existing slash-command registry, while retaining palette-specific labels and IDs in a small metadata map. CSS and living-doc fixes remain local: one selector-strength correction and one release-phase synchronization across current-state docs.

**Tech Stack:** React, TypeScript, Vitest, CSS, Markdown.

---

### Task 1: Pin command-surface parity

**Files:**
- Modify: `src/hooks/commandPalette/useCommandPaletteController.test.ts`
- Modify: `src/hooks/commandPalette/useCommandPaletteController.ts`
- Modify: `src/hooks/app/useAppShellController.ts`

- [x] Add a test that supplies the real slash-command registry to the palette, requires every Markdown command including date/time, and requires table insertion to use the shared insertion text.
- [x] Run `npm test -- src/hooks/commandPalette/useCommandPaletteController.test.ts` and confirm the new expectation fails.
- [x] Add palette metadata keyed by slash-command id, derive `run` from each slash command, and pass `slashCommands` from `useAppShellController`.
- [x] Re-run the focused test and confirm it passes.

### Task 2: Pin EPUB note styling

**Files:**
- Modify: `src/styles/editorCss.test.ts`
- Modify: `src/styles/dialogs.css`

- [x] Add a CSS source test requiring the EPUB note selector to outrank `.close-dialog p`.
- [x] Run the focused CSS test and confirm it fails.
- [x] Change the selector to `.close-dialog .epub-export-settings-note` without altering unrelated dialog styles.
- [x] Re-run the focused CSS test and confirm it passes.

### Task 3: Synchronize release and phase docs

**Files:**
- Modify: `src/buildScripts.test.ts`
- Modify: `README.md`
- Modify: `docs/current-status.md`
- Modify: `docs/current-work.md`
- Modify: `docs/roadmap.md`
- Modify: `docs/development-automation.md`
- Modify: `docs/handoff.md`
- Modify: `docs/app-store-build.md`

- [x] Add a source-level docs test requiring `1.1.0` as the public App Store version and v1.2 as the active lane in living docs.
- [x] Run `npm test -- src/buildScripts.test.ts` and confirm the new expectation fails.
- [x] Replace stale candidate/public-version wording with concise current-state wording; preserve historical `1.0.0` evidence.
- [x] Re-run the focused test and confirm it passes.

### Task 4: Verify and hand off

**Files:**
- Modify: `docs/handoff.md`

- [x] Run `npm test`.
- [x] Run `npm run build:vite`.
- [x] Run `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`.
- [x] Run `cargo test --manifest-path src-tauri/Cargo.toml`.
- [x] Run `npm run build`.
- [x] Run `git diff --check`.
- [x] Record only the checks that actually passed and keep built-app interaction smoke explicitly pending if it was not exercised.
