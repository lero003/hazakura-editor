# Current Status

Status: Operational
Scope: Current implementation state and next safe actions
Authority: High
Last reviewed: 2026-06-03 (v0.9 `useAppShellController` 1473-line split lane landed)

## Current State

- A touchable Tauri desktop prototype exists.
- Current published preview is `v0.8.0` at `https://github.com/lero003/hazakura-editor/releases/tag/v0.8.0`. It is a warning-expected DMG preview: ad-hoc signed, not notarized, and expected to trigger Gatekeeper warnings. Older public tags remain immutable.
- Current development lane is post-v0.8 follow-up triage and small Safe Editor quality slices. v0.7 shipped as a Review Desk MVP, not as the full editable two-column Review Desk workbench. The 2026-06-03 post-v0.8 feature-folder refactor lane (5 slices, 6 commits, no behavior change) is now complete; the front-end layer is fully split by feature area (`lib/tauri/*` and `lib/locale/*` barrels, `src/App.tsx` is now a 6-line pure composition file calling `useAppShellController()` and rendering `<AppShell {...} />`), the Rust test layer is fully per-module (`src-tauri/src/tests/*`), the Agent Workbench session state machine is separated from the Tauri command shims, the shared CodeMirror highlight is shared, and the format helpers + file preflight are deduped.
- 2026-06-03 v0.8 release-readiness and publication pass: version surfaces were aligned to `0.8.0`, `docs/releases/0.8.0-warning-expected-dmg-preview.release.md` was added as the release body, `docs/agent-workbench-boundary.md` was corrected so `claude` is documented as implemented through the same allowlist / launch gate as `codex` / `opencode` / `pi`, and `search_workspace_files_with_label` now caps `total_files_scanned` at `MAX_WORKSPACE_SEARCH_FILES` even when the query matches no files. The regression test `search_caps_files_visited_and_marks_truncation` first failed on the old no-match behavior, then passed after the cap fix. Local release gates passed, the GitHub Release was published as `v0.8.0`, and the remote assets were re-downloaded and verified with `shasum -c`, `hdiutil verify`, mounted-app metadata checks, and `codesign --verify --deep --strict --verbose=2`. DMG SHA-256: `c6e128b7519c5a3ffc16bd27cb32d804c1daff0b2f2530c5976925b18046659d`.
- 2026-06-03 detached Agent window layout fix slice restores the agent-terminal flex layout that was orphaned when `src/styles/agent-pane.css` was deleted in the right-pane removal commit (`daccd98`). `src/styles/agent-window.css` now defines `.agent-window-shell .agent-terminal-shell` (flex column, `xterm-bg` background, `overflow: hidden`), `.agent-window-shell .agent-terminal-host` (`flex: 1 1 auto`, `min-height: 0`), and `.agent-window-shell .agent-terminal-placeholder` (grid place-items center, fills remaining flex space), and the shell's grid template widened from `auto auto minmax(0, 1fr) auto` to `auto auto auto minmax(0, 1fr) auto` so the conditional Start panel sits on its own auto row instead of stealing the `1fr` from the terminal. `src/components/agent/AgentTerminalView.tsx` `fitAndNotify()` now bails out when `proposeDimensions().cols` or `.rows` is non-finite, so the "Agent terminal resize failed: invalid args `columns` for command `resize_agent_workbench_terminal`: invalid type: null, expected u16" status footer no longer fires before xterm has a measurable host height. No Rust change, no IPC contract change, no Agent Workbench boundary widening. Verification: `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (122 tests, 0 failed, 0 ignored), `npm run build:vite`, `git diff --check`. Built-app smoke of the agent window before / during / after a Start → Stop cycle is not claimed in this automation environment (the same `open -n` / in-app browser policy blockers as prior slices apply); the user should confirm the four-row layout, the absence of the resize footer error, and that Start / Stop no longer reshuffle the terminal area.
- 2026-06-03 Command Palette slice adds a `Cmd+Shift+P` modal command launcher that mirrors the Quick Open shape (overlay + input + scrollable list, no theme / no shadow / no accent). `src/hooks/commandPalette/useCommandPalette.ts` keeps the latest commands array in a ref so the filtered memo does not depend on handler identity, with a small fuzzy scorer (exact > label-prefix > word-start > substring > category > keyword > subsequence) and a 30-entry default command list across File / Edit / View / Review / Agent / Settings — every entry is a real handler already wired into the controller, so the palette is a thin launcher rather than a new plugin surface. The `useAppKeyboardFocusEffects` hook now combines `commandPaletteVisible || modalOpen` and threads the combined flag into `useGlobalKeyboardShortcuts` and `useModalKeyboardGuard`; the global `Cmd+Shift+P` shortcut is gated on `!modalOpen` at the top of the handler, and the modal Esc priority is now `palette > close-tab > app-close > preferences`. `src/components/commandPalette/CommandPalette.tsx` (focused input, Arrow / Enter / Esc, hover-to-select, mouse `onPointerDown` to run) renders inside `AppOverlays` so the existing Quick Open / preferences / dirty-tab / app-close overlays all stay where they are. No Rust change, no IPC contract change, no Agent Workbench boundary widening, no dependency or lockfile change, no new keybindings outside the existing palette shortcut. Verification: `npm run typecheck`, `cargo test --manifest-path src-tauri/Cargo.toml` (122 tests, 0 failed, 0 ignored), `npm run build:vite`, `npm run build` (full Tauri bundle, signed with `-`, notarization skipped as expected), `git diff --check`. Built-app smoke of the palette is not claimed in this automation environment (`open -n` blocker applies); the user should confirm `Cmd+Shift+P` opens / dismisses the palette, fuzzy filtering, Arrow / Enter / Esc navigation, mouse hover-to-select, and that the four dialogs still close on Escape in the documented priority.
- 2026-06-03 v0.8 candidate polish round lands three small slices on top of the Command Palette: (1) **Bounded Global Search (`Cmd+Shift+F`)**. A new Rust command `search_workspace_files_with_label(label, root, query)` in `src-tauri/src/commands/search.rs` walks the workspace under the same `should_skip_workspace_dir` exclusions used by the lazy tree (skips `.git`, `node_modules`, `target`, `dist`, `build`, `.cache`, `.next`, `.turbo`, `.hg`, `.svn` and any binary-looking file by the same `looks_binary` sniff), gated on `ensure_label_is_main`, with hard caps of 500 files visited, 10 matches per file, 200 matches total, and 4096 bytes per result line. The new `WorkspaceSearchResult { files, total_matches, total_files_scanned, truncated }` and `WorkspaceSearchFileResult` / `WorkspaceSearchMatch` types are added to `src-tauri/src/types.rs`; 12 unit tests in `src-tauri/src/tests/search.rs` pin the boundary (case-insensitive substring, empty-query no-op, excluded-dir / binary-file skip, per-file / total / files-visited caps marking `truncated`, missing root rejection, non-main label rejection, long-line truncation, symlink containment). The walk uses `entry.file_type().is_symlink()` plus a `fs::symlink_metadata` re-check so a symlink inside the workspace cannot route reads outside the canonical root, mirroring the `auto_backup.rs` hardening from the 2026-06-01 bug-check pass. The TS side adds `searchWorkspaceFiles(root, query)` in `src/lib/tauri/workspace.ts`, a `useGlobalSearch({ workspaceRoot, onOpenMatch })` hook in `src/hooks/globalSearch/useGlobalSearch.ts` (debounced 150 ms, stale-response drop via `requestSeqRef` / `latestSeqRef`, flattened `GlobalSearchRow[]` + summary, `String(error)` rejection stringification so the actual Rust error message surfaces), and a `GlobalSearch` modal in `src/components/globalSearch/GlobalSearch.tsx` that mirrors the Command Palette shape (overlay + input + scrollable list, file-group headers, per-row line + clipped text, Arrow / Enter / Esc, early-return on `isImeComposing` so Japanese / kana conversion is not intercepted, dedicated en / ja / kana copy). Opening a match calls `openWorkspaceFile(path).then(() => editorPaneRef.current?.goToLine(line))` so the click lands the cursor on the matched line. The Command Palette gets a new `Find in Files…` entry (`⇧⌘F` shortcut hint). Esc priority is now `palette > search > close-tab > app-close > preferences`. `src/styles/global-search.css` type-qualifies its hover rules (`button.global-search-item:hover` / `button.global-search-item.active`) to avoid the global `button:hover:not(:disabled)` specificity trap from the prior hover-fix slice. (2) **Pinned files in the start panel**. `RecentEntry` gains a `pinnedAt: number | null` field with a back-compat normalizer in `src/lib/storage.ts` (`normalizeRecentEntry` fills `pinnedAt: null` for legacy entries; `recentEntryOrder` sorts pinnedAt desc, then openedAt desc; new `pinRecentEntry` / `unpinRecentEntry` helpers). `useRecentEntries` exposes `pinRecentFile` / `unpinRecentFile`; `StartPanel.tsx` is refactored to render two groups (Pinned / Recent) with star buttons (★ / ☆) and filters pinned paths out of the recents list. New `pinnedFiles` / `pinFile` / `unpinFile` keys land in `SafeEditorCopy` for en ("Pinned files" / "Pin file" / "Unpin file"), ja ("ピン留めのファイル" / "ピン留めする" / "ピン留めを外す"), and kana ("いつもこのふみ" / "いつもこのふみにする" / "いつもこのふみをやめる"). `EditorMainPane` and `AppWorkspace` thread the new `pinnedFiles` + `onTogglePinRecentFile` props through. (3) **Print-ready HTML export polish**. The two HTML templates in `src/hooks/document/useDocumentExport.ts` (`exportPdf` opens a temp HTML for the print dialog; `exportHtml` writes a standalone HTML file via Save As) gained a screen type stack with `--font-body` / `--font-mono` CSS vars and a matching `@media print` block: `@page { margin: 18mm 16mm }`, serif body (`"Iowan Old Style"` → `Charter` → `Georgia` → `Times New Roman` → `serif`) at 11pt / line-height 1.45, `page-break-after: avoid` on headings, `page-break-inside: avoid` on pre / blockquote / table / img, no pre / code backgrounds for paper, link URL annotations via `a[href^="http"]::after { content: " (" attr(href) ")" }`. The standalone `exportHtml` template's `lang="ja"` typo is fixed to `lang="en"`. The Agent Workbench boundary, Review Desk surface, right-pane compare route, native menus, IPC contract, and dependency / lockfile are unchanged. No new Agent provider, no provider-add UI, no project-wide indexing, no Git integration. Verification: `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (134 tests, 0 failed, 0 ignored; +12 from the prior 122), `npm run build:vite`, `npm run build` (full Tauri bundle, signed with `-`, notarization skipped as expected), `git diff --check`. Built-app smoke of the three slices is not claimed in this automation environment (`open -n` / in-app browser policy blockers); the user should confirm `Cmd+Shift+F` opens / dismisses the Global Search modal, debounced search returns file-grouped results with line numbers, Enter / click on a row opens the file at the matched line, the Pin / Unpin star toggles persist across restart and split the start panel into two groups, and that printed HTML / PDF uses the new serif print layout.
- 2026-06-03 v0.8 candidate polish round audit + sister-bug pass. The audit reviewed the three v0.8 candidate polish slices (bounded Global Search, pinned start-panel files, Print-ready HTML export) for security boundaries, bugs, and philosophical alignment, then extended to the two sister modals (Command Palette, Quick Open) that share the same input-keyboard-handler shape. **Bugs (sister modals)** — the existing `CommandPalette` and `QuickOpen` modals from the earlier v0.8 Command Palette slice intercepted Enter / Escape / ArrowUp / ArrowDown in their `handleKeyDown` handlers even during active IME composition, breaking Japanese / kana conversion. (The `GlobalSearch` modal from the prior commit already had the guard baked in.) Both modals now early-return on `isImeComposing(event.nativeEvent)` from `src/lib/keyboard.ts`, matching the find-hook pattern in `useFindReplaceActions` and the new `GlobalSearch` modal. **Philosophical / quality** — `QuickOpen` placeholder / empty copy used `menuLanguage !== "en"` and collapsed kana into the Japanese strings, so the kana mode was indistinguishable from Japanese. `QuickOpen` now has a 3-way branch (en: "Type a file name..." / "No matching files"; ja: "ファイル名を入力..." / "一致するファイルがありません"; kana: "ふみのなまえを..." / "ぴったりのふみはありません"), matching the `safeEditor.ts` / `slashMenu.ts` pattern. No new Agent provider, no provider-add UI, no project-wide indexing, no Git integration, no Rust capability widening, no IPC contract change, no dependency / lockfile change. Verification: `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (134 tests, 0 failed, 0 ignored), `npm run build:vite`, `npm run build` (full Tauri bundle, signed with `-`, notarization skipped as expected), `git diff --check`. Built-app smoke of the audit fixes is not claimed in this automation environment; the user should confirm Japanese / kana IME conversion in Command Palette and Quick Open is not intercepted by Enter / Esc / Arrow, and that kana-mode Quick Open now reads "ふみのなまえを...".
- 2026-06-03 kana 3-way split residual sweep extends the Quick Open `safeEditor.ts` / `slashMenu.ts` pattern to the remaining surfaces that were still using the 2-way `menuLanguage !== "en" ? <ja> : <en>` shape and collapsing kana into the Japanese branch. The sweep touches 18 files across diff/review (`src/components/diff/DiffBody.tsx`, `DiffSetupPane.tsx`, `FileCompareView.tsx`, `src/components/review/ChangeReviewView.tsx`, `ReviewSurface.tsx`), editor chrome (`src/components/editor/ScrollPositionHud.tsx` — line-counter unit gains `ぎょう` for kana), slash menu / editor commands (`src/hooks/editor/useEditorCommands.ts` — 26 ternaries replaced with a `loc(kana, japanese, english)` closure helper that branches on `isKanaStyle(menuLanguage)` / `isJapaneseMenuLanguage(menuLanguage)`; covers Markdown heading / list / quote / code-block / divider / table / link / image / bold / italic / inline-code / strikethrough labels and hints, today/now timestamp commands, the shortcut-list insert text, and the Review Desk / send-to-Agent command labels), Agent Workbench helpers (`src/features/agent/agentWorkbench.ts` — `agentSessionStateLabel`, `agentCompactSessionStateLabel`, `localizeAgentGateMessage` plus an extracted `localizeSessionState(key, menuLanguage)` helper with 5 session-state keys; `src/hooks/agent/useAgentSessionLifecycle.ts` — 3 status messages; `src/hooks/agent/useAgentWorkbenchPreferenceActions.ts` — 8 preference status messages spanning mode toggle, restart, consent, provider change), file opening (`src/hooks/document/useFileOpening.ts` — 3 workspace-relative link messages), document status (`src/hooks/document/useDocumentStatus.ts` — 7 status helpers plus `formatFileType` / `formatSelectionInfo` / `formatActiveEditorStatusDetail`), workspace context menu (`src/components/workspace/WorkspaceContextMenu.tsx` — 11 strings), compare hooks (`src/hooks/diff/useCompareExecution.ts` — 3 helpers and `localizeCompareError` now takes `menuLanguage`; `src/hooks/diff/useCompareSetupActions.ts` — 8 setup-action helpers), close dialogs (`src/components/app/CloseDialogs.tsx` — used a `lang !== "en"` shape on a separate `lang` parameter, both `getCloseDialogCopy` and `formatDirtyTabCount` fixed), and the shared `src/lib/utils.ts` (`localizeCompareError(message, menuLanguage = "en")`) / `src/lib/format.ts` (`formatLineEndingKind` gains `こんざい` / `かいぎょう なし`). All kana strings stay in 歴史的仮名遣い-leaning hiragana to match the existing `safeEditor.ts` / `slashMenu.ts` precedent (e.g. みだし, かじょが き, いんよう, こーど ぶろっく, てーぶる, りんく, がぞう, ふとじ, しゃたい, きょうの ひづけ, ぎょう, うちけし せん, とぢる, ゐる). No behavior change for English or Japanese users — the existing ja branches were renamed to be conditional on `isJapaneseMenuLanguage(menuLanguage) && !isKanaStyle(menuLanguage)` and kept their exact strings — and kana-mode users now read kana copy at every surface that previously fell through to the Japanese branch. No new strings in en or ja branches, no Rust change, no IPC contract change, no Agent Workbench boundary widening, no dependency or lockfile change, no new keybindings, no new menu items, no `lib/locale/*` barrel change. Verification: `npm run typecheck`, `npm run build:vite`, `git diff --check`. `cargo` is unchanged (TS-only). Built-app smoke of the kana mode across all 18 surfaces is not claimed in this automation environment; the user should toggle Preferences > Menu Language > かなふみ and spot-check diff/review buttons (Compare / Apply / dismiss / column headers), the slash menu (`Cmd+B` / `Cmd+I` / `Cmd+E` / `Cmd+K` Markdown format labels, today/now timestamps, shortcut list), Agent Workbench status footer (session state, gate error, send-to-Agent path), close-tab and close-app dialogs, workspace right-click menu, scroll-position HUD (`ぎょう` unit), and document status bar (file-type / encoding / line-ending / final-newline / selection summary) — all should now show kana copy instead of the prior Japanese fallback.
- 2026-06-03 TypeScript hook unit-test layer bring-up adds Vitest 4 + jsdom 29 + @testing-library/react 16 (plus the auto-resolved @testing-library/dom 10 peer dep) as `devDependencies` so the front-end hook layer can have its own pinned-behavior tests alongside the existing 134-test Rust `cargo test` layer. `vitest.config.ts` keeps the test runner config isolated from the production `vite.config.ts` (no jsdom or testing-library leakage into the build output); `environment: "jsdom"`, `globals: false` (prefer explicit imports), `setupFiles: ["src/test-setup.ts"]`, `include: ["src/**/*.test.{ts,tsx}"]`, `css: false`, `clearMocks: true`, `restoreMocks: true`. `src/test-setup.ts` works around an upstream interaction between vitest 4 and Node 22+: Node ships an experimental `localStorage` global that requires `--localstorage-file` to actually work, and vitest's `populateGlobal` skips jsdom's real `localStorage` because the key already exists on the runtime global — so hooks that touch `window.localStorage` (e.g. `useAgentWorkbenchPreferences`) would throw `TypeError: Cannot read properties of undefined (reading 'clear')`. The setup file reads jsdom's real Storage off `globalThis.jsdom.window` and reassigns it onto the shared global so tests see the same behavior the app sees in the browser. Two new `npm` scripts: `test` (one-shot `vitest run`) and `test:watch` (interactive). The first 3 hook tests land alongside the infra: `src/hooks/agent/useAgentOutputSeqCursor.test.ts` (5 tests — initial undefined, advance-on-update, ignore non-advancing, reset-on-key-change, no-reset-on-same-key), `src/hooks/agent/useAgentWorkbenchPreferences.test.ts` (7 tests — Safe Editor defaults, localStorage read on mount, frozen-active behavior, persist on change, allowlisted-provider acceptance, codex fallback for unknown provider, `agentWorkbenchAvailable = active && consent` derivation), `src/hooks/find/useFindReplaceState.test.ts` (9 tests — initial state, setter isolation, case-insensitive match count, case-sensitive option, whole-word option, valid regex matches, invalidRegex flag for malformed regex, no-flag when regex option is off, source-prop change re-computes). Total: 21 tests / 3 files / `~390ms` cold start. No Rust change, no IPC contract change, no Agent Workbench boundary widening, no behavior change at any user-visible surface, no new app code (only test code + config + 4 new devDependencies in `package.json` + the resulting `package-lock.json` delta). Verification: `npm run typecheck`, `npm test` (21 tests, 0 failed), `npm run build:vite`, `git diff --check`. `cargo` is unchanged (TS-only). The infra is intentionally minimal: no `@testing-library/jest-dom` matchers yet (the first 3 hook tests don't need DOM matchers); no coverage tool; no MSW-style request mocking; no Tauri IPC stub layer. Future hook tests should pick targets that don't call into Tauri (or stub the small surface they need at the call site); the Rust runtime stays covered by `cargo test --manifest-path src-tauri/Cargo.toml`.
- 2026-06-03 second TS hook unit-test pass adds 3 more hook test files on top of the infra + first 3 from the prior bullet, covering the remaining pure-logic patterns in the find / diff / document folders that have no Tauri, IndexedDB, or DOM-hit-test dependencies. `src/hooks/diff/useCompareState.test.ts` (8 tests — initial null anchor / target / view and empty `Map`, independent setters, `setCompareCaseEntry` storage, same-key overwrite, the same-entry Map-reuse shortcut returns the same Map reference when `current.size === 1 && current.get(entry.key) === entry` so the consumer memo isn't invalidated, fresh-Map on actual change, `getCompareCaseByKey` lookup-or-undefined, get-after-overwrite), `src/hooks/find/useFindMatches.test.ts` (12 tests — empty and whitespace-only query return no matches, `invalidRegex` only flags when the regex option is on AND the trimmed query is non-empty (so an empty / whitespace query never flags and a `(unclosed` pattern with regex off never flags), plain and regex matches against the same source, the 999-match cap for both plain and regex paths, recomputation when source / query / options change), `src/hooks/document/useDocumentOutline.test.ts` (15 tests — `hasActiveDocument: false` short-circuits to a null outline, basic `# / ## / ###` parsing reports `currentMarkdownHeading` for the selection line, code-fence skipping for both ``` and ~~~ markers, truncation at `MARKDOWN_OUTLINE_MAX_HEADINGS` setting `truncated: true`, empty-source no headings; plus `useMarkdownHeadingContext` covering above-all / between / past-last / empty-list cases; plus `clampScrollRatio` covering NaN / ±Infinity / sub-zero / over-one / in-range). Total: 56 tests / 6 files / `~430ms` cold start. No Rust change, no IPC contract change, no Agent Workbench boundary widening, no behavior change at any user-visible surface, no new app code (test code only). Verification: `npm run typecheck`, `npm test` (56 tests, 0 failed), `npm run build:vite`, `git diff --check`. The remaining low-risk hook target from the prior bullet's list is `useTabReorder` (pointerdown / pointermove / `setPointerCapture` / `document.elementFromPoint` / `getBoundingClientRect` — the move-order callback itself is pure, but the pointer-handler surface needs `document.elementFromPoint` and `setPointerCapture` stubbed to test the full hook).
- 2026-06-03 v0.9 `useAppShellController` 1473-line split lane lands as 8 incremental, behavior-preserving slices with **no behavior, type, capability, IPC, props, dependency, or lockfile change** at any surface the user can see. Orchestrator went from 1473 → 1052 lines (29% reduction) and the leaf-hook call surface is now fronted by 8 named, single-purpose composers in `src/hooks/`: (1) `useAppShellFoundation` (state pool: 18 dep-free leaf hooks bundled into one tested surface), (2) `useAppShellRefs` (2 ref-hook leaves bundled), (3) `useEditorFindController` (`useFindReplaceController` + `useGoToLine`, 23 fields), (4) `useDocumentCoreController` (`useEditorTabState` + `usePastedImageAction`, 11 fields; `useActiveDocumentIdentity` deferred because it depends on `selectedImage` from a downstream hook), (5) `useDocumentPreviewController` (`useImagePreview` + `useActiveDocumentIdentity`, 9 fields, the cross-section handoff is now internal), (6) `useEditorSurfaceController` (`useSidePaneController` + `useActiveDocumentSurface`, 22 fields, the `sidePaneMode` wiring is now internal), (7) `useAgentWorkbenchController` (preference + session + terminal actions, 12 fields, the `refreshAgentSessionState` wiring is now internal), (8) `useCommandPaletteController` (Command Palette + Global Search, 20 fields; the 30-entry command list and the `handleOpenSearchMatch` callback moved out of the orchestrator into the composer), and the final (9) `useAppShellSideEffectsController` (`useAppMenuIntegration` + `useAppRuntimeEffects` bundled, gives the orchestrator a single "side effects" section). 9 new shape-test files (one per new composer, including the side-effects controller's `result.current` undefined assertion) bring the Vitest total to 65 / 10 files. Hard boundaries upheld throughout: no Rust change, no IPC contract change, no Agent Workbench boundary widening, no Review Desk / right-pane compare-route change, no native menu / window / theme / dialog widening, no new dependency or lockfile change. Each slice was a real composition (not a rename) where the new composer owns the cross-section handoff internally — `selectedImage` (C-3), `sidePaneMode` (C-4), `refreshAgentSessionState` (D), `editorPaneRef.current?.goToLine` (E-1), and the menu + runtime sub-objects (E-2). Verification per slice: `npm run typecheck`, `npm test` (65 tests, 0 failed), `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (134 tests, 0 failed, 0 ignored), `npm run build:vite`, `git diff --check`. Built-app smoke of the split surface is not claimed in this automation environment; the user should confirm find/replace (`⌘F`), go-to-line (`⌘L`), command palette (`⌘⇧P`), global search (`⌘⇧F`), the Agent Workbench action surface (mode toggle, consent, provider change, session start/stop, terminal input/resize, send-to-Agent), and the menu integration (recent files, settings, theme, preview) all behave identically to v0.8.0. Each commit uses the `Co-Authored-By` trailer suppression rule; primary author is `lero003` via local git config.
- Current automation emphasis should move from v0.7 release-prep to post-release triage, focused quality checks, Safe Editor daily-use polish, and v0.8 Assist Surface planning docs unless the user explicitly opens a patch-release lane. The post-v0.8 refactor lane is now complete, the 2026-06-03 v0.8 candidate polish round landed three more slices (bounded Global Search, pinned start-panel files, Print-ready HTML export polish) on top of the Command Palette slice, the follow-up audit pass closed the sister-modal IME composition bug and Quick Open kana copy, the broader 18-file kana 3-way split residual sweep brought the rest of the surfaces (diff/review, slash menu, Agent Workbench status, document status, workspace context menu, close dialogs, compare hooks, file opening) onto the same `isKanaStyle` / `isJapaneseMenuLanguage` pattern, and the TS hook unit-test layer bring-up landed Vitest 4 + jsdom 29 + @testing-library/react 16 plus 6 hook tests across find / diff / document / agent (`useAgentOutputSeqCursor`, `useAgentWorkbenchPreferences`, `useFindReplaceState`, `useCompareState`, `useFindMatches`, `useDocumentOutline` + `useMarkdownHeadingContext` + `clampScrollRatio`; 56 tests total), and the v0.9 `useAppShellController` 1473-line split lane just landed (8 incremental, behavior-preserving slices — see the bullet above for the composer list, 65 tests total, 1473 → 1052 lines). The next run can pick from remaining v0.8 candidate work (further daily-editor polish, Markdown toolbar polish), v0.9 candidate work (calm Markdown workspace, bounded file-tree operations, Frontmatter, keybinding customization, KaTeX beta), the deferred v0.8 helper `EditorTab.line_ending` rename, additional TS hook unit-test bring-up (next safe target is `useTabReorder`, the only remaining low-risk hook — pointer-event surface needs `document.elementFromPoint` / `setPointerCapture` / `getBoundingClientRect` stubbed but the reorder callback itself is pure), or a one-off feature slice the user explicitly opens.
- The v0.7 publication pass on 2026-06-02 passed `npm ci`, `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build:vite`, `npm run build`, `npm audit`, `cargo audit --file src-tauri/Cargo.lock`, `npm run build:dmg-preview`, local checksum verification, local `hdiutil verify`, built-app and mounted-app metadata checks, and `codesign --verify --deep --strict --verbose=2`. Remote verification downloaded the published GitHub Release assets, confirmed `shasum -c`, `hdiutil verify`, mounted app metadata, and codesign verification.
- v0.6 has 10/10 roadmap items implemented after the App.tsx split MVP, was published as a warning-expected DMG prerelease, and should remain immutable.
- Latest post-v0.8 feature-folder refactor lane landed five pure code-organization slices on 2026-06-03 with **no behavior, type, capability, or contract change** at any surface the user can see. (1) `lib/tauri.ts` and `lib/locale.ts` were split by feature area into `src/lib/tauri/{_runtime,agent,app,autoBackup,dialog,files,menu,theme,window,workspace,index}.ts` and `src/lib/locale/{_helpers,agentWorkbench,editorChrome,preferences,recovery,reviewDesk,safeEditor,sidePane,slashMenu,index}.ts`; both keep a thin `index.ts` barrel so existing `import { … } from "./tauri"` / `import { … } from "./locale"` call sites are unchanged. (2) `src-tauri/src/tests.rs` (3378 lines, 122 tests) was split into `src-tauri/src/tests/{mod,common,agent_workbench,app_window,backup,encoding,files,security,util,workspace,workspace_broadcast}.rs`; shared fixtures (`RecordingRuntimeAdapter`, `FakeProviderFixture`, `unique_test_dir`, `fake_provider_fixture`, `wait_for_agent_state`, `combined_agent_output`, `assert_agent_output_seq_strictly_increases`, `make_executable`, `process_exists`, `RuntimeAdapterCall`) live in `tests/common.rs` with `pub(crate) use std::path::Path; pub(crate) use std::process::Stdio; pub(crate) use std::time::Duration;` re-exports so the per-feature test modules can stay focused. (3) The Agent Workbench session state machine moved out of `src-tauri/src/commands/agent_workbench.rs` into `src-tauri/src/agent.rs`; the commands file is now Tauri-shim-only with `*_with_label(label, …)` wrappers, and the state-machine functions (`start_agent_workbench_session_with_store`, `stop_agent_workbench_session_with_store`, `get_agent_workbench_session_state_with_store`, `get_agent_workbench_session_state_since_with_store`, `write_agent_workbench_session_input_with_store`, `resize_agent_workbench_terminal_with_store`, `refresh_agent_workbench_session_exit`, `build_agent_workbench_preflight`, `validate_agent_workbench_launch`) own all the runtime logic. (4) The shared Markdown `HighlightStyle` was extracted to `src/features/editor/codeMirrorTheme.ts` (`markdownHighlightStyle()` + `markdownSyntaxHighlighting()`); the duplicate definitions in `EditorPane.tsx` and `CandidateEditor.tsx` were removed. (5) Two small consolidations: `formatLineEndingKind` and `formatTextEncoding` moved to `src/lib/format.ts` so `useDocumentStatus.ts` and `useEditorCommands.ts` share the same enum-to-label helpers; and `open_text_file_with_label` in `src-tauri/src/commands/files.rs` now calls the existing `readable_text_metadata(&path_buf)?` helper instead of duplicating the is_file / size / binary preflight checks, so the open and metadata commands can't drift on the preflight rules. Hard boundaries upheld throughout: no Rust capability widening, no Agent Workbench boundary change, no Review Desk / right-pane compare-route change, no dependency or lockfile change, no `src/hooks/agentWorkbench/` restructure, no provider-add UI, no new Agent provider / API surface, no docs claim beyond the implemented subset. Verification per slice: `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (122 tests, 0 failed, 0 ignored), `npm run build:vite`, `npm run build` (full Tauri bundle, signed with `-`, notarization skipped as expected), `git diff --check`. Built-app smoke of the refactor surface is not claimed in this automation environment (the same `open -n` / in-app browser policy blockers as prior slices apply); the user runs the standard manual smoke per the existing `docs/smoke-checklist.md` rows. Deliberately left out of scope per the user-approved "no behavior change" constraint: the `EditorTab.line_ending` → `lineEnding` rename (would require a localStorage key migration line and was judged invasive) and the `useSlashMenu` `setTimeout` cleanup (would be a strict-mode behavior change).
- Latest v0.7 manual candidate MVP slice added the manual candidate paste area + diff preview on the `chore/v0.7-review-desk-manual-candidate` branch (R-4 next). `CompareCase` gained `kind: "candidate"`; `useReviewDeskState` now owns the input text, diff view, and error; `ReviewSurface` reuses `DiffBody` for the preview. The right-pane compare route, the `DiffPane` / `FileCompareView` / `ChangeReviewView` shape, the existing review / diff / tab / workspace flows, and the `compareSource` / `compareTarget` slots are all unchanged. Apply / dismiss log and persistent `.hazakura/reviews/` storage remain deferred (R-3).
- Latest v0.8 Review Desk candidate editor foundation slice replaced the manual-candidate `<textarea>` with a self-contained CodeMirror 6 editor (`src/components/CandidateEditor.tsx`, `basicSetup` + `markdown()` + `indentWithTab`). The candidate side now renders Markdown syntax highlighting, line numbers, Tab/Shift+Tab indentation, the active editor's theme/fontSize/tabSize/wrapLines, and spellcheck state. `useReviewDeskState` is unchanged so the existing candidate edit / tab / buffer safety guards all still bind to the same `value` + `onChange` contract; Compare / Apply remain explicit; the right-pane compare route, `DiffPane` / `FileCompareView` / `ChangeReviewView` shape, and `compareSource` / `compareTarget` slots are all untouched. Future Review Desk workbench expansion is deferred until Apple Foundation Models / Apple Local Assist work in v0.11+ makes the candidate-review flow truly useful.
- Latest v0.8+ Detached Agent Window workstream adds a parallel Tauri child window (`agent.html` + `src/agentEntry.tsx` + `src/components/AgentWindowApp.tsx`) that hosts a UI mirror of the Agent Workbench session state from Rust, alongside the existing right-pane Agent path. The spike landed first (plain-text scrollback + textarea + Stop / Send) and was followed by an xterm parity slice (the agent window now reuses the right-pane `AgentTerminalView` — xterm + addon-fit + ResizeObserver — so it can drive a real PTY, plus the Rust gate on `resize_agent_workbench_terminal` was widened from `main`-only to `main | agent` with 6 new unit tests pinning the boundary; the right-pane Agent path is unchanged and kept for verification). The window is intentionally thinner than the right-pane Agent pane: it reads the session/output buffer from Rust via the existing 200 ms polling cadence and exposes four IPC actions — Stop session, send input (xterm `onData`), resize the PTY (fit-addon `ResizeObserver`), and a no-op get-state poll — gated server-side on `main | agent` for the three session commands + resize and on `main` for `open_agent_window`. It does NOT trigger the launch-gate check, change provider, toggle consent, start a second session, persist window position, or reflect mode-toggle changes in real time. Rust is the single source of truth: the `open_agent_window` command is **server-side gated** on `agent_workbench_active && agent_workbench_consent` (defense in depth — the menu item is also gated client-side, but a direct IPC call must not be able to spawn the window when Agent Workbench is disabled or consent is missing). The two flags are mirrored from the existing `update_app_menu_state` call site into `Arc<AtomicBool>` fields on `AgentWorkbenchSessionStore`. The window adds exactly one new Rust command (`open_agent_window`) — the mirroring reuses the existing `update_app_menu_state` IPC, so the new IPC contract is one command, not two. The agent window's capability footprint is **truly minimal and isolated**: a new `src-tauri/capabilities/agent-window.json` is scoped to `"windows": ["agent"]` and grants **no core or plugin permissions at all** (empty `permissions` array) so the child webview cannot emit events, manipulate its own or other windows or webviews, or touch the menu / tray / dialog subsystems. The main window's `src-tauri/capabilities/default.json` is **unchanged** (still at `["main"]`, not widened) so the main window's permission set stays exactly as it is. Because Tauri v2's capability model gates plugin / core APIs by window label but auto-allowlists custom `#[tauri::command]` functions for any window in a capability, the agent window could still call every project command from its webview. A second server-side layer — `ensure_label_is_main` / `ensure_label_is_main_or_agent` label-only helpers in `src-tauri/src/lib.rs`, called from `*_with_label(label: &str, ...)` bodies that each tauri command wrapper delegates to (a small set of main-only commands without unit-test call sites use a sibling `ensure_main_window(&tauri::WebviewWindow<R>)` helper with the same `main`-only allowlist and the same error string) — is applied to every sensitive command (file I/O, workspace tree, save, auto-backup, menu state, theme state, app restart, the new `open_agent_window`, the agent launch surface) and to the three Agent session commands + `resize_agent_workbench_terminal` with the wider `main | agent` allowlist. 27 unit tests in `src-tauri/src/tests.rs` pin the gate: each main-only command rejects `AGENT_WINDOW_LABEL`, each agent-allowed command accepts `MAIN_WINDOW_LABEL` and `AGENT_WINDOW_LABEL`, every gate rejects an unknown label, and the new resize-widening tests pin that dimension validation lives in the body (not the gate) — the zero-dimension test sends `columns=0, rows=24` from both `MAIN_WINDOW_LABEL` and `AGENT_WINDOW_LABEL` and asserts the body's `"terminal size"` error; a gate short-circuit would have returned the label error and tripped the assertion. A new `View > Open Agent Window` menu item (`MENU_OPEN_AGENT_WINDOW`, kana `えーじぇんとまどをひらく`) is gated on the same `active && consent` pair. Vite is configured for multi-entry (`build.rollupOptions.input = { main, agent }`) so `agent.html` ships in the bundle. The Agent Workbench boundary remains explicit / allowlisted / one-session / no-restore / no-auto-apply; no Start session, provider selection, settings, file commands, dialog access, dependency or lockfile change; no Rust capability widening; the right-pane Agent path is unchanged and kept for verification. The workstream does NOT add Tauri events for cross-window sync, window position persistence, a Start button in the agent window, multi-session support, or real-time mode-toggle reflection in the agent window; those are explicit residual gaps called out in the entries below.
- Latest v0.8 Agent Workbench action/sync hook boundary docstrings slice finished the boundary-docstring parity for the action/sync hooks that mirror the pattern already in place on the state/ref hooks (the prior state-hook docstring completion slice). Today each of the six action/sync hooks carries a top-of-file "what it owns / what it does NOT own" comment that references `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md`: `src/hooks/useAgentLaunchGate.ts` (one-shot launch flow: preflight + `startAgentWorkbenchSession` + launch-gate transitions; no Agent state, no session, no terminal size, no output buffer), `src/hooks/useAgentSessionLifecycle.ts` (per-session handlers: differential session-state refresh, stop, write workspace file path; uses the internal `useAgentOutputSeqCursor` for the differential poll; no session, terminal, or output state), `src/hooks/useAgentTerminalActions.ts` (terminal I/O handlers: send / resize / preset / selection-send; no session, terminal, gate, or output state; the resize failure path is documented as status-only by design), `src/hooks/useAgentWorkbenchSessionActions.ts` (fan-in wrapper exposing lifecycle + launch-gate handlers under a single boundary name; options type is now derived from the sub-hook options types so wrapper / sub-hook drift is caught by the type system), `src/hooks/useAgentWorkbenchPreferenceActions.ts` (preference-level handlers: mode toggle, app restart request, consent, provider change; no preference state, no gate, no restart-pending, no global error; the provider-change guard is documented as intentionally not flowing through `reportAgentLaunchGateError`), and `src/hooks/useAgentWorkbenchSessionSync.ts` (background-sync effects only: right-pane demote, session/buffer reset on availability change, session-state poll on `AGENT_WORKBENCH_SESSION_POLL_MS` cadence, workspace-root gate sync; no state, no return value). The same slice exported `UseAgentLaunchGateOptions` and `UseAgentSessionLifecycleOptions` from their respective sub-hooks and rewrote `useAgentWorkbenchSessionActions`'s 14-field options union as `Omit<UseAgentSessionLifecycleOptions & UseAgentLaunchGateOptions, "refreshAgentSessionState">` so the wrapper type is mechanically derived from the sub-hooks (App.tsx's single-object-literal call site is unchanged). No behavior, types-as-seen-from-caller, props semantics, dependencies, or Agent Workbench capabilities changed; the Agent Workbench boundary remains explicit / allowlisted / one-session / no-restore / no-auto-apply; no Rust, dependency/lockfile, Review Desk, or right-pane compare-route change.
- Latest v0.8 Claude Code CLI readiness state: `claude` is now implemented as an allowlisted local CLI provider in the same Agent Workbench provider model as `codex`, `opencode`, and `pi`. It uses the existing allowlist / launch gate / selected-workspace / one-session / provider-not-found / trusted-workspace smoke path, with no Claude-specific permission UI, MCP UI, arbitrary arguments, provider-add UI, Git integration, or auto-apply.
- Latest v0.8 Review Desk compare/apply information design slice adds a preview meta row (Target / Buffer at compare / Candidate size / Compared at HH:MM:SS), an "Apply will mark unsaved" caption under the Apply button, and a stale preview banner with a Re-run Compare action. The candidate `CompareCase` now records `comparedAt: number`; `useReviewDeskState.runCandidateCompare` stamps it with `Date.now()`; and `ReviewSurface` derives a `CandidateStaleness` from `documentTabId` / `documentContents` / active-tab presence so Apply candidate is disabled while the preview is stale (no active tab / tab switched / buffer edited). The diff table is dimmed and `pointer-events: none` while the banner is shown. Compare / Apply remain explicit buttons, the existing candidate edit / tab / buffer safety guards still flow through `runCandidateCompare` / `setCandidateInputText` / `clearCandidate`, and the right-pane compare route, `DiffPane` / `FileCompareView` / `ChangeReviewView` shape, and `compareSource` / `compareTarget` slots are all untouched. Side-by-side workbench, live diff refresh, candidate source variants, source file selection, and save policy remain out of scope until v0.11+ Apple Local Assist candidate-review planning.
- Latest v0.8 Agent Workbench preferences extract slice moved `agentWorkbenchActive`, `agentWorkbenchConsent`, `agentWorkbenchPreference`, and `agentWorkbenchProvider` plus their setters out of `src/hooks/useAppPreferences.ts` into a new `src/hooks/useAgentWorkbenchPreferences.ts` hook. The `localStorage` keys (`AGENT_WORKBENCH_ENABLED_STORAGE_KEY`, `AGENT_WORKBENCH_CONSENT_STORAGE_KEY`, `AGENT_WORKBENCH_PROVIDER_STORAGE_KEY`) and the existing write effects are unchanged, so the Agent Workbench trust boundary, restart-required mode change, and responsibility-boundary consent flow behave identically. The Safe Editor preferences layer (`useAppPreferences.ts`) now owns only theme preference, editor display settings, preview visibility, and menu language, and the new Agent Workbench hook carries a top-of-file boundary comment that points at `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md`.
- Latest v0.8 Agent Workbench state boundary docstrings slice added a top-of-file boundary comment to the three Agent Workbench state hooks that mirror the pattern already in `useAgentWorkbenchPreferences.ts`: `src/hooks/useAgentWorkbenchRuntimeState.ts` (launch gate, session, terminal size, stop-pending, app-restart-pending, active-session derivation; `appRestartPending` stays here because the only writer is the Agent Workbench mode-change restart flow), `src/hooks/useAgentOutputBuffer.ts` (frontend mirror of the bounded provider-output buffer owned by the Rust runtime), and `src/hooks/useAgentUiRefreshGate.ts` (session-polling UI refresh suspension ref consulted by the Agent session polling effect). Each comment references `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md` so the Safe Editor / Assist Surface split is visible at every Agent state hook. No behavior, types, props, hooks, dependencies, or Agent Workbench capabilities changed; the Agent Workbench boundary stays explicit / allowlisted / one-session / no-restore / no-auto-apply.
- Latest v0.8 Agent Workbench state hook boundary docstrings completion slice finished the boundary-docstring intent that the prior "state boundary docstrings" entry above already documented as done. Today the three state hooks the prior slice named — `src/hooks/useAgentWorkbenchRuntimeState.ts` (launch gate, session, terminal size, stop-pending, app-restart-pending, active-session derivation; `appRestartPending` stays here because the only writer is `useAgentWorkbenchPreferenceActions.restartAppForAgentMode`), `src/hooks/useAgentOutputBuffer.ts` (frontend mirror of the bounded provider-output buffer owned by the Rust runtime, appended to on each successful session-state refresh and reset when the session goes away), and `src/hooks/useAgentUiRefreshGate.ts` (session-polling UI refresh suspension ref consulted by `useAgentWorkbenchSessionSync` and flipped by hover/focus events that should pause polling) — each carry the top-of-file boundary comment referencing `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md`. The same slice added the matching comment to `src/hooks/useAgentOutputSeqCursor.ts` (the last-`seq` ref feeding the differential `getAgentWorkbenchSessionState` poll, reset to 0 when the active session changes) for symmetry across the Agent Workbench ref-state hooks, and tightened the existing comment on `src/hooks/useAgentWorkbenchPreferences.ts` to spell out the `agentWorkbenchActive` (read-only *currently-running* state frozen at app start) vs `agentWorkbenchPreference` (user-desired setting, settable, only takes effect after an app restart) duality so the restart-required mode change is visible at the call site. No behavior, types, props, hooks, dependencies, localStorage keys, or Agent Workbench capabilities changed; the Agent Workbench boundary remains explicit / allowlisted / one-session / no-restore / no-auto-apply; no Review Desk, no right-pane compare-route, no Rust, and no dependency or lockfile change.
- Latest v0.8 Agent Workbench stateless helper consolidation slice renamed `src/agentSession.ts` to `src/agentWorkbench.ts` and moved `providerLabel`, `isActiveAgentSession`, `agentSessionStateLabel`, `agentCompactSessionStateLabel`, and `localizeAgentGateMessage` out of `src/utils.ts` so the Safe Editor utility layer no longer carries Assist Surface helpers. The new `src/agentWorkbench.ts` is the single home for Agent Workbench output sequencing, session-identity checks, provider labels, and gate-message localization, and it carries a top-of-file boundary comment that points at `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md`. The same slice added a new `reportAgentLaunchGateError` helper that consolidates the repeated `setAgentLaunchGate({kind:"rejected", message:String(err), preflight:null}); setStatus("...")` pattern, and refactored the 8 catch / rejection sites in `useAgentLaunchGate`, `useAgentSessionLifecycle`, and `useAgentTerminalActions` to use it. The misnamed `// ── Number Helpers ──` section header in `src/utils.ts` that wrapped `isSupportedImageFile` was also renamed to `// ── Image Extension Helpers ──` so the remaining section headers describe their contents. The Agent Workbench boundary stays explicit / allowlisted / one-session / no-restore / no-auto-apply; the Agent restart-failure path that uses `setGlobalError` and a localized `setStatus` (instead of the launch gate) was intentionally left alone because it does not fit the new helper.
- Latest v0.6 refactor slice grouped tab navigation and pointer reordering wiring in `src/hooks/useTabBarController.ts`; `src/App.tsx` was 867 lines. Further refactor work should be justified by the v0.7 readiness gate or a concrete Review Desk implementation risk.
- External-agent implementation plus Codex review is now an explicit workflow in `docs/external-agent-review-workflow.md`: external agents may implement one approved slice, while Codex reviews findings-first before acceptance unless the user asks for review-and-fix.
- Post-v0.5 planning notes now live under `docs/` as internal, low-authority planning memos. Public/product claims should still be taken from README plus the high-authority docs.
- Markdown authoring feature readiness is now tracked in `docs/authoring-feature-readiness.md`; do not claim image paste, export, table editing, or Agent authoring beyond the implemented subset until those gaps are closed or explicitly deferred.
- The prototype creates user-selected text/Markdown files, opens common UTF-8 text documents from File > Open or Finder/app-icon open events, opens a user-selected folder, shows a lazy bounded file tree, opens multiple files in tabs, edits the active tab with CodeMirror 6, saves through Rust with external-change protection, searches with visible match highlights and keyboard/navigation options, renders a toggleable sanitized Markdown preview, and shows selected workspace images in a read-only preview.
- v0.6 editor-foundation work includes App.tsx split MVP, Cmd+P quick open, workspace auto-backups for dirty tabs, Replace one / Replace all, Agent output differential polling, selected text to Agent send, preset Agent prompt chips, tab drag-and-drop reordering, multi-cursor, and rectangular selection. Auto-backup is enabled by default and can be turned off from Preferences > Application; the auto-backup scheduler, document save/export/backup controller hook, document export and backup integration hook, editor command hook including Insert Table, editor tabs state hook, editor selection state hook, app feedback state hook, dialog state hook, view mode state hook, draft recovery state hook, document safety action controller hook, file opening/new-file hook, workspace/file opening controller hook, tab close/window close flow hook, recovery action hook, compare controller hook, compare setup action hook, compare execution/review callback hook, compare state hook, tab bar controller hook, tab navigation hook, tab reordering logic, Quick Open visibility hook, workspace shell state hook, workspace runtime effects hook, workspace restore hook, workspace opening hook, workspace tree loader hook, Go to Line hook, image preview state hook, editor tab and active-draft derived-state hook, active document identity/presence hook, active document surface hook, side pane controller hook, side pane visibility hook, side pane resize hook, side pane toggle hook, preview scroll-sync hook, Agent output buffer hook, Agent UI refresh gate hook, opened files listener hook, window drag-drop listener hook, app menu action listener hook, native menu action bundle ref hook, native menu integration hook, app shell/menu/title/theme/workspace persistence sync hook, keyboard/focus effects hook, app runtime effects controller hook, dialog/window-close ref and dialog open-state hook, app editor ref hook, app activity listener hook, window close confirmation hook, workspace context-menu state hook, workspace context-menu dismissal hook, latest-value ref hook for App handlers/state and auto-backup state, preview cleanup hook, Agent output sequence cursor hook, active-tab external-change checks hook, Agent Workbench runtime state hook, active Agent session derivation, Agent Workbench session sync hook, Agent Workbench session lifecycle/UI callback hook, draft persistence hook, workspace tree replacement helper, workspace state persistence hook, clipboard helper, recent entries state/persistence hook, window title sync hook, native app menu state sync hook, theme menu state sync hook, localized app copy hook, editor tab utility helpers, numeric clamp helper reuse, status message localization, keyboard/IME and global shortcut guards, global keyboard shortcuts hook, Agent session comparison helpers, document status calculation, Markdown outline calculation, Markdown local-link resolution, localStorage persistence helpers, modal focus-trap helper, dialog initial focus hook, modal keyboard guard hook, Find/Replace state and match-count hook, Find/Replace match calculation, Find match index sync hook, Find/Replace actions hook, Find/Replace controller hook, diff calculation, top tab/document chrome shell, tab bar view, document metadata controls, Find/Replace and recovery message shell, recovery message banners, workspace shell, workspace sidebar, editor main pane, pane resizer, right side pane, dirty-close dialogs, status bar shell, status bar, Preferences dialog shell, normal settings pane, Agent Workbench settings pane, and Find/Replace bar view now live outside App.tsx.
- The 2026-06-01 bug-check pass fixed Replace using stale search match state, allowed empty replacement strings, restored visible tab drag state during drag-and-drop, prevented selected-text Agent send from falling through to editor key handling, and hardened auto-backup paths so backups stay under the selected workspace without following backup-directory symlinks.
- The 2026-06-01 quality polish pass fixed the Review Desk keyboard-toggle close path so `Cmd+Shift+R` / `Ctrl+Shift+R` clears manual candidate input, diff preview, and errors before returning to the normal editor surface, matching the close-button reset behavior.
- The 2026-06-01 Review Desk candidate safety pass fixed failed manual-candidate comparisons so a stale successful preview is cleared before the error is shown, keeping Apply candidate disabled until the user produces a fresh preview.
- The 2026-06-01 Review Desk candidate edit safety pass clears the manual-candidate preview, error, and Apply candidate action as soon as the textarea changes after a compare, so edited candidate text must be reviewed again before it can be applied.
- The 2026-06-02 Review Desk candidate tab-switch safety pass binds a manual-candidate preview to the tab it was compared against. If the active tab changes before Apply candidate, the old preview is no longer treated as applicable and the apply handler rejects the stale tab id.
- The 2026-06-02 Review Desk active-buffer edit safety pass binds a manual-candidate preview to the exact buffer text compared. If the active tab contents change after Compare, Apply candidate is disabled until the candidate is compared again, and the apply handler rejects the stale buffer snapshot.
- The 2026-06-02 Review Desk discoverability and slash-keyboard polish pass added a View menu `Review Desk` item, a `/` command for opening Review Desk, a `/` shortcut-list insertion command, and capture-phase Slash menu handling so `Enter` / `Tab` execute the selected command instead of inserting a newline. The persistent top-chrome Review Desk entry was later removed so Review Desk stays lower prominence until Apple Local Assist / Foundation Models work makes candidate review a daily path.
- The 2026-06-02 Change Review routing pass fixes the top-right `変更を確認` / `Review changes` dirty-buffer button so successful buffer-vs-disk and draft-vs-disk reviews open the existing right-pane Diff surface instead of only updating the status bar. The route also reopens the right pane after the user closes it, so repeated review attempts do not depend on prior pane state. Closing a Change Review result returns to the editor/preview surface instead of dropping into the generic Diff setup screen.
- The 2026-06-02 final UI polish pass aligns seasonal top chrome and native macOS window appearance for Yakou, adds a radio-style marker for the native Theme menu, expands Japanese menu/localized dialog coverage, and adds the intentionally hard-to-read `かなふみ` menu language mode. Built-app smoke confirmed `かなふみ` changes the app chrome and native menu bar (`ふみ / てならひ / ながめ / まど / たすけ`).
- The next Review Desk UX direction is not v0.8 work. Keep Review Desk as a low-prominence safety receiver for candidate text; expect it to become genuinely useful only after Apple Foundation Models / Apple Local Assist work in v0.10+ supplies candidate generation that needs explicit comparison, with deeper Review Desk integration deferred to v0.11+.
- The Assist Surface direction is now tracked in `docs/assist-surface-strategy.md`: future assist features should remain detachable from Safe Editor, allow a later Apple Local Assist / Foundation Models-based document-helper path, and avoid becoming a generic provider platform, local HTTP layer, command runner, background indexer, or auto-apply agent.
- The 2026-06-01 built-app smoke launched `hazakura editor`, opened temporary Markdown files, reordered open tabs with drag-and-drop, moved focus with previous/next tab shortcuts, verified Save, Export as HTML, split Diff comparison, Replace all, Cmd+P Quick Open, toggled Preferences > Application > Auto-backup off/on, and confirmed a dirty workspace-local file produced a `.hazakura/backups/` entry containing the unsaved text. Browser smoke was unavailable in this session.
- Existing LF / CRLF line endings are detected when a file is opened and preserved through save.
- The status bar shows file type, UTF-8 encoding, approximate byte count, character count, saved line-ending mode, final-newline state, and clean/unsaved state.
- Line endings can be explicitly converted between LF and CRLF; conversion marks the tab unsaved until saved.
- Save As can create a new UTF-8 text file with common text extensions such as `.txt`, `.log`, `.json`, `.yaml`, `.toml`, `.csv`, `.css`, and `.html`, while refusing to overwrite an existing file.
- New File, Open, Open Folder, Save, Save As, Close Window, and Recent file/folder actions are available from the native File menu instead of occupying the top toolbar.
- Preferences and Agent Workbench are available from the native app menu on macOS.
- Preview, Wrap, Invisibles, and Theme live in the native View menu. Editor/application settings live in Preferences, including an English/Japanese native menu language switch; Japanese mode also localizes the empty start surface, workspace shell/context-menu labels, editor helper/search controls, common status bar messages and active-document metadata, draft/external-change/save-error recovery banners, preview unavailable messages, Preferences dialog labels, and Agent Workbench pane/gate/status copy.
- The menu language setting also includes `かなふみ`, an intentionally old-Japanese-ish, hiragana-heavy display mode for app chrome and native menus.
- Agent Workbench now opens as its own dialog instead of sharing the Preferences contents. Changing the developer-mode gate stores the requested mode, shows localized Japanese/English boundary copy, offers an explicit restart button, and requires restart before Agent UI or backend launch-command availability changes.
- When Agent Workbench mode is active or pending restart, the top toolbar shows an Agent Mode badge so the current trust boundary is visible outside the settings dialog.
- When Agent Workbench mode is active for the current app session, the Agent Workbench dialog shows only allowlisted provider choices (`codex` / `opencode` / `pi`) and requires explicit responsibility-boundary consent before the backend launch gate can pass.
- The top editor chrome exposes Preview, Diff, Outline, and Agent as open/close toggles instead of a tab strip inside the pane. Diff is always available as a dedicated workbench mode; when open, it hides the center editor area and uses the main content width for comparison setup or results. Outline is available for the current text tab and lists the current Markdown file's ATX headings with click-to-jump navigation, without project-wide indexing; very large outlines show a visible note when the first 200 headings are shown. The status bar shows the current Markdown position as `§` context when the cursor is under an ATX heading, the Outline pane highlights that current section, and a transient scroll-position HUD shows the approximate current section and line progress while scrolling Markdown files. When Agent Workbench mode is active and consent is acknowledged, the compact Agent pane shell emphasizes provider, running state, and workspace path above an expanded xterm-based terminal surface. The shell can start one allowlisted provider process in the selected workspace root, render PTY output, send terminal input to it, resize the backend PTY from xterm rows/columns, and stop it through the runtime adapter boundary.
- The app window title follows the active file and marks unsaved state, so the redundant in-app title header is no longer shown.
- The workspace header includes a small open-folder action for switching workspace without returning to the native menu.
- Save writes the editor text without adding or removing a final trailing newline by policy; Rust tests cover LF and CRLF final-newline presence.
- Markdown preview shows embedded `data:image` PNG/JPEG/GIF/WebP images and safe workspace `assets/...`, `./assets/...`, and `/assets/...` image references, while blocking external or other local image references with an in-preview note.
- Markdown preview uses readable review-oriented spacing for paragraphs, lists, task checkboxes, horizontal rules, blockquotes, code blocks, and GFM tables; tables stay in a bounded horizontal scroll frame with clearer headers, grid lines, row striping, and alignment support.
- Selecting a PNG/JPEG/GIF/WebP file up to 20 MB in the workspace tree opens a read-only local image preview in the work area after a lightweight content-signature check, without adding Markdown local-image loading. Closing that image preview returns to the prior text tab when one is still open.
- Markdown preview and editor panes use lightweight bidirectional scroll synchronization with a small tolerance to avoid jitter while preview is visible.
- The editor / preview split can be resized with a draggable vertical divider while preview is visible.
- Recent workspace, open tabs, active tab, and theme preference are restored after restart.
- Unsaved dirty tab drafts are stored locally and offered for explicit restoration after restart when the on-disk file still matches the draft's saved fingerprint.
- Editor display settings for wrap, invisible characters, font size, and tab size are persisted locally and adjustable from Preferences.
- Theme changes now reconfigure the active CodeMirror editor without recreating it, so the current cursor, selection, and undo/redo session state are not reset by switching Light / Dark / Sakura / Yakou / Shokou / Kouyou.
- The status bar groups supplemental document details: file type, UTF-8 encoding, approximate byte count, character count, line-ending mode, final-newline state, clean/unsaved state, cursor line/column, and approximate selection counts.
- Active-file search supports case-sensitive, whole-word, and safe regex modes, with invalid regex input reported without throwing.
- Go to Line jumps the active editor to a requested line.
- The current-file Outline pane lists ATX Markdown headings outside fenced code blocks, jumps the editor to the selected heading line, highlights the heading that contains the cursor line, shows a cap note when a very large file has more headings than the outline displays, and supports a transient scroll-position HUD for Markdown files.
- Markdown preview opens relative Markdown links only when they resolve to supported text files inside the selected workspace; absolute paths, external URLs, anchor-only links, and links outside the workspace are not opened by hazakura.
- The active tab is rechecked for external on-disk changes when it gains focus through tab switching or app focus/visibility changes. Clean tabs are refreshed from disk automatically, while dirty tabs keep the explicit external-change recovery banner.
- Existing workspace text files with common text/document extensions can be explicitly compared from the dedicated Diff workbench by setting separate source and target slots, then pressing Compare. In Diff mode, clicking comparable workspace file rows fills the source first and then the target; the file-tree context menu can set either side explicitly. Drag selection is intentionally not part of the Diff setup flow. The GitHub-PR-like split Diff workbench uses file/workspace/change-review wording rather than Git wording and does not inspect repository state. Markdown file comparisons insert current heading context before changed blocks when an ATX heading is available. The same non-Git comparison surface can also review the active editor buffer against disk, review a recoverable draft against disk before restoring it, and review local edits against disk during an external-change conflict.
- Find-field Enter / Escape handling and global shortcuts now ignore active IME composition events, so Japanese conversion is not mistaken for search movement, find close, save, open, or tab-close commands.
- Editor-local keyboard editing keeps Tab inside the editor for indentation, supports Shift+Tab outdent, and preserves Shift+Arrow text selection.
- Editor-local Markdown helpers wrap or insert bold, italic, inline-code, and link syntax from the tabs row or Cmd+B / Cmd+I / Cmd+E / Cmd+K while focus is inside the editor.
- Save conflicts now present explicit recovery choices: Reopen from disk, Close without saving, and Keep editing.
- Non-conflict save failures now state that local edits remain in the editor and offer Try save again / Keep editing actions.
- Window close requests now stop when any open tab is unsaved and offer Save All, Discard All, or Cancel.
- Cancelling dirty-tab and app/window close dialogs by button or Escape returns keyboard focus to the editor.
- Dirty-tab and app/window close dialogs keep Tab / Shift+Tab focus cycling inside the dialog while it is open.
- If Save from a dirty-tab close dialog fails or detects an external change, the close is stopped, the failed tab is selected, the dialog is dismissed, editor focus returns, and the existing save-failure or conflict recovery actions remain visible.
- If Save All from the app/window close dialog fails or detects an external change, the close is stopped, the failed tab is selected, editor focus returns, and the existing save-failure or conflict recovery actions remain visible.
- Discard All from the app/window close dialog removes matching stored unsaved drafts before close, so intentionally discarded edits are not offered for restoration on restart.
- Cmd+N creates a new file, Cmd+O opens a file, Cmd+Shift+O opens a folder, Cmd+Option+Left / Cmd+Option+Right focus the previous or next open tab, Cmd+W closes the active tab through the same dirty-tab confirmation path as the tab close button, and Cmd+Shift+W requests window close.
- Workspace tree loading now reads only direct children for the opened root or expanded directory, keeps heavy and hidden directory exclusions, rejects direct child listing outside the selected workspace root, and reports per-folder cap overflow as a partial listing instead of failing the whole workspace.
- Atomic save cleanup removes the hidden temporary save file if the final replace step fails and refuses to overwrite a pre-existing hidden save temp file.
- The app bundle icon uses a cherry-blossom emoji mark centered on a soft pink rounded base.
- The v0.6 macOS app bundle is generated at `src-tauri/target/release/bundle/macos/hazakura editor.app`; release artifacts use the `hazakura-editor` basename.

## Implemented

- Tauri v2 shell
- React + TypeScript frontend
- CodeMirror 6 editor
- Markdown preview with `marked` and `DOMPurify`
- Native open-file dialog through `@tauri-apps/plugin-dialog`
- macOS document-open handling for common text file associations with `rank = None`, routed through the same Rust text-open validation as File > Open
- Native open-folder dialog through `@tauri-apps/plugin-dialog`
- Native save-path dialog through `@tauri-apps/plugin-dialog`
- Rust commands for creating, opening, and saving UTF-8 text files
- Rust command for Save As to a new UTF-8 text file without overwriting existing files
- Rust command for bounded workspace tree listing
- Rust command for bounded direct directory listing inside the selected workspace root
- Save-conflict detection using a Rust-generated file fingerprint
- LF / CRLF line-ending detection and save preservation
- Explicit LF / CRLF conversion control in the tabs row
- Approximate byte count, character count, line-ending mode, final-newline status, and clean/unsaved state in the status bar
- Final-newline presence preservation on save
- Preview visibility toggle through View / Preferences with `localStorage` persistence
- Safe embedded-image and workspace-asset preview policy for Markdown preview
- Markdown preview readability styling for lists, task checkboxes, horizontal rules, blockquotes, code blocks, and GFM tables with a bounded horizontal table frame
- Read-only local workspace image preview for PNG/JPEG/GIF/WebP files up to 20 MB, with extension and content-signature validation
- Lightweight bidirectional editor/preview scroll synchronization while Markdown preview is visible
- Resizable editor / preview columns while Markdown preview is visible
- Multiple open file tabs
- Tab-level unsaved state
- Save / Discard / Cancel confirmation before closing an unsaved tab
- Light / Dark / Sakura / Yakou / Shokou / Kouyou theme switching
- Theme selection persistence through `localStorage`
- Active editor theme reconfiguration without recreating the CodeMirror view
- Recent workspace restoration through `localStorage`
- Open tab and active tab restoration through `localStorage`
- Active-file search with match count, previous/next controls, visible match highlights, active-match selection, Enter / Shift+Enter match navigation, and Escape return-to-editor behavior
- Search options for case-sensitive, whole-word, and regex search with invalid-regex reporting
- Go to Line control
- Current-file Markdown heading outline with click-to-jump navigation and current-section status context
- Markdown diff heading context before changed blocks
- Local Markdown preview link navigation for supported text files inside the selected workspace
- Cursor line/column and approximate selection count in the status bar
- File type/mode label in the status bar
- Editor display settings for line wrap, invisible characters, font size, and tab size
- Unsaved draft recovery prompt after restart
- External-change metadata recheck on app focus / visibility return and active tab switch
- Explicit center-file-to-right-file text comparison from common workspace text file rows, plus active buffer-versus-disk, draft-versus-disk, and conflict local-edits-versus-disk review, with added/removed line counts and non-Git comparison labels
- IME-safe keyboard handling for find-field Enter / Escape and global shortcuts during active composition
- Editor-local Tab / Shift+Tab indentation and Shift+Arrow selection key handling
- Markdown input helpers for bold, italic, inline code, and link syntax
- Native File menu actions for New File, Open, Open Folder, Save, Save As, Close Window, and Recent file/folder reopening
- Native app menu actions for Preferences and Agent Workbench on macOS
- Native View menu actions for Preview, Wrap, Invisibles, Theme, and fullscreen
- Preferences dialog for editor/application settings that were previously exposed in the top toolbar, including File/View menu language
- Separate Agent Workbench dialog for the mode gate, requested mode, provider, consent, and responsibility boundary
- Agent Workbench right-pane shell, visible only after active-session mode and consent gates pass, with compact provider/running/workspace state, bounded PTY-backed xterm output/input, PTY resize from xterm rows/columns, and no arbitrary command, shell selector, auto-apply, auto-commit, or Git integration
- Rust-side Agent Workbench launch preflight, runtime adapter, and in-memory session lifecycle that allows only `codex` / `opencode` / `pi`, rejects disabled mode, unacknowledged consent, non-allowlisted providers, invalid workspace roots, provider-not-found starts, adapter failures, stop-adapter failures, and second active sessions before starting an allowlisted provider process
- Dynamic window title for active file and unsaved state
- Keyboard shortcuts for New File, Open, Open Folder, Save, Find, previous/next tab focus, active-tab close, and window close
- Conflict recovery actions for reloading, closing, or continuing with local edits
- Save-failure recovery wording and retry / keep-editing actions for non-conflict save errors
- App/window close confirmation for dirty tabs
- Dirty-tab and app/window close dialogs focus Cancel by default, can be cancelled with Escape, and return focus to the editor after cancellation
- Dirty-tab and app/window close dialogs trap Tab / Shift+Tab focus while open
- Failed or conflicted Save from the dirty-tab close dialog selects the failed tab and returns to the normal recovery banner
- Failed or conflicted Save All from the app/window close dialog selects the failed tab and returns to the normal recovery banner
- Discard All from the app/window close dialog clears matching stored recovery drafts before close
- Long file name and constrained-width layout guardrails for tabs, the file tree, status/error rows, and close dialogs
- Lazy file-tree directory expansion with per-folder partial-listing state
- Binary-looking file rejection
- 5 MB large-file warning flag
- 10 MB prototype editing limit
- Atomic save helper with temporary-file cleanup after failed replace attempts and existing-temp-file overwrite protection
- Cherry-blossom emoji-based app icon for Tauri build outputs
- Local macOS `.app` bundle icon resource, explicit non-Carbon launch metadata, macOS 11.0 minimum-system metadata aligned with the Rust binary, and ad-hoc signing for build-output validation

## Verification

Commands run successfully:

```bash
npm run typecheck
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo test --manifest-path src-tauri/Cargo.toml
npm run build:vite
npm run build
git diff --check
```

v0.7 Review Desk visible shell slice on 2026-06-01:

- Added `src/hooks/useReviewDeskState.ts` (R-4 skeleton: `reviewDeskMode`, `pendingReviewCase`, `resetReviewDesk`) and `src/hooks/useReviewDeskController.ts` (`openReviewDesk`, `closeReviewDesk`, `toggleReviewDesk`).
- Wired the no-op `Cmd+Shift+R` reservation from B-3 to the new `onToggleReviewDesk` handler in `src/hooks/useGlobalKeyboardShortcuts.ts`; IME composition and modal-open still bail out at the top of the handler.
- Replaced the B-1 `<ReviewSurface>` placeholder with a visible empty-state shell: header + close button + a future slot for the B-2 `CompareCase`. CSS is added to `src/styles.css`; no existing class was renamed.
- New `ReviewDeskCopy` / `getReviewDeskCopy` / `ReviewDeskMode` add English / Japanese copy for the new surface without touching `useLocalizedAppCopy`'s existing copy groups.
- `App.tsx` is now 851 lines (the v0.6 `867` baseline shrank by 16). No existing compare / review / tab / workspace behavior path was changed; the only added App.tsx wiring is two hook calls and the `onToggleReviewDesk` plumbing.
- `npm run typecheck`, `npm run build:vite`, `git diff --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (85 tests passed; no Rust changes), and `npm run build` (full Tauri bundle regenerated) all passed for this slice. Built-app `Cmd+Shift+R` toggle smoke was not claimed in this automation environment (osascript keystroke dispatch was not permitted); the smoke row is recorded in `docs/smoke-checklist.md` for the user to run.

v0.7 Review Desk manual candidate MVP slice on 2026-06-01:

- Promoted the B-2 R-4 placeholder `pendingReviewCase: unknown | null` to a real `CompareCase` slot in `src/hooks/useReviewDeskState.ts`, and added `candidateInputText`, `candidateCompareCase`, `candidateCompareView`, `candidateErrorMessage`, `setCandidateInputText`, `setCandidateCompare`, `runCandidateCompare`, and `clearCandidate`. `resetReviewDesk` now clears the candidate input, diff, and error in addition to the mode.
- Extended `src/types.ts` `CompareCase` with `kind: "candidate"` (manual paste: `documentPath`, `documentLabel`, `leftColumnLabel`, `rightColumnLabel`, `candidateSourceLabel`). `CompareViewState` and the existing `kind: "file" | "changes"` branches are unchanged.
- Updated `src/locale.ts` `ReviewDeskCopy` with English / Japanese copy for the candidate input label, hint, placeholder, Compare / Clear button titles, empty-no-active-tab heading, and the diff preview title. `getReviewDeskCopy` returns the new keys for both languages.
- Reworked `src/components/ReviewSurface.tsx` to render the manual candidate paste area (textarea + Compare / Clear buttons) plus a candidate diff preview that reuses `DiffBody` from B-2. The right-pane `DiffPane` / `FileCompareView` / `ChangeReviewView` path is untouched.
- Narrowed `src/components/DiffPane.tsx` to a right-pane-only `CompareCase` (file / changes). The candidate case never reaches the right-pane compare route; `src/components/SidePane.tsx` defensively filters candidate cases before forwarding to `DiffPane`. `getCaseSidePaths` in `src/components/DiffBody.tsx` now returns the candidate document path for both sides so Markdown heading context still renders.
- Added `.review-surface-candidate-*` rules to `src/styles.css` (textarea, compare / clear buttons, error, preview header, summary badges, table header, line-number gutter, empty preview hint). No existing CSS class was renamed.
- `src/App.tsx` gained only the candidate hook destructuring and the new `shellProps` entries (`activeTab`, `menuLanguage`, `candidateCompareCase`, `candidateCompareView`, `candidateErrorMessage`, `candidateInputText`, `clearCandidate`, `runCandidateCompare`, `setCandidateInputText`) — no candidate-specific logic.
- `src/hooks/useReviewDeskState.ts` builds the candidate `CompareCase` and diff view via the existing `buildLineDiff` from `src/diff.ts` inside `runCandidateCompare`, and surfaces the `DIFF_MAX_LINE_PRODUCT` error as a localized alert in `ReviewSurface` (`localizeCandidateError` in `src/components/ReviewSurface.tsx`).
- `npm run typecheck`, `npm run build:vite`, `git diff --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, and `npm run build` pass for this slice. Built-app smoke confirmed Review Desk open/close, manual candidate paste, Compare diff preview, Clear, and Close.

v0.8 Review Desk candidate editor foundation slice on 2026-06-02:

- Added a new self-contained `src/components/CandidateEditor.tsx` (CodeMirror 6 + `basicSetup` + `@codemirror/lang-markdown` + `indentWithTab` keymap) for the manual-candidate input slot. The component owns a `Compartment` per config axis (theme/fontSize, wrap, tabSize, spellcheck + data-placeholder, readOnly) and reuses the same Markdown highlighter shape as `src/components/EditorPane.tsx` (headings, strong/emphasis, links, monospace, blockquote). It exposes a `value` + `onChange` contract that matches the previous `<textarea>`, so `useReviewDeskState` and the existing candidate edit / tab / buffer safety guards are not changed.
- Reworked `src/components/ReviewSurface.tsx` to replace the manual-candidate `<textarea>` with `<CandidateEditor>`. The label, hint, empty-tab note, Compare / Clear buttons, error banner, and candidate preview header (`+added / -removed` summary, Apply button, diff table) layout is unchanged. The `htmlFor` on the label now points at the editor mount; the editor is marked `readOnly` while there is no active tab. The Apply / Compare flow, the candidate edit-safety reset, the tab-switch safety, and the active-buffer-edit safety all still flow through the same `runCandidateCompare` / `clearCandidate` / `setCandidateInputText` callbacks.
- `src/components/AppShell.tsx` passes the existing `editorSettings` and `editorTheme` from the `AppWorkspace` props union straight through to `ReviewSurface`; the explicit `Review Desk`-specific prop block is unchanged. No new shell-side wiring.
- `src/styles.css` drops the dead `.review-surface-candidate-textarea*` rules and adds `.review-surface-candidate-editor` plus a `.cm-content[data-placeholder]:empty::before` placeholder rule. The candidate editor stays inside the same `.review-surface-candidate-input` slot; minimum height is 240px with a 360px max so the editor still respects the workbench layout.
- `docs/smoke-checklist.md` records the slice and updates the manual-candidate review checklist to confirm the Markdown-aware editor (line numbers, syntax highlighting, Tab/Shift+Tab indent, readOnly with no active tab, no Cmd+Shift+Enter send-to-agent) while keeping the existing safety rows.
- No dependency or lockfile change. No Git / LSP / terminal / arbitrary-command surface added. No Agent Workbench change. No right-pane compare-route change. No live diff refresh; Compare / Apply remain explicit. No file overwrite or Save As policy for candidate documents yet.
- `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (85 Rust tests), `npm run build:vite`, `npm run build` (full Tauri bundle regenerated), and `git diff --check` all pass. Built-app smoke of the manual candidate review path is not claimed in this automation environment (`open -n` returned the local `kLSNoExecutableErr` blocker and the in-app browser policy refused `http://127.0.0.1:1420/`); use the updated `docs/smoke-checklist.md` row to confirm manually.

v0.8 Review Desk compare/apply information design slice on 2026-06-02:

- Extended `src/types.ts` `CompareCase` `kind: "candidate"` with a new `comparedAt: number` field; `kind: "file"` and `kind: "changes"` branches are unchanged, and the right-pane compare route still filters candidate cases out.
- `src/hooks/useReviewDeskState.ts` `runCandidateCompare` now stamps the new `comparedAt: Date.now()` on the `CompareCase` it builds, and `setCandidateCompare` (the existing setter) keeps the same `CompareCase` shape so external callers and the right-pane diff route are not affected. The `setCandidateInputText` / `clearCandidate` / `resetReviewDesk` / `applyManualCandidateToActiveTab` safety paths are unchanged.
- Added new `ReviewDeskCopy` keys in `src/locale.ts` for English / Japanese / kana: `candidateApplyWillMarkUnsaved`, `candidatePreviewBufferAtCompareLabel`, `candidatePreviewBufferAtCompareText`, `candidatePreviewCandidateSizeLabel`, `candidatePreviewCandidateSizeText`, `candidatePreviewComparedAtLabel`, `candidatePreviewTargetLabel`, `candidateStaleActionReCompare`, `candidateStaleHeading`, `candidateStaleReasonBufferEdited`, `candidateStaleReasonNoActiveTab`, and `candidateStaleReasonTabSwitched`. The size / tab-switched reasons are formatter functions so the line / char counts and the active tab name read naturally in each language.
- Reworked `src/components/ReviewSurface.tsx` to add a preview meta row (Target / Buffer at compare / Candidate size / Compared at HH:MM:SS) under the existing preview header, an "Apply will mark unsaved" caption under the Apply button, and a stale preview banner that lists the matched target file, explains the stale reason, and offers a Re-run Compare action that reuses the existing `runCandidateCompare` path. A new `computeCandidateStaleness` helper derives `fresh` / `no-active-tab` / `tab-switched` / `buffer-edited` from `CompareCase` + `activeTab`; Apply candidate is now disabled while the preview is stale, and the diff table is dimmed and `pointer-events: none` while the banner is shown. No new prop or App-level wiring.
- `src/styles.css` adds `.review-surface-candidate-preview-meta` (grid dl with auto-fit columns), `.review-surface-candidate-preview-meta-cell` (dt label / dd value, monospace for sizes and times), `.review-surface-candidate-apply-note` (muted caption), `.review-surface-candidate-stale` (warning-bordered banner with text + Re-run Compare button), and `.review-surface-candidate-table.is-stale` (dimmed + `pointer-events: none`). No existing class was renamed; the existing `.review-surface-candidate-*` rules are intact.
- `docs/smoke-checklist.md` adds the slice row to the top-level "Latest" section and updates the "Review Desk Manual Candidate Review" checklist to cover the new meta row, the apply caption, the stale banner with Re-run Compare action for tab-switch / buffer-edit / no-active-tab triggers, and the menu-language coverage for the new copy. Built-app smoke of the new information design is not claimed in this automation environment (`open -n` returned the local `kLSNoExecutableErr` blocker and the in-app browser policy refused `http://127.0.0.1:1420/`); use the updated `docs/smoke-checklist.md` row to confirm manually.
- No dependency or lockfile change. No Git / LSP / terminal / arbitrary-command surface added. No Agent Workbench change. No right-pane compare-route change. No live diff refresh; Compare / Apply remain explicit. No file overwrite or Save As policy for candidate documents yet. Side-by-side workbench, candidate source variants, source file selection, and save policy remain out of scope for this information design slice.
- `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (85 Rust tests), `npm run build:vite`, `npm run build` (full Tauri bundle regenerated), and `git diff --check` all pass.

v0.8 Agent Workbench preferences extract slice on 2026-06-02:

- Moved the four Agent Workbench preference fields and their setters out of `src/hooks/useAppPreferences.ts` into a new `src/hooks/useAgentWorkbenchPreferences.ts` hook: `agentWorkbenchActive` (read-only mirror of the persisted preference), `agentWorkbenchPreference` / `setAgentWorkbenchPreference`, `agentWorkbenchConsent` / `setAgentWorkbenchConsent`, and `agentWorkbenchProvider` / `setAgentWorkbenchProvider`. The hook keeps the same `localStorage` keys (`AGENT_WORKBENCH_ENABLED_STORAGE_KEY`, `AGENT_WORKBENCH_CONSENT_STORAGE_KEY`, `AGENT_WORKBENCH_PROVIDER_STORAGE_KEY`) and the same write effects, so the Agent Workbench trust boundary, restart-required mode change, and responsibility-boundary consent flow behave identically and any previously persisted preference survives the move.
- The new hook carries a top-of-file boundary comment that points at `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md`; the Safe Editor preferences layer (`useAppPreferences.ts`) now owns only theme preference, editor display settings, preview visibility, and menu language. `src/App.tsx` was updated to consume `agentWorkbenchActive` / `agentWorkbenchPreference` / `setAgentWorkbenchPreference` / `agentWorkbenchConsent` / `setAgentWorkbenchConsent` / `agentWorkbenchProvider` / `setAgentWorkbenchProvider` from the new hook instead of `useAppPreferences`. No dependency or lockfile change, no Rust change, no Agent Workbench capability change, and no right-pane compare-route change.
- The Agent Workbench boundary remains explicit (mode toggle in the macOS app menu + restart required), allowlisted (provider stays `codex` / `opencode` / `pi`), one-session, no-restore, no-auto-apply, no-auto-commit, and without provider-add UI or arbitrary command/path field.
- `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (85 Rust tests), `npm run build:vite`, `npm run build` (full Tauri bundle regenerated), and `git diff --check` all pass for this slice.

v0.8 Agent Workbench state boundary docstrings slice on 2026-06-02:

- Added a top-of-file boundary comment to the three Agent Workbench state hooks that mirror the pattern already in `src/hooks/useAgentWorkbenchPreferences.ts`. `src/hooks/useAgentWorkbenchRuntimeState.ts` now documents the launch gate, session, terminal size, stop-pending, app-restart-pending, and active-session derivation fields; `appRestartPending` stays in this hook because the only writer is the Agent Workbench mode-change restart flow. `src/hooks/useAgentOutputBuffer.ts` now documents the frontend mirror of the bounded provider-output buffer owned by the Rust runtime. `src/hooks/useAgentUiRefreshGate.ts` now documents the session-polling UI refresh suspension ref consulted by the Agent session polling effect. Each comment references `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md` so the Safe Editor / Assist Surface split is visible at every Agent state hook.
- No behavior, types, props, hooks, dependencies, localStorage keys, or Agent Workbench capabilities changed. The Agent Workbench boundary remains explicit / allowlisted / one-session / no-restore / no-auto-apply. No Review Desk change, no right-pane compare-route change, no Rust change, no dependency or lockfile change.
- `npm run typecheck`, `npm run build:vite`, and `git diff --check` all pass for this slice. The slice touched only frontend hooks and the docs entry, so `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` and `cargo test --manifest-path src-tauri/Cargo.toml` were not re-run; the prior v0.8 slice above already confirmed the 85-test suite is green with these hooks in their previous shape and the new comments are inside doc comments only.

v0.8 Agent Workbench stateless helper consolidation slice on 2026-06-02:

- Renamed `src/agentSession.ts` to `src/agentWorkbench.ts` and moved the five Agent Workbench helpers (`providerLabel`, `isActiveAgentSession`, `agentSessionStateLabel`, `agentCompactSessionStateLabel`, `localizeAgentGateMessage`) out of `src/utils.ts` so the Safe Editor utility layer no longer carries Assist Surface helpers. The new file is the single home for Agent Workbench stateless helpers (output sequencing, session-identity checks, provider labels, gate-message localization) and carries a top-of-file boundary comment that points at `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md`. `src/utils.ts` lost the `// ── Agent Helpers ──` section, the misnamed `// ── Number Helpers ──` section header that wrapped `isSupportedImageFile` was renamed to `// ── Image Extension Helpers ──`, and the remaining Safe Editor helpers (`parentFolderName`, `buildRecentDisplayEntries`, `fileNameFromPath`, `folderLabelFromPath`, `suggestedNewFilePath`, `suggestedSaveAsPath`, `normalizeAbsolutePath`, `directoryPathFromPath`, `isPathInsideDirectory`, `isSupportedImageFile`, `normalizeTextLineEndings`, `isMarkdownDocumentPath`, `parseMarkdownHeadingLine`, `findCurrentMarkdownHeading`, `localizeCompareError`, `clampNumber`, `isEditorKeyboardTarget`, `trapFocusInElement`) stay where they are.
- Added a new `reportAgentLaunchGateError(setAgentLaunchGate, setStatus, statusLabel, error, preflight?)` helper in `src/agentWorkbench.ts` that consolidates the repeated `setAgentLaunchGate({kind:"rejected", message:String(err), preflight:null}); setStatus("...")` pattern, and refactored 8 call sites to use it: `useAgentLaunchGate` (provider-not-found preflighted path, no-session preflighted path, catch error path), `useAgentSessionLifecycle` (catch refresh, catch path send, catch stop), and `useAgentTerminalActions` (catch input, catch resize). The provider-change guard in `useAgentWorkbenchPreferenceActions` (which spreads the current gate to keep the existing preflight) and the `restartAppForAgentMode` failure path (which uses `setGlobalError` and a localized `setStatus`, not the launch gate) were intentionally left alone because they do not fit the helper.
- Updated import sites: `useAgentLaunchGate`, `useAgentSessionLifecycle`, `useAgentTerminalActions`, `useAgentWorkbenchSessionSync`, and `useAgentWorkbenchRuntimeState` now import from `../agentWorkbench`; `AgentPaneShell`, `AppOverlays`, and `AppStatusBar` now import from `../agentWorkbench`. The `diff.ts`, `markdownLinks.ts`, `editorTabs.ts`, `StartPanel`, `WorkspaceTree`, `DiffBody`, `useRecentEntries`, `useAppMenuStateSync`, `useFileOpening`, `useDocumentStatus`, `useSaveActions`, `useSidePaneResize`, `useDocumentOutline`, `useEditorCommands`, `useCompareExecution`, `useAppPreferences`, and `usePreviewScrollSync` imports of `../utils` still work because they only consume Safe Editor helpers that stayed in place.
- No behavior, types, props, hooks, dependencies, or Agent Workbench capabilities changed. The Agent Workbench boundary remains explicit / allowlisted / one-session / no-restore / no-auto-apply. No Rust change, no dependency or lockfile change, no Review Desk change, no `src/hooks/agentWorkbench/` restructure, no provider-add UI, and no new Agent provider / API surface.
- `npm run typecheck`, `npm run build:vite`, and `git diff --check` all pass for this slice. The slice touched only frontend files and a renamed frontend file, so `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` and `cargo test --manifest-path src-tauri/Cargo.toml` were not re-run; the prior v0.8 slices above already confirmed the 85-test suite is green with the Agent Workbench hooks in their previous shape and this slice is a pure code-organization refactor with no behavior change.

v0.8 Agent Workbench state hook boundary docstrings completion slice on 2026-06-02:

- Added a top-of-file boundary comment to the three Agent Workbench state hooks the prior "state boundary docstrings" entry above already declared as done — `src/hooks/useAgentWorkbenchRuntimeState.ts`, `src/hooks/useAgentOutputBuffer.ts`, and `src/hooks/useAgentUiRefreshGate.ts` — referencing `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md`. The runtime-state comment lists launch gate, session, terminal size, stop-pending, app-restart-pending, active-session derivation, and the deliberate placement of `appRestartPending` next to its sole writer `useAgentWorkbenchPreferenceActions.restartAppForAgentMode`. The output-buffer comment identifies the hook as the frontend mirror of the bounded Rust-side provider-output buffer, appended to on each successful session-state refresh and reset when the session goes away. The UI-refresh-gate comment identifies the polling-suspension ref and its consulters (`useAgentWorkbenchSessionSync` plus the hover/focus callers).
- Added a matching top-of-file boundary comment to `src/hooks/useAgentOutputSeqCursor.ts` for symmetry across the Agent Workbench ref-state hooks: it documents that the cursor tracks the last `seq` already applied to `useAgentOutputBuffer` so subsequent `getAgentWorkbenchSessionState` polls fetch only the delta, and that the `resetKey` (session `createdAtMs`) resets the cursor to 0 when the active session changes.
- Tightened the existing top-of-file boundary comment on `src/hooks/useAgentWorkbenchPreferences.ts` to spell out the duality: `agentWorkbenchActive` is the read-only *currently-running* state frozen at app start from `localStorage`, while `agentWorkbenchPreference` is the *user-desired* setting (settable, only takes effect after an app restart — the restart-required mode change is part of the Agent Workbench trust boundary). This makes the restart-required mode change visible at the call site instead of being implicit in the setter name.
- No behavior, types, props, hooks, dependencies, localStorage keys, or Agent Workbench capabilities changed. The Agent Workbench boundary remains explicit / allowlisted / one-session / no-restore / no-auto-apply. No Rust change, no dependency or lockfile change, no Review Desk change, no right-pane compare-route change, no `src/hooks/agentWorkbench/` restructure, no provider-add UI, and no new Agent provider / API surface.
- `npm run typecheck`, `npm run build:vite`, and `git diff --check` all pass for this slice. The slice touched only doc comments inside existing frontend hooks plus the `docs/current-status.md` entry, so `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` and `cargo test --manifest-path src-tauri/Cargo.toml` were not re-run; the prior v0.8 slices above already confirmed the 85-test suite is green with the Agent Workbench hooks in their previous shape and adding or rewording top-of-file comments is a no-op at the type and behavior level.

v0.8 Agent Workbench action/sync hook boundary docstrings slice on 2026-06-02:

- Added a top-of-file "what it owns / what it does NOT own" boundary comment to the six Agent Workbench action/sync hooks, mirroring the pattern already in place on the state/ref hooks. `src/hooks/useAgentLaunchGate.ts` documents the one-shot launch flow (preflight + `startAgentWorkbenchSession` + launch-gate transitions) and notes that it does not own any Agent state, session, terminal size, or output buffer. `src/hooks/useAgentSessionLifecycle.ts` documents the per-session handlers (differential session-state refresh, stop, write workspace file path), the internal `useAgentOutputSeqCursor` for the differential poll, and the absence of session/terminal/output ownership. `src/hooks/useAgentTerminalActions.ts` documents the terminal I/O handlers (send / resize / preset / selection-send) and calls out the resize-failure path as status-only by design (a PTY resize failure during an already-passed active session is a transport concern, not a launch-gate rejection), which is why it does not flow through `reportAgentLaunchGateError`. `src/hooks/useAgentWorkbenchSessionActions.ts` documents the fan-in wrapper role and the mechanical derivation of its options type from the two sub-hook options types. `src/hooks/useAgentWorkbenchPreferenceActions.ts` documents the preference-level handlers (mode toggle, app restart, consent, provider change), the absence of preference / gate / restart-pending / global-error ownership, and the provider-change guard's deliberate bypass of `reportAgentLaunchGateError`. `src/hooks/useAgentWorkbenchSessionSync.ts` documents the four background-sync effects (right-pane demote, session/buffer reset on availability change, session-state poll on `AGENT_WORKBENCH_SESSION_POLL_MS` cadence, workspace-root gate sync) and the fact that it returns nothing. Each comment references `docs/assist-surface-strategy.md` and `docs/agent-workbench-boundary.md`.
- Tightened the small options-type shape in `useAgentWorkbenchSessionActions`: the 14-field hand-rolled union was replaced with `Omit<UseAgentSessionLifecycleOptions & UseAgentLaunchGateOptions, "refreshAgentSessionState">`, with `UseAgentSessionLifecycleOptions` and `UseAgentLaunchGateOptions` newly exported from their respective sub-hooks. The wrapper's caller pattern in `src/App.tsx` is unchanged (it still passes one object literal with all 14 fields), and the type is now mechanically derived so any drift between the wrapper and the sub-hooks is caught at compile time.
- No behavior, types-as-seen-from-caller, props semantics, dependencies, localStorage keys, or Agent Workbench capabilities changed. The Agent Workbench boundary remains explicit / allowlisted / one-session / no-restore / no-auto-apply. No Rust change, no dependency or lockfile change, no Review Desk change, no right-pane compare-route change, no `src/hooks/agentWorkbench/` restructure, no provider-add UI, and no new Agent provider / API surface.
- `npm run typecheck`, `npm run build:vite`, and `git diff --check` all pass for this slice. The slice touched only doc comments, two new `*Options` exports, the wrapper's options-type derivation, and the `docs/current-status.md` entry, so `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` and `cargo test --manifest-path src-tauri/Cargo.toml` were not re-run; the prior v0.8 slices above already confirmed the 85-test suite is green with the Agent Workbench hooks in their previous shape, the new exports are pure type re-exports, and the wrapper's options-type derivation is a type-only tightening (the runtime call shape is unchanged and the previous hand-rolled union had the same 14 fields).

v0.8+ Detached Agent Window spike on 2026-06-02:

- Added the three new files for the detached Agent window: `agent.html` (Vite multi-entry mount with `<div id="root">` and `/src/agentEntry.tsx` script), `src/agentEntry.tsx` (second React entry that imports `./styles.css` and renders `<AgentWindowApp />`), and `src/components/AgentWindowApp.tsx` (the agent window's React tree: header with provider + status badge, launch-gate echo, plain-text output scrollback, textarea + Send / Stop buttons, status bar). The window owns its own `useAgentOutputBuffer` mirror and `useAgentOutputSeqCursor` cursor (reset by the active session's `createdAtMs`); it polls `getAgentWorkbenchSessionState(lastSeenSeq)` at the existing `AGENT_WORKBENCH_SESSION_POLL_MS` cadence while `document.hasFocus()`, stops the session with `stopAgentWorkbenchSession`, and writes a line of input with `writeAgentWorkbenchSessionInput`. Errors are status-only by design (no launch-gate flip in the child window, matching the resize-path precedent in `useAgentTerminalActions`).
- Added the Rust `open_agent_window` command (`src-tauri/src/lib.rs`) that **first** reads the `agent_workbench_active` and `agent_workbench_consent` `Arc<AtomicBool>` flags on the `AgentWorkbenchSessionStore` and returns `Err(...)` if either is false (defense in depth — the menu item is also gated client-side, but a direct IPC call must not be able to spawn the window when Agent Workbench is disabled or consent is missing). On pass, it focuses the existing `agent` window if present, or builds a new 800x600 (min 640x480, centered) `WebviewWindowBuilder` pointing at `agent.html` with title `hazakura agent`. The command is registered in `tauri::generate_handler![...]`. The new tauri imports are `WebviewUrl` and `WebviewWindowBuilder` (Tauri v2; sync `WebviewWindowBuilder` works on macOS at v0.7 target but deadlocks on Windows per Tauri v2 + WebView2 docs — making the command `async fn` is the correct future fix if Windows support is added). Added a new `MENU_OPEN_AGENT_WINDOW = "open-agent-window"` constant in `src-tauri/src/types.rs`, an `Open Agent Window` / `Agent ウィンドウを開く` (`えーじぇんとまどをひらく`) View menu item gated on `agent_workbench_active && agent_workbench_consent`, and the menu-event whitelist entry in `emit_app_menu_event`. The two new flags were added to `AppMenuState` (Rust `src-tauri/src/types.rs` and TS `src/tauri.ts`) and are mirrored into the session store by the existing `update_app_menu_state` command on every menu-state push, so the server-side gate and the menu-enablement flag stay in lockstep.
- **Server-side caller-window validation (second boundary layer).** Tauri v2's capability model gates plugin / core APIs by window label but auto-allowlists every custom `#[tauri::command]` for any window listed in a capability. With the agent window in `agent-window.json`, the child webview could otherwise call all 25+ project commands (file I/O, workspace tree, save, auto-backup, menu state, theme state, app restart, etc.). Every tauri command body goes through a `*_with_label(label: &str, ...)` variant that performs the gate via two label-only helpers in `src-tauri/src/lib.rs`: `ensure_label_is_main` (allowlist: `main` only) and `ensure_label_is_main_or_agent` (allowlist: `main` or `agent`). Both return `Err("Command is not allowed from window '<label>'.")` for any other label. The narrow `main`-only check is applied to `open_text_file`, `reveal_path_in_file_manager`, `open_temp_print_html`, `create_text_file`, `get_file_metadata`, `save_text_file`, `save_text_file_as`, `save_auto_backup`, `list_auto_backups`, `read_auto_backup`, `prune_auto_backups`, `list_workspace_tree`, `list_workspace_directory`, `open_workspace_image`, `save_pasted_image`, `import_image_from_path`, `start_agent_workbench_session`, `update_app_menu_state`, `update_theme_menu_state`, `drain_opened_files`, `request_app_restart`, and the new `open_agent_window`. The wider `main | agent` check is applied to the four Agent session commands the agent window legitimately needs: `stop_agent_workbench_session`, `get_agent_workbench_session_state`, `write_agent_workbench_session_input`, and `resize_agent_workbench_terminal` (the xterm parity slice widened `resize_agent_workbench_terminal` from `main`-only to `main | agent` so the detached Agent window's xterm fit-addon `ResizeObserver` can call it; the `start_agent_workbench_session` body stays `main`-only). Every tauri command signature gained a `window: tauri::WebviewWindow<R>` first parameter; the wrapper is a thin shim that calls `*_with_label(window.label(), ...)` and the body performs the gate. The two new `pub(crate) const` window-label constants (`MAIN_WINDOW_LABEL = "main"`, `AGENT_WINDOW_LABEL = "agent"`) sit next to the helpers. A small set of main-only commands that do not have unit-test call sites (the four `auto_backup::*` shims, `start_agent_workbench_session`, `update_app_menu_state`, `open_agent_window`, `update_theme_menu_state`, `drain_opened_files`, `request_app_restart`) use the same gate via a sibling `ensure_main_window(&window)` helper that takes a `&tauri::WebviewWindow<R>` instead of a `&str` — same allowlist (`main` only), same error string, just a different signature to avoid constructing a fake `WebviewWindow` in those bodies.
- Capability footprint is split and truly minimal: the main window's `src-tauri/capabilities/default.json` is **unchanged** (still at `"windows": ["main"]`, not widened), and a new `src-tauri/capabilities/agent-window.json` is scoped to `"windows": ["agent"]` with an **empty `permissions` array** — no `core:default`, no `core:event:default`, no `core:window`, no `core:webview`, no `core:menu`, no `core:tray`, no `dialog:default`, no plugin permissions. The empty `permissions` is deliberate: the earlier `core:default` grant expanded to event / menu / window / webview defaults, which would let a script in the agent webview emit global menu events (the main window's `useAppMenuActionListener` trusts the `APP_MENU_ACTION_EVENT` payload as menu actions like `save-as` / `export-html`). With no `core:event:default` the agent webview cannot emit events, and the server-side caller-window check above is the layer that restricts the agent webview to the three Agent session commands. The agent-window capability file's `description` field calls out this two-layer model.
- Frontend wiring: `src/types.ts` gained a `MENU_OPEN_AGENT_WINDOW` constant next to the other menu constants; `src/tauri.ts` gained an `openAgentWindow` fire-and-forget wrapper that mirrors the `updateAppMenuState` `isTauriRuntime()` guard and `console.warn`s on error; `AppMenuState` in `src/tauri.ts` gained `agentWorkbenchActive` and `agentWorkbenchConsent`; `useAppMenuActionListener` gained an `openAgentWindow` handler and the `MENU_OPEN_AGENT_WINDOW` case in the action switch; `useAppMenuActionsRef`, `useAppMenuIntegration` thread the new handler through; `useAppMenuStateSync` forwards the two new fields into `updateAppMenuState`; `useAppShellSync` accepts them and passes them down; `App.tsx` imports `openAgentWindow` from `./tauri`, wires the action via `useAppMenuIntegration.actions`, and threads `agentWorkbenchActive` / `agentWorkbenchConsent` into the `appShellSync` payload that feeds `useAppMenuStateSync`. `vite.config.ts` adds a `build.rollupOptions.input` map with `main: index.html` and `agent: agent.html` (with `import { resolve } from 'node:path'` at the top). `src/styles.css` gains a minimal `.agent-window-*` block (shell grid, header, status badge per session state, gate echo, plain-text scrollback, input + actions, status bar) — no existing class was renamed.
- Hard boundaries upheld: no Start session, no provider selection, no settings UI, no file commands, no dialog access, no xterm, no multi-session, no auto-apply, no session restore, no dependency or lockfile change, no Rust dependency change. The right-pane Agent path is unchanged and kept for verification. The `useAgentWorkbench*` hook family and `src/agentWorkbench.ts` helpers are not changed. Exactly one new Tauri command (`open_agent_window`) and one new TS wrapper (`openAgentWindow`) are added; the new `AppMenuState` fields reuse the existing `update_app_menu_state` IPC; the existing 5 Agent commands and 20+ project commands all keep the same JS call sites (Tauri auto-injects the new `window` parameter, so no JS / TS code needed to change).
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (106 Rust tests, including 21 new window-label-gate tests that pin the `main`-only / `main | agent` boundary and the unknown-label rejection), `npm run typecheck`, `npm run build:vite` (validates the Vite multi-entry and confirms `dist/agent.html` is emitted), `npm run build`, and `git diff --check` all pass for this slice with no `dead_code` / unused-import warnings. Built-app smoke of the new detached window is not claimed in this automation environment (the same `open -n` / in-app browser policy blockers as prior slices apply); the user will run the four-row manual smoke (Safe Editor opens, menu gating on the View > Open Agent Window item, child window opens / closes / reopens, no Start button in the agent window and no second-session path) per the plan file.
- Residual gaps (deliberately out of scope for this spike): no Tauri events for cross-window sync (both windows poll Rust independently; brief ≤200 ms inconsistency possible during fast state changes); no window position / size persistence (the agent window opens centered each time; `tauri-plugin-window-state` is a follow-up); no xterm rendering in the agent window (plain text, ANSI escape sequences stripped; a future slice can layer xterm on top of the same bounded buffer); no "Start" / launch-gate check in the agent window (the user must start sessions from the main window — intentional and matches the brief); the new `open_agent_window` command uses a sync `WebviewWindowBuilder` (works on macOS at v0.7 target but deadlocks on Windows per Tauri v2 + WebView2 docs — making the command `async fn` is the correct future fix); the right-pane Agent path is unchanged and kept for verification (a future slice may remove it once the detached window is confirmed equivalent or better); the agent window does not reflect mode-toggle changes in real time (if the user disables Agent Workbench in the main window's Preferences while the agent window is open, the agent window keeps showing the last-known session state until the next poll — closing and reopening the agent window is the safe reset path); the server-side gate reads the *current* active + consent state, not the preference-to-be-applied-after-restart, matching the existing launch-gate behavior.

v0.8+ Detached Agent Window xterm parity slice on 2026-06-02:

- Replaced the plain-text scrollback + `<textarea>` body in `src/components/AgentWindowApp.tsx` with the existing right-pane xterm surface (`src/components/AgentTerminalView.tsx` — xterm + addon-fit + ResizeObserver) so the detached Agent window can drive a real PTY. The window's own `useAgentOutputBuffer` / `useAgentOutputSeqCursor` mirror and 200 ms `document.hasFocus()` poll stay unchanged; the four new handlers are `handleTerminalData` (xterm `onData` → `writeAgentWorkbenchSessionInput`, gated on `isActiveAgentSession` so dead input cannot reach Rust; xterm already echoes the keystroke so the success path is a no-op), `handleTerminalResize` (fit-addon columns/rows → `resizeAgentWorkbenchTerminal`, updates the cached session on success), `handleTerminalEngage` / `handleTerminalRelease` (xterm focus / blur → status bar). The `AgentTerminalView` is configured with `theme="dark"`, `placeholder` set to either "Agent is starting..." (active session) or "No Agent session. Start one from the main window to use this terminal." (idle), and `terminalLabel="Detached Agent terminal"`. The agent window's `agentLaunchGate` echo, provider / status badge, and Stop-session button are unchanged. The Stop-session call still uses the `stopAgentWorkbenchSession` IPC path; the only Rust changes for this slice are the gate widening described below.
- Widened the server-side window-label gate for `resize_agent_workbench_terminal` from `main`-only to `main | agent` in `src-tauri/src/lib.rs` (added a sibling `resize_agent_workbench_terminal_with_label(label, session_store, columns, rows)` body that calls `ensure_label_is_main_or_agent` then delegates to the existing `resize_agent_workbench_terminal_with_store` body; the tauri command wrapper passes the new `window: tauri::WebviewWindow<R>` parameter through). Added a `start_agent_workbench_session_with_label(label, session_store, provider, workspace_root)` body that calls `ensure_label_is_main` (no widening — Start must stay main-only) then delegates to the existing start body, with the tauri command wrapper passing the new `window` parameter through, so the `main`-only gate on Start now has a unit-test call site. The top-of-file comment in `src-tauri/src/lib.rs` was updated to call out the new `main | agent` allowlist on `resize_agent_workbench_terminal` and to list the three `main | agent`-allowed session commands plus the resize command, mirroring the `main`-only list. The agent window therefore legitimately uses **four** project commands (Stop / get-state / write-input / resize) plus its own `open_agent_window` opener.
- 6 new unit tests in `src-tauri/src/tests.rs` pin the new boundary: `agent_resize_allows_main_and_agent_labels` (proves the widening on `resize_agent_workbench_terminal_with_label`), `agent_resize_rejects_unknown_label` (proves the gate catches anything outside `main | agent`), `agent_resize_rejects_zero_dimensions_from_any_label` (proves that dimension validation lives in the body, not the gate — the test loops over `MAIN_WINDOW_LABEL` and `AGENT_WINDOW_LABEL`, sends `columns=0, rows=24`, and asserts the error contains `"terminal size"` (the body error string); a gate short-circuit would have returned the label error and tripped the assertion's "got gate error" message, so the test pins that the label gate passes for both allowed labels and the body runs to reject the zero dimensions), `agent_start_rejects_agent_window_label` (pins that Start stays `main`-only even though the agent label is otherwise widely allowed), `agent_start_rejects_unknown_label`, and `agent_start_allows_main_label` (positive case using a non-allowlisted provider to prove the gate cleared, which the start body would otherwise reject as `Agent provider not allowlisted`). The 6 new tests bring the suite to **112 tests passing** (was 106).
- Trimmed `src/styles.css`'s `.agent-window-*` block to match the xterm body: dropped `.agent-window-output` / `.agent-window-output:empty::before` (the plain-text scrollback), `.agent-window-controls` / `.agent-window-input-label` / `.agent-window-input` / `.agent-window-input:focus-visible` / `.agent-window-input:disabled` (the textarea), and `.agent-window-send` / `.agent-window-send:disabled` (the Send button); updated the comment at the top of the block from "does not run xterm" to "reuses the right-pane xterm surface (`.agent-terminal-host`)"; promoted the shell to a 4-row grid (`auto auto minmax(0, 1fr) auto` — header, gate, terminal, status bar) with a 12px shell padding + 10px row gap; rounded every section (header / gate / actions / statusbar) into 6px cards so the window reads as a self-contained surface; and added a small `.agent-window-shell .agent-terminal-shell { min-height: 0; width: 100%; }` rule so the right-pane terminal wrapper fills its grid cell. The Stop button rule is the only action rule that survived the trim.
- Hard boundaries upheld: no Start button in the agent window, no provider selection, no settings UI, no file commands, no dialog access, no auto-apply, no multi-session, no session restore, no dependency or lockfile change, no Rust dependency change. The right-pane Agent path is unchanged and kept for verification. The new TS surface added to `AgentWindowApp` is the four `AgentTerminalView` handlers — no new IPC contract, no new Tauri command, no new tauri.ts wrapper, no new capability entry, no new menu state. The `useAgentWorkbench*` hook family and `src/agentWorkbench.ts` helpers are not changed. Theme sync between windows is intentionally out of scope (the agent window is hard-coded to `theme="dark"` — propagating the main window's theme is a follow-up).
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (112 Rust tests, including the 6 new boundary tests described above), `npm run typecheck`, `npm run build:vite`, `npm run build`, and `git diff --check` all pass for this slice with no `dead_code` / unused-import warnings. Built-app smoke of the new xterm-equipped agent window is not claimed in this automation environment (the same `open -n` / in-app browser policy blockers as prior slices apply); the user will run the same four-row manual smoke as the prior spike, plus a new "xterm echoes typed input / Resize the agent window → PTY size follows / `q` or `Ctrl+D` from the agent window reaches the provider and exits cleanly" check before merging.
- Residual gaps (deliberately out of scope for this slice): theme is hard-coded to `dark` in `AgentWindowApp` (no propagation from the main window's theme preference — a follow-up); no Tauri events for cross-window sync (both windows poll Rust independently; brief ≤200 ms inconsistency possible during fast state changes); no window position / size persistence (the agent window opens centered each time; `tauri-plugin-window-state` is a follow-up); the new `open_agent_window` command is still sync `WebviewWindowBuilder` (works on macOS at v0.7 target but deadlocks on Windows per Tauri v2 + WebView2 docs — making the command `async fn` is the correct future fix if Windows support is added); the right-pane Agent path is unchanged and kept for verification; the agent window does not reflect mode-toggle changes in real time (if the user disables Agent Workbench in the main window's Preferences while the agent window is open, the agent window keeps showing the last-known session state until the next poll — closing and reopening the agent window is the safe reset path); the server-side gate reads the *current* active + consent state, not the preference-to-be-applied-after-restart, matching the existing launch-gate behavior.

v0.8+ Detached Agent Window design polish slice on 2026-06-02:

- Restyled `src/components/AgentWindowApp.tsx` and `src/styles.css`'s `.agent-window-*` block from a rounded-card layout into a four-row tool-window chrome: a panel-style header (title + "Detached tool window" subtitle on the left, provider mono-label + state pill on the right with a 6px dot that pulses when the session is active), a monospace info row (workspace path / started time / last output time / input state, divided by 1px borders — same key-then-value pattern as the terminal meta), the shared xterm surface, and a compact footer with a left-aligned Stop button (with a 7px red dot) and a right-aligned status message. Outer padding is `0` and the section dividers are 1px borders instead of 6px rounded cards, so the window reads as a purpose-built tool window (think Photoshop / IDE panels) rather than a generic browser popup. The redundant `agentLaunchGate` echo row was dropped — the launch-gate flow is main-window-only and was hard-coded to `idle` here, so the row was just decoration; the info row now carries the more useful session metadata. Theme is fully propagated from the main window: `AgentWindowApp` reads `THEME_STORAGE_KEY` on mount, listens for the cross-window `storage` event, stamps `document.documentElement.dataset.theme` + `document.documentElement.dataset.themePreference` so the same CSS variable surface used by the main window applies here, and forwards the resolved `ThemePreference` to `AgentTerminalView`'s `theme` prop so the xterm palette tracks the main window. The detached window never mutates the theme; it only listens for the main window's writes.
- Added a `now` ticking state in `AgentWindowApp` (one-second `setInterval`) and derived `startedLabel` (from `session.createdAtMs`) and `lastOutputLabel` (from the last output chunk's `receivedAtMs`). Both labels render as compact `Xs ago` / `Xm Ys ago` / `Xh Ym ago` strings so the user can judge session age and liveness at a glance. The polling effect and the `useAgentOutputBuffer` mirror stay untouched — the time labels derive from the same state the polling effect already updates.
- Enhanced `src/components/AgentTerminalView.tsx`'s meta line (shared by both the right-pane Agent and the detached agent window) from a single `Output chunks: N / 500` text into a structured three-cell row: `{outputLabel}: {output.length} / {AGENT_WORKBENCH_MAX_OUTPUT_CHUNKS}` · `last: <relative time>` · `input: <ready|disabled>`, with the `input` cell colored via `data-input` (accent when ready, muted when disabled). The `last` time is derived from the last chunk's `receivedAtMs` against an internal 1s `now` tick that does not touch the xterm output. Both panes now show the same live activity information without any new IPC, and the right-pane benefits from the new meta line + the focus ring described below.
- Added a focus-within state to the shared `.agent-terminal-shell`: when `.active` and focused, the border color shifts to `color-mix(var(--accent) 55%, var(--border))` and an inset 1px `var(--accent)` glow at 28% opacity; when `.inactive` and focused, the border color shifts to `var(--border-strong)`. The previous behavior was a flat `var(--border)` regardless of state, so users had no visual cue for "I can type here now" — the new state gives a clear focus affordance in both panes. The detached-window override at `.agent-window-shell .agent-terminal-shell` only sets `border-left/right: 0` and `border-radius: 0` (so the terminal flush-fits the tool-window chrome); the focus state is preserved.
- Hard boundaries upheld: no Start button, no provider selection, no settings UI, no file commands, no dialog access, no multi-session, no auto-apply, no session restore, no dependency or lockfile change, no Rust change, no capability widening, no product/roadmap doc change, no `useAgentWorkbench*` hook change, no new IPC contract, no new Tauri command, no new tauri.ts wrapper, no new menu state, no `src/agentWorkbench.ts` helper change. The detached window's IPC surface is still the four commands established by the prior slice (Stop / get-state / write-input / resize) plus its own `open_agent_window` opener. The right-pane Agent path is unchanged in code; it only inherits the new terminal meta line and the focus ring from the shared `AgentTerminalView` / `.agent-terminal-shell` rules. Theme sync between windows is one-way: main → agent; the agent window never writes the theme. The `lastOutputLabel` updates are tick-driven (1s), not poll-driven, so they don't increase IPC traffic.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (112 Rust tests, unchanged from the prior slice), `npm run typecheck`, `npm run build:vite` (emits `dist/agent.html` and the new `dist/assets/agent-*.js` at ~5kB, up from ~3kB; the `dist/assets/styles-*.css` is now ~80kB, up from ~76kB for the new agent-window / terminal-meta rules), `npm run build` (full Tauri bundle builds, signed with `-`, notarization skipped as expected), and `git diff --check` all pass with no `dead_code` / unused-import warnings. Built-app visual smoke of the new design is not claimed in this automation environment (the same `open -n` / in-app browser policy blockers as prior slices apply); the user will run the standard manual smoke (start a session in the main window → open the agent window → xterm accepts input → output is readable → window resize keeps the terminal intact → Stop is effective) plus a theme-sync check (toggle the main window's theme while the agent window is open → agent window's chrome and xterm palette update without a restart).
- Residual gaps (deliberately out of scope for this slice): no Tauri events for cross-window sync (both windows still poll Rust independently; brief ≤200 ms inconsistency possible during fast state changes); no window position / size persistence (the agent window opens centered each time; `tauri-plugin-window-state` is a follow-up); the `open_agent_window` command is still sync `WebviewWindowBuilder` (works on macOS at v0.7 target but deadlocks on Windows per Tauri v2 + WebView2 docs — making the command `async fn` is the correct future fix if Windows support is added); the right-pane Agent path is kept for verification and only inherits the shared terminal-meta / focus-ring improvements; the agent window does not reflect mode-toggle changes (active / consent) in real time (the last-known session state is shown until the next poll — closing and reopening the agent window is the safe reset path); the server-side gate reads the *current* active + consent state, not the preference-to-be-applied-after-restart, matching the existing launch-gate behavior; the `inputStateLabel` and `lastOutputLabel` are not keyboard-navigable (no `aria-live` polite region — a follow-up if the agent window is used in voiceover / accessibility scenarios).

v0.8+ Detached Agent Window pre-merge fixes on 2026-06-02:

- Replaced the static-dark title bar workaround with a proper per-theme title bar color path. The fix uses a **custom** Tauri command that does the `set_background_color` + `set_theme` work on the Rust side via the `app.get_webview_window("agent")` handle — this stays inside the existing capability model (custom commands are auto-allowlisted for any window listed in a capability, and the body touches windows through the `AppHandle`, not through a JS-side core window API), so the agent-window capability stays at the same empty `permissions` array and no `core:window:allow-set-theme` / `core:window:allow-set-background-color` grants are required. Three pieces. **(1) `open_agent_window(theme: Option<String>)`** in `src-tauri/src/lib.rs` now takes the main window's current `THEME_STORAGE_KEY` value as a parameter; a new `agent_window_background_color(theme)` helper maps each of the six preferences (`dark` / `sakura` / `yakou` / `shokou` / `kouyou` / `light`) to the same hex the main window uses (mirroring `windowBackgroundColorForTheme` in `src/hooks/useAppPreferences.ts`), and a new `agent_window_os_theme(theme)` helper maps `dark` / `yakou` → `Theme::Dark`, everything else → `Theme::Light` (mirroring the `BaseTheme` derivation in `useAppPreferences`). The `WebviewWindowBuilder` gained `.title_bar_style(TitleBarStyle::Transparent).background_color(agent_window_background_color(&theme)).theme(Some(agent_window_os_theme(&theme)))` so the initial paint already matches the main window's chrome — no flash of the wrong-color title bar on first open. The "window already open, refocus" branch also calls `set_background_color` + `set_theme` on the existing window so a re-open from the menu catches up to a theme change that happened while the window was open. **(2) `set_agent_window_theme(window, app, theme)`** is a new Tauri command gated with `ensure_label_is_main_or_agent` (so both the main window and the agent window can invoke it). The body looks up `app.get_webview_window(AGENT_WINDOW_LABEL)`, calls `set_background_color(Some(bg))` and `set_theme(Some(os_theme))`, and is a no-op (returns `Ok(())`) when the window is not open. The new command is registered in `tauri::generate_handler![...]`. The agent-window capability's `description` was updated to list it alongside the four Agent session commands and explain that all other custom commands stay main-only. **(3) Frontend wiring**: `src/tauri.ts` gained a `setAgentWindowTheme(theme: string)` fire-and-forget wrapper (same `isTauriRuntime()` guard + `console.warn` pattern as `updateAppMenuState`); the existing `openAgentWindow()` wrapper now takes an optional `theme` arg and passes it as `{ theme: theme ?? null }` to `invoke("open_agent_window", ...)`. `src/App.tsx`'s menu action calls `openAgentWindow(themePreference)` so the open-time title bar uses the main window's current theme. `src/hooks/useAppPreferences.ts` now also calls `setAgentWindowTheme(themePreference)` inside the existing `[themePreference]` useEffect, so a theme toggle in the main window's Preferences pushes into the open agent window in real time (Rust-side, no JS core window permissions). `src/components/AgentWindowApp.tsx`'s existing `[theme]` useEffect (the one that already stamps `document.documentElement.dataset.theme` on the cross-window `storage` event) also calls `setAgentWindowTheme(theme)`, so the agent window self-syncs on mount and on every cross-window theme change — handles the case where the main window changes the theme while the agent window is open AND the case where the agent window is opened after a theme change has already happened.
- **App menu auto-close on session start (pre-existing main-window bug, not fully fixed)**: investigated the `useAppMenuStateSync` → `update_app_menu_state` → `app.set_menu` path, all 13 of the sync hook's deps (`activeDirty` / `activeTab` / `agentWorkbenchActive` / `agentWorkbenchConsent` / `editorSettings.{showInvisibles,spellcheckEnabled,wrapLines}` / `menuLanguage` / `previewVisible` / `recentFiles` / `recentFolders` / `themePreference`), and the only caller of `app.set_menu` in Rust (the `update_app_menu_state` body at `src-tauri/src/lib.rs:1175-1178`). None of the sync hook deps are expected to change on `useAgentLaunchGate.checkAgentLaunchGate` → `startAgentWorkbenchSession` → session-active transition, so `app.set_menu` should not be re-invoked on session start; the 1-second timing the user reported does not match the 200 ms polling cadence or any `useEffect` in the agent launch / refresh path. Root cause is likely a macOS native menu / focus quirk when a long IPC call completes (e.g., the menu submenu dismisses itself when the app becomes "busy" and does not reappear cleanly), but a full diagnosis requires running the app. Documented here as a residual gap so the next reviewer / future slice can pick it up. No code change targets it.
- Hard boundaries upheld: no capability widening (the agent-window capability's `permissions` array is still `[]`; the new `set_agent_window_theme` is a custom command that does its window work on the Rust side via `app.get_webview_window`, so the JS-side core window permissions that would have been needed for an in-page `getCurrentWindow().setBackgroundColor()` call stay forbidden); no JS-side `setCurrentWindowTheme` / `setCurrentWindowBackgroundColor` calls in the agent window; no menu-state change, no `useAgentWorkbench*` hook change, no new IPC contract beyond the one custom `set_agent_window_theme` command, no dependency or lockfile change. The detached window's IPC surface is the four Agent session commands established by the prior slice (Stop / get-state / write-input / resize) plus `open_agent_window` and the new `set_agent_window_theme` opener. The right-pane Agent path is unchanged in code; it only inherits the design-polish slice's terminal-meta / focus-ring improvements.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (112 Rust tests, unchanged from the prior slice — the new `set_agent_window_theme` body is a thin label gate + an `app.get_webview_window(...).set_background_color(...).set_theme(...)` chain that the existing test surface does not exercise, consistent with the project's pattern of leaving small main- or main|agent-allowed custom commands without `_with_label` body variants), `npm run typecheck`, `npm run build:vite` (emits `dist/agent.html` and the updated `dist/assets/agent-*.js` at ~5.1kB, up from ~5.0kB; the new `setAgentWindowTheme` call adds ~80 bytes), `npm run build` (full Tauri bundle builds, signed with `-`, notarization skipped as expected), and `git diff --check` all pass for this fix with no `dead_code` / unused-import warnings. Built-app visual smoke of the new per-theme title bar is not claimed in this automation environment (the same `open -n` / in-app browser policy blockers as prior slices apply); the user will verify by toggling the main window's theme between at least two of `dark` / `sakura` / `light` and confirming the agent window's title bar / traffic-lights follow.
- Residual gaps (deliberately out of scope for this fix): the menu auto-close on session start is a pre-existing main-window bug and is not fully fixed (no code change targets it; a future slice can add a Rust-side focus guard or a JS-side `useAppMenuStateSync` debounce once the root cause is reproduced in a dev build).

Post-v0.8 feature-folder refactor lane on 2026-06-03 (5 slices, 6 commits, no behavior change):

- Slice 1: `lib/tauri.ts` and `lib/locale.ts` were split by feature area. `src/lib/tauri/` is now `_runtime.ts` + `agent.ts` + `app.ts` + `autoBackup.ts` + `dialog.ts` + `files.ts` + `menu.ts` + `theme.ts` + `window.ts` + `workspace.ts` + `index.ts` (barrel). `src/lib/locale/` is now `_helpers.ts` + `agentWorkbench.ts` + `editorChrome.ts` + `preferences.ts` + `recovery.ts` + `reviewDesk.ts` + `safeEditor.ts` + `sidePane.ts` + `slashMenu.ts` + `index.ts` (barrel). Both barrels re-export the public surface so every existing `import { … } from "./tauri"` / `import { … } from "./locale"` call site is unchanged. The `lib/tauri/_runtime.ts` module isolates the `isTauriRuntime()` / `isTauriMobile()` predicates that the per-feature wrappers use as their fire-and-forget guards; the `lib/locale/_helpers.ts` module isolates the deep-merge and copy-derivation utilities used by the per-feature copy tables. No surface-level type / behavior / call-site change; the Agent Workbench, Review Desk, right-pane compare, save/recovery, and preferences boundaries are untouched.
- Slice 2: `src-tauri/src/tests.rs` (3378 lines, 122 tests) was split into `src-tauri/src/tests/{mod,common,agent_workbench,app_window,backup,encoding,files,security,util,workspace,workspace_broadcast}.rs`. The `tests/mod.rs` entry point declares the submodules and re-exports the shared items via `pub(crate) use common::*;` / `pub(crate) use crate::auto_backup;` so per-feature test modules can name `RecordingRuntimeAdapter`, `FakeProviderFixture`, `unique_test_dir`, etc. without re-importing from the entry point. `tests/common.rs` holds the shared fixtures (`RuntimeAdapterCall` with `pub(crate)` fields, `RecordingRuntimeAdapter`, `FakeProviderFixture`, `unique_test_dir`, `fake_provider_fixture`, `wait_for_agent_state`, `combined_agent_output`, `assert_agent_output_seq_strictly_increases`, `make_executable`, `process_exists`, `unique_label_path`) and the `pub(crate) use std::path::Path; pub(crate) use std::process::Stdio; pub(crate) use std::time::Duration;` re-exports that sibling modules need. The 122 tests stay green; the per-feature test file `backup.rs` was named to avoid colliding with the existing `crate::auto_backup` module (a `tests/auto_backup.rs` would have produced E0255 "defined multiple times").
- Slice 3: The Agent Workbench session state machine moved out of `src-tauri/src/commands/agent_workbench.rs` into `src-tauri/src/agent.rs`. The commands file is now Tauri-shim-only with `*_with_label(label, …)` wrappers, and the state-machine functions (`start_agent_workbench_session_with_store`, `stop_agent_workbench_session_with_store`, `get_agent_workbench_session_state_with_store`, `get_agent_workbench_session_state_since_with_store`, `write_agent_workbench_session_input_with_store`, `resize_agent_workbench_terminal_with_store`, `refresh_agent_workbench_session_exit`, `build_agent_workbench_preflight`, `validate_agent_workbench_launch`) own all the runtime logic. The boundary between the layer that talks to Tauri (commands) and the layer that talks to the runtime store (state machine) is now visible at a glance; the Tauri command shim no longer carries the `Mutex` / `Arc` / launch-gate logic and can be read as a thin label-gate-and-delegate wrapper. No call-site change; the Tauri shim still exposes the same `*_with_label` signatures to the unit tests and the same command names to the frontend.
- Slice 4: The shared Markdown `HighlightStyle` was extracted to `src/features/editor/codeMirrorTheme.ts` (`markdownHighlightStyle()` + `markdownSyntaxHighlighting()`). The duplicate definitions in `src/components/editor/EditorPane.tsx` (`editorMarkdownHighlightStyle`) and `src/components/editor/CandidateEditor.tsx` (`candidateEditorHighlightStyle`) were removed; both now call `markdownSyntaxHighlighting()`. The unused `HighlightStyle` / `syntaxHighlighting` / `tags as highlightTags` imports were dropped from each component. The candidate editor and the main editor now share the exact same Markdown tag → style mapping; future highlight tweaks happen in one file.
- Slice 5a: `formatLineEndingKind` and `formatTextEncoding` moved to `src/lib/format.ts` so `src/hooks/document/useDocumentStatus.ts` and `src/hooks/editor/useEditorCommands.ts` share the same enum-to-label helpers. The new module is a pure-formatting layer (no React, no Tauri dependencies) that lives outside the per-feature hooks so it can be reused by other call sites without dragging the hook import graph along. The status bar's `UTF-8` / `UTF-8 BOM` / `Shift-JIS` / `EUC-JP` label and the line-ending selector's `LF` / `CRLF` / `Mixed` / `No line endings` label both flow through the shared helper.
- Slice 5b: `open_text_file_with_label` in `src-tauri/src/commands/files.rs` now calls the existing `readable_text_metadata(&path_buf)?` helper instead of duplicating the is_file / size / binary preflight checks, so the `open_text_file` and `get_file_metadata` commands can't drift on the preflight rules. The 13 lines of duplicate preflight collapse to a single helper call; the file behavior (file-only, ≤ 10 MB, non-binary, with the same `Cannot read file: {err}` / `Selected path is not a file.` / `File is larger than the prototype editing limit of 10 MB.` / `Binary-looking files are not opened by this editor.` error messages) is unchanged because the preflight was lifted verbatim.
- Hard boundaries upheld across all 5 slices: no Rust capability widening, no Agent Workbench boundary change, no Review Desk / right-pane compare-route change, no dependency or lockfile change, no `src/hooks/agentWorkbench/` restructure, no provider-add UI, no new Agent provider / API surface, no docs claim beyond the implemented subset. Per-slice verification: `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (122 tests, 0 failed, 0 ignored; the test suite is unchanged because Slice 2 is a pure file split with no test additions / removals), `npm run build:vite` (emits `dist/agent.html` plus the split `dist/assets/*.{js,css,png}` chunks), `npm run build` (full Tauri bundle builds, signed with `-`, notarization skipped as expected), `git diff --check`. Built-app smoke of the refactor surface is not claimed in this automation environment (the same `open -n` / in-app browser policy blockers as prior slices apply); the user runs the standard manual smoke per the existing `docs/smoke-checklist.md` rows (file open / save / encoding chip / auto-backup / Find-Replace / review desk / detached agent window / per-theme title bar / show-in-main-pane reverse link) to confirm the refactor changed nothing user-visible.
- Deliberately left out of scope per the user-approved "no behavior change" constraint: the `EditorTab.line_ending` → `lineEnding` rename (would require a localStorage key migration line and was judged invasive for a strict no-behavior-change slice) and the `useSlashMenu` `setTimeout` cleanup (would be a strict-mode behavior change and was judged a separate, explicitly-behavior-adjacent slice).



- Manual smoke of the detached window on the prior title-bar-fix slice showed input / output / resize / Stop / theme / long output / close-reopen / cross-window state drift were all clean, and the detached window is materially better than the right-pane Agent. This slice promotes the detached window to the primary route and demotes the right-pane Agent to a fallback / verification surface. Three changes.
- **(1) Window size**. `src-tauri/src/lib.rs` `open_agent_window` builder changed from `.inner_size(800.0, 600.0).min_inner_size(640.0, 480.0)` to `.inner_size(440.0, 760.0).min_inner_size(380.0, 520.0)`. 440 px is a Photoshop-style tool-window width (not a browser popup), 760 px keeps the xterm surface readable (≈ 53 monospace columns at the existing `fontSize: 13`), and 380 × 520 is the floor below which the chrome starts overlapping. Centered, transparent title bar, per-theme `background_color` + `theme(...)` from the prior slice stay.
- **(2) Narrow-layout polish (CSS)**. `src/styles.css`: `.agent-window-info-cell { max-width: 18rem }` → `10rem` so the four info cells (`workspace / started / last output / input`) share the row fairly at 440 px (workspace path was dominating at 18 rem ≈ 288 px). The full workspace path is still in the `title` attribute. `.agent-window-provider` gained `max-width: 9rem` + `text-overflow: ellipsis` + `overflow: hidden` + `white-space: nowrap` so a long provider label can't crowd the title (full label still in `title`). The state pill, terminal meta (already `flex-wrap: wrap` from the prior design-polish slice), and status message need no change. xterm at 440 px keeps the existing `fontSize: 13`; no font-size change.
- **(3) Right-pane demotion (TSX)**. New `agent-pane-demotion` banner at the top of `src/components/AgentPaneShell.tsx` — a thin row with a one-liner that says "For the dedicated Agent Window, use **View → Open Agent Window**" plus a small button (`agent-pane-demotion-cta`) styled as an inline accent-colored link that calls the existing `openAgentWindow(themePreference)` wrapper. The right-pane Agent toggle (`RightPaneToggleControls.tsx`), Start button, input / output, and gate flow are all unchanged — the demotion is via banner copy, not via visual de-emphasis of the toggle or removal of the path. `onOpenAgentWindow` was threaded through `AppWorkspace` → `SidePane` → `AgentPaneShell` (and the `AppShellProps` union auto-extends it via `ComponentProps<typeof AppWorkspace>`), and the en copy ("For the dedicated Agent Window, ...") and ja/kana copy ("専用の Agent ウィンドウは、表示 → Agent ウィンドウを開く、から。") live on the existing `menuLanguage`-gated `copy` table.
- **(4) Detached → main reverse link (Rust + TS)**. Added a new Tauri command `open_main_agent_pane(window, app)` in `src-tauri/src/lib.rs`. Gated on `ensure_label_is_main_or_agent` (so the agent window can call it directly; the main window can call it too, where it's a no-op-ish "ensure main is focused + emit the event"). Body: `app.get_webview_window(MAIN_WINDOW_LABEL).map(|w| w.set_focus())` + `app.emit_to(MAIN_WINDOW_LABEL, OPEN_MAIN_AGENT_PANE_EVENT, ())` (both best-effort, `let _ =`). A label-only `open_main_agent_pane_with_label(label: &str)` shim with `#[cfg_attr(not(test), allow(dead_code))]` is unit-testable without a real `AppHandle`, mirroring the `*_with_label` pattern used by every other boundary test; 2 new tests in `src-tauri/src/tests.rs` (`open_main_agent_pane_allows_main_and_agent_labels`, `open_main_agent_pane_rejects_unknown_label`) pin the gate, taking the suite from 112 → 114. New `OPEN_MAIN_AGENT_PANE_EVENT` constant in `src-tauri/src/types.rs` and mirrored in `src/types.ts`; new `openMainAgentPane()` fire-and-forget wrapper in `src/tauri.ts`; new `src/hooks/useMainAgentPaneFocus.ts` hook that `listen<()>(OPEN_MAIN_AGENT_PANE_EVENT, ...)` and calls `onOpen()` on event (cancelled-flag pattern, `isTauriRuntime()` guard, `console.warn` on listen failure, matching `useOpenedFilesListener` / `useAppMenuActionListener`); mounted in `src/App.tsx` right after `useAppRuntimeEffects({...})` with `onOpen = toggleAgentPane` so the main window's right pane flips to Agent mode (and opens the side pane if it was closed). Detached footer in `src/components/AgentWindowApp.tsx` gained a small text button between the existing Stop button and the status message ("Show in main pane", with a `title` tooltip) that calls `openMainAgentPane()`. Always enabled (this is a navigation, not a session action — clicking before a session is active just shows the right pane in its idle state). The new command is `main | agent`-gated and the agent-window capability's `description` was updated to list it alongside the four Agent session commands and `set_agent_window_theme`; `permissions: []` is unchanged so no capability widening occurred. New `agent-window-show-in-main` CSS rule in `src/styles.css` matches the existing footer button chrome (rounded border, hover surface-strong, focus-visible accent outline).
- Hard boundaries upheld: no `core:window:*` / plugin permissions added; the agent-window capability's `permissions` array is still `[]`; the new `open_main_agent_pane` is a custom command that does its cross-window work on the Rust side via `app.emit_to` and `app.get_webview_window`, so the JS-side core event / window permissions that would have been needed for an in-page emit stay forbidden; no Start button / provider selection / settings / file commands / dialog access / multi-session / auto-apply / session restore; no product or roadmap doc changes; no dependency or lockfile change. The detached window's IPC surface grows from 6 commands (Stop / get-state / write-input / resize / open-agent-window / set-agent-window-theme) to 7; the new command is reverse-link-only. The right-pane Agent path is fully functional and unchanged in behavior — the demotion is via the new banner copy only. App.tsx remains a pure composition file (now ~926 lines; +15 from the prior slice for the `useMainAgentPaneFocus({ onOpen: toggleAgentPane })` call and the `onOpenAgentWindow: () => openAgentWindow(themePreference)` shellProp wiring).
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (114 Rust tests, 0 failed, 0 ignored, +2 from the prior slice), `npm run typecheck`, `npm run build:vite` (emits `dist/agent.html` and the updated `dist/assets/agent-*.js` at ~5.2kB, up from ~5.1kB; the new `openMainAgentPane` call adds ~80 bytes), `npm run build` (full Tauri bundle builds, signed with `-`, notarization skipped as expected), and `git diff --check` all pass with no `dead_code` / unused-import warnings. Built-app visual smoke of the new narrow chrome and the new reverse link is not claimed in this automation environment (the same `open -n` / in-app browser policy blockers as prior slices apply); the user will verify the new default size + narrow layout + reverse link per the manual smoke checklist in the slice plan.
- Residual gaps (deliberately out of scope for this slice): no Tauri events for cross-window session sync (both windows still poll Rust independently; brief ≤ 200 ms inconsistency possible during fast state changes) — unchanged from prior slice; no window position / size persistence (the agent window opens centered each time; `tauri-plugin-window-state` is a follow-up); the right-pane Agent does not visually dim / hide when the detached window is open (would require a new IPC for the main window to learn about the detached window's state, explicitly out of scope per the slice plan); the reverse-link button is always enabled (not gated on active session) since it's a navigation action; app menu auto-close on session start remains a pre-existing main-window bug, not addressed here.

v0.8 Claude Code CLI readiness docs pass on 2026-06-02:

- Updated `docs/roadmap.md`, `docs/development-automation.md`, `docs/assist-surface-strategy.md`, and `docs/agent-workbench-boundary.md` so Claude Code CLI is a candidate future allowlisted local CLI provider, not a replacement strategy and not currently implemented. The docs keep current implemented provider claims (`codex` / `opencode` / `pi`) separate from the future `claude` readiness lane.
- No code, provider allowlist, README provider list, smoke matrix, dependency, lockfile, Rust, Review Desk, Foundation Models, provider-add UI, arbitrary args, MCP UI, Git, or auto-apply changes.
- `git diff --check` passed for this docs-only planning pass.

v0.8 planning adjustment on 2026-06-02:

- Review Desk should stay as a small safety receiver for candidate text, not grow into the main workbench. Keep explicit Compare / Apply, stale guards, no auto-save, no auto-apply, and no persistent review logs by default.
- Review Desk should be lower prominence in the default UI. Shortcut, View menu, and slash access remain useful; the persistent top-chrome entry is no longer the preferred default.
- v0.8 should focus on Safe Editor daily-use polish plus Assist Surface separation: clarify Agent Workbench surface/state boundaries and prepare request/candidate/review logic that could later support Apple Local Assist / Foundation Models output.
- Do not implement Foundation Models-backed behavior in v0.8. Review Desk's full usefulness is expected in v0.11+ after the v0.10 Apple Local Assist experiment can generate selected-text or document-excerpt candidates that route through Review Desk or Diff for explicit user review.

v0.6 bug-check / planning-doc consolidation on 2026-06-01:

- Split export behavior into `src/hooks/useDocumentExport.ts`, persisted preferences/theme/Agent Workbench settings into `src/hooks/useAppPreferences.ts`, auto-backup scheduling into `src/hooks/useAutoBackup.ts`, Quick Open visibility into `src/hooks/useQuickOpenState.ts`, active editor commands into `src/hooks/useEditorCommands.ts`, file opening/new-file behavior into `src/hooks/useFileOpening.ts`, tab/window close flow into `src/hooks/useTabCloseFlow.ts`, recovery actions into `src/hooks/useRecoveryActions.ts`, compare setup actions into `src/hooks/useCompareSetupActions.ts`, tab navigation into `src/hooks/useTabNavigation.ts`, workspace restore into `src/hooks/useWorkspaceRestore.ts`, workspace opening into `src/hooks/useWorkspaceOpening.ts`, workspace tree loading into `src/hooks/useWorkspaceTreeLoader.ts`, Go to Line state/key handling into `src/hooks/useGoToLine.ts`, image preview state/open-close handling into `src/hooks/useImagePreview.ts`, editor tab and active-draft derived state into `src/hooks/useEditorTabState.ts`, side pane visibility into `src/hooks/useSidePaneState.ts`, side pane resizing into `src/hooks/useSidePaneResize.ts`, side pane toggles into `src/hooks/useSidePaneToggles.ts`, preview scroll sync into `src/hooks/usePreviewScrollSync.ts`, Agent output buffering into `src/hooks/useAgentOutputBuffer.ts`, Agent UI refresh suspension into `src/hooks/useAgentUiRefreshGate.ts`, opened files listener into `src/hooks/useOpenedFilesListener.ts`, window drag-drop listener into `src/hooks/useWindowDragDrop.ts`, app menu action listener into `src/hooks/useAppMenuActionListener.ts`, app activity listeners into `src/hooks/useAppActivityListeners.ts`, window close confirmation into `src/hooks/useWindowCloseConfirmation.ts`, workspace context-menu state into `src/hooks/useWorkspaceContextMenu.ts`, workspace context-menu dismissal into `src/hooks/useWorkspaceContextMenuDismissal.ts`, latest-value refs for App handlers/state and auto-backup state into `src/hooks/useLatestValueRef.ts`, preview cleanup into `src/hooks/usePreviewCleanup.ts`, Agent output sequence cursor into `src/hooks/useAgentOutputSeqCursor.ts`, active-tab external-change checks into `src/hooks/useExternalChangeChecks.ts`, Agent Workbench session sync into `src/hooks/useAgentWorkbenchSessionSync.ts`, draft persistence into `src/hooks/useDraftPersistence.ts`, tab reordering into `src/hooks/useTabReorder.ts`, workspace tree replacement into `src/workspaceTree.ts`, workspace state persistence into `src/hooks/useWorkspaceStatePersistence.ts`, clipboard writing into `src/clipboard.ts`, recent entries state/persistence into `src/hooks/useRecentEntries.ts`, window title sync into `src/hooks/useWindowTitle.ts`, native app menu state sync into `src/hooks/useAppMenuStateSync.ts`, theme menu state sync into `src/hooks/useThemeMenuStateSync.ts`, editor tab utility helpers into `src/editorTabs.ts`, numeric clamp reuse through `src/utils.ts`, status message localization into `src/statusMessages.ts`, keyboard/IME and global shortcut guards into `src/keyboard.ts`, global keyboard shortcuts into `src/hooks/useGlobalKeyboardShortcuts.ts`, Agent session comparison helpers into `src/agentSession.ts`, document status calculation into `src/hooks/useDocumentStatus.ts`, Markdown outline calculation into `src/hooks/useDocumentOutline.ts`, Markdown local-link resolution into `src/markdownLinks.ts`, localStorage persistence helpers into `src/storage.ts`, modal focus-trap helper into `src/focusTrap.ts`, dialog initial focus into `src/hooks/useDialogInitialFocus.ts`, modal keyboard guard into `src/hooks/useModalKeyboardGuard.ts`, Find/Replace match calculation into `src/hooks/useFindMatches.ts`, Find match index sync into `src/hooks/useFindMatchIndexSync.ts`, Find/Replace actions into `src/hooks/useFindReplaceActions.ts`, diff calculation into `src/diff.ts`, the tab row view into `src/components/TabBar.tsx`, document metadata controls into `src/components/DocumentMetaBar.tsx`, recovery message banners into `src/components/RecoveryMessages.tsx`, workspace sidebar into `src/components/WorkspaceSidebar.tsx`, editor main pane into `src/components/EditorMainPane.tsx`, pane resizer into `src/components/PaneResizer.tsx`, right side pane into `src/components/SidePane.tsx`, dirty-close dialogs into `src/components/CloseDialogs.tsx`, status bar into `src/components/StatusBar.tsx`, Preferences dialog shell into `src/components/PreferencesDialog.tsx`, normal settings pane into `src/components/SettingsPreferencesPane.tsx`, Agent Workbench settings pane into `src/components/AgentWorkbenchPreferencesPane.tsx`, and the Find/Replace row view into `src/components/FindReplaceBar.tsx`, reducing App.tsx from 6039 to 1954 lines without adding Context/zustand or changing the Agent boundary.
- Continued the App.tsx split by moving compare execution/review actions into `src/hooks/useCompareExecution.ts`; App.tsx is now 1793 lines, with compare setup still handled separately in `src/hooks/useCompareSetupActions.ts`.
- Continued the App.tsx split by moving save/save-as actions into `src/hooks/useSaveActions.ts`; App.tsx is now 1650 lines.
- Continued the App.tsx split by moving external-change action handling into `src/hooks/useExternalChangeActions.ts`; App.tsx is now 1545 lines.
- Continued the App.tsx split by moving editor content-change, Markdown insert, and heading-jump actions into `src/hooks/useEditorCommands.ts`; App.tsx is now 1517 lines.
- Continued the App.tsx split by moving window/dialog actions into `src/hooks/useWindowDialogActions.ts`; App.tsx is now 1491 lines.
- Continued the App.tsx split by moving pasted-image save handling into `src/hooks/usePastedImageAction.ts`; App.tsx is now 1472 lines.
- Continued the App.tsx split by moving Agent Workbench Preferences action handlers into `src/hooks/useAgentWorkbenchPreferenceActions.ts`; App.tsx is now 1402 lines.
- Continued the App.tsx split by moving Agent terminal interaction handlers into `src/hooks/useAgentTerminalActions.ts`; App.tsx is now 1351 lines.
- Continued the App.tsx split by moving Agent Workbench session lifecycle actions into `src/hooks/useAgentWorkbenchSessionActions.ts`; App.tsx is now 1151 lines.
- Continued the App.tsx split by moving dirty-close dialogs, Quick Open, Preferences, and workspace context-menu overlay rendering into `src/components/AppOverlays.tsx`; App.tsx is now 1103 lines.
- Continued the App.tsx split by moving the main workspace shell into `src/components/AppWorkspace.tsx`; App.tsx is now 1068 lines.
- Continued the App.tsx split by moving the top tab/document chrome shell into `src/components/AppTopChrome.tsx`; App.tsx is now 1061 lines.
- Continued the App.tsx split by moving the Find/Replace and recovery message shell into `src/components/AppDocumentFeedback.tsx`; App.tsx is now 1056 lines.
- Continued the App.tsx split by moving the native menu action bundle ref into `src/hooks/useAppMenuActionsRef.ts`; App.tsx is now 1039 lines.
- Continued the App.tsx split by moving localized app copy and Agent mode badge derivation into `src/hooks/useLocalizedAppCopy.ts`; App.tsx is now 1033 lines.
- Continued the App.tsx split by moving status bar localization and Agent label rendering into `src/components/AppStatusBar.tsx`; App.tsx is now 1028 lines.
- Continued the App.tsx split by moving dialog/window-close refs and modal-open derivation into `src/hooks/useAppDialogRefs.ts`; App.tsx is now 1034 lines.
- Continued the App.tsx split by moving Find/Replace panel state and match derivation into `src/hooks/useFindReplaceState.ts`; App.tsx is now 1033 lines.
- Continued the App.tsx split by moving editor/find/preview/tabs refs into `src/hooks/useAppEditorRefs.ts`; App.tsx is now 1031 lines.
- Continued the App.tsx split by moving compare view state into `src/hooks/useCompareState.ts`; App.tsx is now 1035 lines.
- Continued the App.tsx split by moving workspace shell state into `src/hooks/useWorkspaceShellState.ts`; App.tsx is now 1037 lines.
- Continued the App.tsx split by moving editor selection state into `src/hooks/useEditorSelectionState.ts`; App.tsx is now 1032 lines.
- Continued the App.tsx split by moving dialog state into `src/hooks/useAppDialogState.ts`; App.tsx is now 1034 lines.
- Continued the App.tsx split by moving view mode state into `src/hooks/useAppViewState.ts`; App.tsx is now 1034 lines.
- Continued the App.tsx split by moving draft recovery state into `src/hooks/useDraftRecoveryState.ts`; App.tsx is now 1034 lines.
- Continued the App.tsx split by moving app feedback state into `src/hooks/useAppFeedbackState.ts`; App.tsx is now 1035 lines.
- Continued the App.tsx split by moving editor tabs state into `src/hooks/useEditorTabsState.ts`; App.tsx is now 1034 lines.
- Continued the App.tsx split by moving Agent Workbench runtime state into `src/hooks/useAgentWorkbenchRuntimeState.ts`; App.tsx is now 1028 lines.
- Continued the App.tsx split by moving active Agent session derivation into `src/hooks/useAgentWorkbenchRuntimeState.ts`; App.tsx is now 1024 lines.
- Continued the App.tsx split by moving active document identity derivation into `src/hooks/useActiveDocumentIdentity.ts`; App.tsx is now 1029 lines.
- Continued the App.tsx split by moving pending close-tab dialog open-state derivation into `src/hooks/useAppDialogRefs.ts`; App.tsx is now 1030 lines.
- Continued the App.tsx split by moving active document presence derivation into `src/hooks/useActiveDocumentIdentity.ts`; App.tsx is now 1035 lines.
- Continued the App.tsx split by moving Find/Replace match-count derivation into `src/hooks/useFindReplaceState.ts`; App.tsx is now 1036 lines.
- Continued the App.tsx split by moving the Insert Table editor command into `src/hooks/useEditorCommands.ts`; App.tsx is now 1037 lines.
- Continued the App.tsx split by moving Compare review UI callbacks into `src/hooks/useCompareExecution.ts`; App.tsx is now 1035 lines.
- Continued the App.tsx split by moving Agent Workbench session UI callbacks into `src/hooks/useAgentWorkbenchSessionActions.ts`; App.tsx is now 1035 lines.
- Continued the App.tsx split by moving active document surface derivation into `src/hooks/useActiveDocumentSurface.ts`; App.tsx is now 1021 lines.
- Continued the App.tsx split by moving side pane state/resize/toggle orchestration into `src/hooks/useSidePaneController.ts`; App.tsx is now 1009 lines.
- Continued the App.tsx split by moving app shell/menu/title/theme/workspace persistence sync into `src/hooks/useAppShellSync.ts`; App.tsx is now 989 lines.
- Continued the App.tsx split by moving workspace restore/opened-files/drag-drop/context-menu effects into `src/hooks/useWorkspaceRuntimeEffects.ts`; App.tsx is now 973 lines.
- Continued the App.tsx split by moving keyboard/focus effects into `src/hooks/useAppKeyboardFocusEffects.ts`; App.tsx is now 952 lines.
- Continued the App.tsx split by grouping Find/Replace state/actions/index sync in `src/hooks/useFindReplaceController.ts`; App.tsx is now 933 lines.
- Continued the App.tsx split by grouping app activity, Agent session sync, app shell sync, workspace runtime, and keyboard/focus effect hooks in `src/hooks/useAppRuntimeEffects.ts`; App.tsx is now 927 lines.
- Continued the App.tsx split by grouping native menu action refs/listener wiring in `src/hooks/useAppMenuIntegration.ts`; App.tsx is now 926 lines.
- Continued the App.tsx split by grouping document export actions and auto-backup scheduling in `src/hooks/useDocumentPersistence.ts`; App.tsx is now 922 lines.
- Continued the App.tsx split by grouping workspace tree loading, workspace opening, and file opening in `src/hooks/useWorkspaceFileOpening.ts`; App.tsx is now 910 lines.
- Continued the App.tsx split by grouping tab close, recovery, and external-change actions in `src/hooks/useDocumentSafetyActions.ts`; App.tsx is now 893 lines.
- Continued the App.tsx split by grouping compare setup and execution actions in `src/hooks/useCompareController.ts`; App.tsx is now 879 lines.
- Continued the App.tsx split by grouping save/save-as, export, and auto-backup wiring in `src/hooks/useDocumentIoController.ts`; App.tsx is now 869 lines.
- Continued the App.tsx split by grouping tab navigation and pointer reordering wiring in `src/hooks/useTabBarController.ts`; App.tsx is now 867 lines.
- Aligned npm, Tauri, and Cargo version surfaces to `0.6.0`, renamed app-facing/package identity to `hazakura editor` / `hazakura-editor`, drafted `docs/releases/0.6.0-warning-expected-dmg-preview.release.md`, and updated the v0.6 release-review brief.
- Built local warning-expected DMG preview `hazakura-editor_0.6.0_aarch64-warning-expected.dmg`; SHA-256 is `91ff6cca4c731f965e5d80459c68c0309a7eacc9b64afcb4157e312efd041dd3`.
- Local v0.6 release-prep gates passed after the rename/version bump: `npm run typecheck`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build`, `SKIP_BUILD=1 npm run build:dmg-preview`, `shasum -c`, app bundle metadata inspection, mounted-DMG app metadata inspection, and `codesign --verify --deep --strict --verbose=2`.
- Post-push dependency audit on 2026-06-01 passed with `npm audit --json` reporting 0 vulnerabilities and `cargo audit --file src-tauri/Cargo.lock --json` reporting 0 vulnerabilities. GitHub Dependabot alert #1 is open for `GHSA-wrw7-89jp-8q8g` / `glib 0.18.5` in `src-tauri/Cargo.lock`; this matches the known RustSec informational `glib::VariantStrIter` unsoundness warning from the transitive Tauri/wry GTK3 stack and is not treated as a macOS warning-expected preview blocker. The alert should remain visible for future Tauri dependency-refresh review.
- `spctl -a -vv -t open "src-tauri/target/release/bundle/macos/hazakura editor.app"` rejected the app with `source=Insufficient Context`, which is expected for this ad-hoc signed, not-notarized preview.
- GitHub prerelease `v0.6.0` was published at `https://github.com/lero003/hazakura-editor/releases/tag/v0.6.0`; older public tags remain immutable. Remote verification downloaded the published GitHub Release assets, confirmed `shasum -c` passed, `hdiutil verify` passed, and the mounted app reported version `0.6.0` / bundle identifier `lab.hazakura.note` / product name `hazakura editor` / executable `hazakura-editor` with `codesign --verify --deep --strict --verbose=2` passing.
- Added a Rust regression test that saving the same pasted image twice returns the same `assets/<hash>.png` path and leaves one asset file.
- Fixed Replace one / Replace all so they use the current search matches and allow empty replacement text.
- Hardened auto-backup path handling on both frontend and Rust command boundaries; backup paths must remain under `.hazakura/backups/` for the selected workspace and must not pass through symlinked backup directories. Rust tests now cover valid backup read/write and rejection of absolute, parent-directory, separator-bearing, and symlinked backup paths.
- Fixed tab drag-and-drop visual state so the dragged and drop-target tabs render their CSS states during interaction.
- Reworked tab reordering to use pointer drag over tab positions instead of native HTML drag/drop, avoiding WebView drop-handler inconsistencies.
- Added an Auto-backup toggle to Preferences > Application, persisted with the existing editor settings.
- Fixed auto-backup scheduling so the 30-second interval is not restarted on every edit; each tick reads the latest open tabs and workspace path before saving dirty tabs.
- Extended Agent provider discovery to include common toolchain manager directories and MacPorts, and now returns the exact effective searched paths in the provider-not-found preflight result.
- Prevented Cmd+Shift+Enter selected-text Agent send from falling through to editor key handling.
- Moved post-v0.5 planning material under `docs/`, marked internal/low-authority memos, and folded the v0.8 Markdown toolbar/UI direction into `docs/roadmap.md`.
- `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build:vite`, `npm run build`, and `git diff --check` passed. Vite still emits the existing large chunk warning during build.

Earlier checks:

```bash
npm run build:vite
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo test --manifest-path src-tauri/Cargo.toml
npm run build
git diff --check
codesign --verify --deep --strict --verbose=2 "src-tauri/target/release/bundle/macos/hazakura editor.app"
```

Print and workspace image preview fix on 2026-05-31:

- `Print to PDF` now prints only a standalone rendered Markdown HTML document, not the full editor workspace UI; workspace asset images are inlined before opening the temporary print HTML.
- Workspace Markdown images under `assets/...`, `./assets/...`, and `/assets/...` are resolved through the existing `open_workspace_image` validation path and then inlined as `data:` URIs for preview/export/print instead of relying on broad Tauri asset protocol scope.
- `npm run typecheck`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build`, `git diff --check`, and app bundle `codesign --verify --deep --strict --verbose=2` passed. Native print-dialog and image-preview behavior still need app-level smoke because build checks cannot open the macOS dialog.

Markdown Authoring Feature Readiness audit on 2026-05-31:

- Clipboard image paste has a backend save path and editor insertion path; duplicate pasted-image saves reuse the existing hash-named asset, and `assets/...` references are now allowed for Markdown preview/export, but still need release-facing smoke coverage with actual pasted images.
- Drag-and-drop currently opens supported text files through the app-level file-open path; it does not ingest dropped images into `assets/` or insert Markdown image syntax.
- HTML export and Print to PDF menu entries exist, and workspace asset images are inlined for standalone output. HTML export is not yet preview-identical, and PDF remains a system print flow rather than an app-owned PDF file pipeline.
- Seasonal ambient effects now have an adjustable intensity preference, and WebView spellcheck still needs release-facing smoke coverage before stronger platform claims.
- Table support is currently fixed 3-column Markdown insertion plus preview table styling, not WYSIWYG-style row/column/alignment editing.
- Agent Workbench can send file paths to a running allowlisted provider, but selection-range summarize/proofread/translate actions and candidate-to-diff apply flow are not implemented.
- `docs/authoring-feature-readiness.md`, `docs/next-goals.md`, `docs/roadmap.md`, and `docs/development-automation.md` now identify the next safe slices without adding new product behavior.

v0.5 Pi provider allowlist slice on 2026-05-30:

- Added `pi` to the existing Agent Workbench UI/backend provider allowlist as a local CLI provider only.
- Rust coverage now confirms `codex`, `opencode`, and `pi` are accepted by allowlisted lookup while non-allowlisted commands remain rejected.
- `command -v pi` returned no local provider path in the automation environment, so real Pi trusted-workspace smoke remains provider-install dependent and was not claimed.
- `npm run build:vite`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build`, and `git diff --check` passed.

pre0.2 warning-expected DMG preview on 2026-05-29:

- Version surfaces were aligned to `0.2.0-pre.0` in `package.json`, `package-lock.json`, `src-tauri/tauri.conf.json`, and `src-tauri/Cargo.toml`.
- GitHub prerelease published at `https://github.com/lero003/hazakura-note/releases/tag/v0.2.0-pre.0`.
- Public release tag `v0.2.0-pre.0` points at commit `5a799be6b0b6f54d913e97883c00f9cca519d9ec`; older public tags were left immutable.
- Built DMG asset: `hazakura-note_0.2.0-pre.0_aarch64-warning-expected.dmg`.
- SHA-256: `8061361528c771e2f7864ce379033154e5a214e8747ec401438e9587007afe39`.
- Screenshot assets were added to README via `docs/images/pre0.2-normal-mode.png` and `docs/images/pre0.2-agent-mode.png`.
- `npm ci` passed with 0 npm vulnerabilities.
- `npm run build:vite` passed with the existing Vite chunk-size warning.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 69 Rust tests.
- `npm audit --audit-level=high` passed with 0 vulnerabilities.
- `cargo audit` completed with the known Tauri/wry transitive warnings for GTK3/gtk-rs, `glib`, `proc-macro-error`, and `unic-*`; no new high or critical macOS release blocker was identified for this warning-expected preview.
- `npm run build:dmg-preview` passed, including app build, ad-hoc signing, `hdiutil verify`, and basename checksum generation.
- `cd src-tauri/target/release/bundle/dmg && shasum -c hazakura-note_0.2.0-pre.0_aarch64-warning-expected.dmg.sha256` passed.
- The DMG mounted read-only; the contained `hazakura-note.app` reported `CFBundleShortVersionString` `0.2.0-pre.0`, bundle identifier `lab.hazakura.note`, and passed `codesign --verify --deep --strict --verbose=2`.
- The built app launched locally from `src-tauri/target/release/bundle/macos/hazakura-note.app` and was then quit.
- `spctl -a -vv -t open` rejected the app with `source=Insufficient Context`, which is expected for this ad-hoc signed, not-notarized preview lane.
- Remote verification downloaded the published GitHub Release assets, confirmed `shasum -c` passed, `hdiutil verify` passed, and the mounted app reported version `0.2.0-pre.0` / bundle identifier `lab.hazakura.note` with `codesign --verify --deep --strict --verbose=2` passing.
- The release assets are only the DMG and `.sha256`; normal-mode and Agent-mode screenshots are stored in the repository README image paths instead of as release assets.

pre0.2 follow-up warning-expected DMG preview on 2026-05-29:

- Version surfaces were aligned to `0.2.0-pre.1` in `package.json`, `package-lock.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`, and `src-tauri/Cargo.lock`.
- GitHub prerelease published at `https://github.com/lero003/hazakura-note/releases/tag/v0.2.0-pre.1`.
- Public release tag `v0.2.0-pre.1` is the fresh tag for this follow-up release; older public tags were left immutable.
- Built DMG asset: `hazakura-note_0.2.0-pre.1_aarch64-warning-expected.dmg`.
- SHA-256: `812431095aa0a1d8449f1ff06efbe82aab10efdaaf5a0264f0cfc55410fbfce7`.
- This follow-up adds Finder/app-icon document-open support for common UTF-8 text files, including `.json`, using the same Rust text-open validation as File > Open.
- `npm run build:vite` passed with the existing Vite chunk-size warning.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 70 Rust tests.
- `npm audit --audit-level=high` passed with 0 vulnerabilities.
- `cargo audit --file src-tauri/Cargo.lock` completed with the known Tauri/wry transitive warnings for GTK3/gtk-rs, `glib`, `proc-macro-error`, and `unic-*`; no new high or critical macOS release blocker was identified for this warning-expected preview.
- `npm run build:dmg-preview` passed, including app build, ad-hoc signing, `hdiutil verify`, and basename checksum generation.
- `cd src-tauri/target/release/bundle/dmg && shasum -c hazakura-note_0.2.0-pre.1_aarch64-warning-expected.dmg.sha256` passed.
- The DMG mounted read-only; the contained `hazakura-note.app` reported `CFBundleShortVersionString` `0.2.0-pre.1`, bundle identifier `lab.hazakura.note`, and passed `codesign --verify --deep --strict --verbose=2`.
- A `.json` file opened through the mounted app with macOS open-file handling landed in a normal clean editor tab, and the app was quit after the smoke with no `hazakura-note` process left running.
- `spctl -a -vv -t open` rejected the app with `source=Insufficient Context`, which is expected for this ad-hoc signed, not-notarized preview lane.
- Remote verification downloaded the published GitHub Release assets, confirmed `shasum -c` passed, `hdiutil verify` passed, and the mounted app reported version `0.2.0-pre.1` / bundle identifier `lab.hazakura.note` with `codesign --verify --deep --strict --verbose=2` passing.

v0.2 warning-expected DMG preview release on 2026-05-29:

- Version surfaces were aligned to `0.2.0` in `package.json`, `package-lock.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`, and `src-tauri/Cargo.lock`.
- Release notes were prepared at `docs/releases/0.2.0-warning-expected-dmg-preview.release.md`.
- GitHub prerelease published at `https://github.com/lero003/hazakura-note/releases/tag/v0.2.0`.
- Public release tag `v0.2.0` is the fresh tag for this release; older public tags were left immutable.
- Roadmap priority moved `Safe Editor Non-Git Diff And Review` to v0.3 and placed `Markdown Review Navigation` at v0.4.
- At the v0.2 release, recurring automation guidance used the `hazakura-note-safe-editor-review-loop` lane and prioritized v0.3 diff/review preparation before Agent Workbench maintenance.
- Built DMG asset: `hazakura-note_0.2.0_aarch64-warning-expected.dmg`.
- SHA-256: `6a39cc2e10b2caeed0328f8a217cc98ee40c3de5ff78751a5736a9667964d67d`.
- `npm ci` passed.
- `npm run build:vite` passed with the existing Vite chunk-size warning.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 70 Rust tests.
- `npm audit --audit-level=high` passed with 0 vulnerabilities.
- `cargo audit --file src-tauri/Cargo.lock` completed with the known Tauri/wry transitive warnings for GTK3/gtk-rs, `glib`, `proc-macro-error`, and `unic-*`; no new high or critical macOS release blocker was identified for this warning-expected preview.
- `npm run build:dmg-preview` passed, including app build, ad-hoc signing, `hdiutil verify`, and basename checksum generation.
- `cd src-tauri/target/release/bundle/dmg && shasum -c hazakura-note_0.2.0_aarch64-warning-expected.dmg.sha256` passed.
- The DMG mounted read-only; the contained `hazakura-note.app` reported `CFBundleShortVersionString` `0.2.0`, bundle identifier `lab.hazakura.note`, and passed `codesign --verify --deep --strict --verbose=2`.
- The built app launched locally from `src-tauri/target/release/bundle/macos/hazakura-note.app` and was then quit with no `hazakura-note` process left running.
- `spctl -a -vv -t open` rejected the app with `source=Insufficient Context`, which is expected for this ad-hoc signed, not-notarized preview lane.
- Remote verification downloaded the published GitHub Release assets, confirmed `shasum -c` passed, `hdiutil verify` passed, and the mounted app reported version `0.2.0` / bundle identifier `lab.hazakura.note` with `codesign --verify --deep --strict --verbose=2` passing.

Agent Workbench Trusted Provider Smoke Readiness on 2026-05-29:

- `docs/smoke-checklist.md` now reflects the compact xterm Agent pane and includes separate codex/opencode result fields for trusted-workspace manual smoke.
- The checklist explicitly records provider-not-found / not-run outcomes instead of requiring provider installation during smoke.
- The optional external-change smoke path now states that hazakura does not auto-apply, auto-commit, or make Git decisions for provider-made changes; existing external-change/conflict handling remains the safe-editor receiving path.
- Agent pane launch messages now make provider-not-found, selected-workspace launch, and rejected launch states easier to read during manual smoke.
- No new Agent Workbench capabilities were added; multiple sessions, session restore, arbitrary shell/command/path UI, auto-apply, auto-commit, Git integration, and provider-add UI remain out of scope.

Agent Workbench TUI Responsiveness Stabilization on 2026-05-29:

- Trusted-provider smoke feedback reported that OpenCode CLI launched, could edit a local file through OpenCode Zen/free model, and rendered in the xterm pane, but text input in the OpenCode TUI felt very slow.
- Terminal input writes now return success/failure only instead of returning a full session/output snapshot for every keypress. Active-session output refresh remains handled by the session polling path.
- Active Agent session polling now runs every 100 ms instead of every 1000 ms so TUI output echo/render updates reach xterm sooner.
- Rust coverage now includes a fake-provider input-burst test to exercise repeated small writes without adding arbitrary shell, command, path, multi-session, restore, auto-apply, auto-commit, or Git behavior.
- Follow-up user smoke reported the basic input speed became comfortable. Real `codex` / `opencode` provider behavior still remains trusted-workspace manual smoke, not automated provider-internal approval.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 69 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- `git diff --check` passed.

Safe Editor Non-Git Change Review readiness on 2026-05-30:

- Diff now behaves as a dedicated workbench mode rather than only a right-side result pane: the Diff toggle is always present, opening it hides the center editor and shows either setup guidance or a GitHub-PR-like split diff with left/right line numbers and added/removed cells.
- The context-menu comparison flow prefers the open center file as the comparison source, so a user can open file A, right-click file B, and compare A against B without first setting a separate compare source.
- The explicit compare-source flow now uses two visible Diff setup slots: click or right-click a workspace text file as the source, choose another as the target, then press Compare. Drag selection is a non-goal for this flow; click selection keeps the source fixed until the user clears it.
- The comparison pane also has a change-review mode for active editor buffer versus disk, recoverable draft versus disk, and external-change conflict local edits versus disk.
- Draft and external-change recovery banners now offer Review changes / 変更を確認 before restore, reopen, close-without-saving, or keep-editing choices.
- The active dirty tab chrome also exposes Review changes / 変更を確認 for current buffer versus disk review.
- The comparison surface remains file/workspace/change-review oriented and still does not inspect Git status, branches, staging, commits, history, or repository state.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 70 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- `npm audit --audit-level=high` passed with 0 vulnerabilities.
- `cargo audit --file src-tauri/Cargo.lock` completed with the known Tauri/wry transitive warnings for GTK3/gtk-rs, `glib`, `proc-macro-error`, and `unic-*`; no new high or critical blocker was identified in this pass.
- `git diff --check` passed.

Dependency audit triage on 2026-05-30:

- GitHub Dependabot alert #1 / `GHSA-wrw7-89jp-8q8g` remains open for `glib 0.18.5` in `src-tauri/Cargo.lock`.
- `npm audit --audit-level=moderate` passed with 0 vulnerabilities.
- `cargo tree --manifest-path src-tauri/Cargo.toml --target all -i glib` showed `glib` only through the Linux Tauri/wry GTK/WebKit path: `tauri 2.11.2` / `wry 0.55.1` / `gtk 0.18.x`.
- `cargo tree --manifest-path src-tauri/Cargo.toml --target aarch64-apple-darwin -i glib` and `--target x86_64-apple-darwin -i glib` showed no `glib` dependency path.
- `cargo update --manifest-path src-tauri/Cargo.toml -p glib --precise 0.20.0 --dry-run` cannot resolve because `gtk 0.18.2` requires `glib ^0.18`; compatible `tauri` / `wry` / `gtk` dry-run updates did not offer a patched path.
- This is not treated as a v0.3 macOS blocker. Recheck before Linux support, before a Tauri/wry dependency refresh lane, before distribution-readiness dependency sign-off, or sooner if the alert severity increases or a compatible patched upstream path becomes available.

v0.3 warning-expected DMG preview release preparation on 2026-05-30:

- Version surfaces were aligned to `0.3.0` in `package.json`, `package-lock.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`, and `src-tauri/Cargo.lock`.
- Release notes were prepared at `docs/releases/0.3.0-warning-expected-dmg-preview.release.md`.
- Built DMG asset: `hazakura-note_0.3.0_aarch64-warning-expected.dmg`.
- SHA-256: `7cc28ae82b5d913382eaf25a90de3eaa3155141200e3d3f3849cc68e50005f10`.
- `npm ci` passed.
- `npm run build:vite` passed with the known production chunk-size warning.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 70 Rust tests.
- `npm audit --audit-level=moderate` passed with 0 vulnerabilities.
- `cargo audit --file src-tauri/Cargo.lock` completed with the known Tauri/wry transitive warnings for GTK3/gtk-rs, `glib`, `proc-macro-error`, and `unic-*`; no new macOS release blocker was identified in this pass.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- `SKIP_BUILD=1 npm run build:dmg-preview` created and verified the warning-expected DMG plus checksum.
- `cd src-tauri/target/release/bundle/dmg && shasum -c hazakura-note_0.3.0_aarch64-warning-expected.dmg.sha256` passed.
- The DMG mounted read-only; the contained `hazakura-note.app` reported `CFBundleShortVersionString` `0.3.0`, `CFBundleVersion` `0.3.0`, bundle identifier `lab.hazakura.note`, and passed `codesign --verify --deep --strict --verbose=2`.
- `spctl -a -vv -t open` rejected the app with `source=Insufficient Context`, which is expected for this ad-hoc signed and not-notarized preview.
- GitHub prerelease published at `https://github.com/lero003/hazakura-note/releases/tag/v0.3.0`.
- Public release tag `v0.3.0` is the fresh tag for this release; older public tags were left immutable.
- Remote verification downloaded the published GitHub Release assets, confirmed `shasum -c` passed, `hdiutil verify` passed, and the mounted app reported version `0.3.0` / bundle identifier `lab.hazakura.note` with `codesign --verify --deep --strict --verbose=2` passing.
- Recurring automation guidance moved to the v0.4 Markdown Review Navigation lane while keeping v0.3 Diff / Change Review work as patch follow-up only.

v0.4 warning-expected DMG preview release preparation on 2026-05-30:

- Version surfaces were aligned to `0.4.0` in `package.json`, `package-lock.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`, and `src-tauri/Cargo.lock`.
- Release notes were prepared at `docs/releases/0.4.0-warning-expected-dmg-preview.release.md`.
- Built DMG asset: `hazakura-note_0.4.0_aarch64-warning-expected.dmg`.
- SHA-256: `920b05ca220c5feaa003e69f8362b654f24cee73df63e49cbb7f14f4d0fd73b8`.
- `npm ci` passed.
- `npm run build:vite` passed with the known production chunk-size warning.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 70 Rust tests.
- `npm audit --audit-level=moderate` passed with 0 vulnerabilities.
- `cargo audit --file src-tauri/Cargo.lock` completed with the known Tauri/wry transitive warnings for GTK3/gtk-rs, `glib`, `proc-macro-error`, and `unic-*`; no new macOS release blocker was identified in this pass.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- The built app reported `CFBundleShortVersionString` `0.4.0`, `CFBundleVersion` `0.4.0`, bundle identifier `lab.hazakura.note`, passed `codesign --verify --deep --strict --verbose=2`, launched locally from `src-tauri/target/release/bundle/macos/hazakura-note.app`, and was then quit with no `hazakura-note` process left running.
- `SKIP_BUILD=1 npm run build:dmg-preview` created and verified the warning-expected DMG plus checksum.
- `cd src-tauri/target/release/bundle/dmg && shasum -c hazakura-note_0.4.0_aarch64-warning-expected.dmg.sha256` passed.
- The DMG mounted read-only; the contained `hazakura-note.app` reported `CFBundleShortVersionString` `0.4.0`, `CFBundleVersion` `0.4.0`, bundle identifier `lab.hazakura.note`, and passed `codesign --verify --deep --strict --verbose=2`.
- `spctl -a -vv -t open` rejected the app with `source=Insufficient Context`, which is expected for this ad-hoc signed and not-notarized preview.
- GitHub prerelease published at `https://github.com/lero003/hazakura-note/releases/tag/v0.4.0`.
- Public release tag `v0.4.0` is the fresh tag for this release; older public tags were left immutable.
- Remote verification downloaded the published GitHub Release assets, confirmed `shasum -c` passed, `hdiutil verify` passed, and the mounted app reported version `0.4.0` / bundle identifier `lab.hazakura.note` with `codesign --verify --deep --strict --verbose=2` passing.
- v0.5 planning can now focus on Pi CLI provider and app stability while keeping Safe Editor Mode primary and release/signing automation separate.

Cherry blossom app icon update on 2026-05-29:

- `src-tauri/icons/icon.png` and `src-tauri/icons/icon.icns` now use a centered `🌸` emoji mark on a soft pink rounded base.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- The built app `Info.plist` still references `icon.icns`, and the bundle resource contains the regenerated `icon.icns`.
- `git diff --check` passed.

Start panel layout polish on 2026-05-29:

- The no-file start panel now stacks recent files below the primary start actions, giving the Japanese heading enough room in the common editor-plus-preview layout.
- Built-app smoke inspected the current app surface and confirmed Safe Editor startup still shows no file open, the workspace browser, Preview/Agent tabs, and the start actions with the heading on one line.
- `npm run build` passed and regenerated the local macOS `.app` bundle.

Agent Workbench Workspace Path Handoff on 2026-05-29:

- Workspace file rows now support right-click Copy full path and Show in Finder / Finderで表示 for locating the selected item without copying file contents.
- Workspace file rows also provide a text/plain drag payload containing the file full path, plus a hazakura-specific path payload, so drag/drop can be used as a path handoff where supported.
- While one Agent session is running, the workspace file context menu can send that selected file's full path to the Agent as plain terminal input. It does not append a command or newline.
- This does not add arbitrary path input UI, arbitrary shell/command execution, auto-apply, auto-commit, Git integration, provider-add UI, or file-copy behavior.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 69 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- `git diff --check` passed.

Agent Workbench External Change Live Refresh on 2026-05-29:

- Clean open tabs now reload automatically when the active file changes on disk, updating the central editor view without treating the change as hazakura approval, auto-apply, auto-commit, or a Git decision.
- Dirty tabs still keep the existing external-change recovery path and stop saving until the user chooses Reopen from disk, Close without saving, or Keep editing.
- While an Agent Workbench provider session is running, the active tab is rechecked on a lightweight interval so provider-made changes in a trusted workspace can appear in the editor without requiring a tab switch.
- The conflict banner now frames the source as an outside-hazakura change, including possible Agent provider edits.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 69 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- `git diff --check` passed.

Agent Workbench UI/Menu Separation Polish on 2026-05-29:

- The Agent pane compact header now emphasizes provider, running state, and workspace path. The verbose gate/status strip is hidden during a normal running session and remains available for idle/checking/rejected states.
- Preferences now contains editor/application settings only, including an English/Japanese File/View menu language switch.
- Agent Workbench now opens as a separate dialog for mode gate, provider, session summary, and responsibility-boundary consent.
- Native Preferences and Agent Workbench entries moved out of the View/File menu path and into the macOS app menu; File now remains focused on document/workspace actions and View remains focused on preview/display actions.
- No Agent backend capability, provider allowlist, arbitrary shell/command/path UI, auto-apply, auto-commit, Git integration, session restore, or multi-session behavior was added.

Native app menu placement polish on 2026-05-29:

- Preferences and Agent Workbench now live in the macOS app menu rather than File, matching standard macOS placement while keeping File focused on document and workspace actions.
- Built-app smoke confirmed File exposes file/workspace actions without Preferences or Agent Workbench, and the app-name menu exposes About, Settings, Agent Workbench, Services, Hide, and Quit.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 69 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.

Agent Workbench 30-minute automation guidance on 2026-05-29:

- `docs/development-automation.md` now treats the active non-goal loop as a 30-minute Agent Workbench and safe-editor micro-polish cadence.
- Selection priority is everyday usability, safety-boundary regression checks, stability/responsiveness, Markdown-first safe-editor quality, local release readiness, then verified no-op.
- Repeated automation runs should not add test code merely to produce activity. Tests are reserved for reproduced bugs, backend/safety contracts, Agent lifecycle/gate/output/input/stop/exit/external-change behavior, or high-value fake-provider coverage.
- Release, publish, tag, DMG attachment, dependency/lockfile changes, arbitrary shell/command/path UI, auto-apply, auto-commit, Git integration, provider-add UI, multi-session, and session restore remain out of scope unless explicitly approved.

Agent Workbench output state stabilization on 2026-05-29:

- The frontend now applies Agent output snapshots monotonically by output sequence, so an older async state response from input, refresh, stop, or launch handling cannot roll the terminal output state backward.
- Rust coverage now verifies Agent output sequence numbers strictly increase, continue across a new provider session after provider exit, and remain monotonic when the bounded output buffer prunes older chunks.
- Rust coverage now verifies stop after provider exit is a no-op for the runtime adapter and preserves the exited session/output state.
- Rust coverage now verifies a second stop after an already stopped session is a no-op and does not call the runtime adapter again.
- The explicit reset path is still used when Agent Workbench becomes unavailable, preserving the Safe Editor default boundary.
- `npm run build:vite` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml agent_workbench_start_allows_new_session_after_exit` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml agent_workbench_output_chunks_are_bounded_and_pruned_oldest_first` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml agent_workbench_fake_provider_large_stdout_prunes_oldest_output` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml agent_workbench_stop_after_exit_does_not_call_runtime_adapter` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml agent_workbench_second_stop_after_stopped_session_is_noop` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 68 Rust tests.
- `git diff --check` passed.

Agent Workbench xterm Terminal Surface on 2026-05-28:

- The Agent pane now uses an xterm-based terminal surface instead of the previous simple log view and textarea input.
- PTY output is passed through to xterm so terminal control sequences are rendered by the terminal emulator instead of printed as raw text.
- The Agent pane status area was compacted so the terminal surface gets most of the right pane height.
- xterm fit dimensions are sent to the backend at provider launch and on terminal resize, and the backend applies them to the provider PTY.
- Terminal data from the xterm surface is sent only to the existing running allowlisted provider session and is not stored as output history, so key-by-key input does not consume the bounded provider-output buffer. No arbitrary command input, shell selector, provider-add UI, session restore, auto-apply, auto-commit, or Git integration was added.
- The backend gate, allowlisted provider lookup, single-session lifecycle, runtime stop boundary, bounded output buffer, and no-restore behavior are unchanged.
- `@xterm/xterm` and `@xterm/addon-fit` were added as frontend dependencies.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 66 Rust tests, including invalid/no-session/active/exited terminal resize coverage, real PTY `stty size` verification for launch-time and runtime terminal resize, exited-session input rejection, and new session start after provider exit or user stop.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without Agent Workbench UI visible.
- Real `codex` / `opencode` behavior still needs trusted-workspace manual smoke because provider-internal behavior is outside hazakura-side automation.

Agent Workbench Mode Gate Foundation on 2026-05-28:

- Preferences now stores requested Agent Workbench mode separately from the app-session active mode, so enable/disable changes require restart before taking effect.
- A Rust `start_agent_workbench_session` entry exists only as a gate. It validates active mode, allowlisted provider, and workspace root, then stops with a not-implemented message before any launch.
- No PTY, xterm, TUI rendering, CLI process spawn, auto-apply, Git flow, multiple sessions, or session restore was added.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 28 Rust tests, including disabled-mode rejection, non-allowlisted provider rejection, workspace-root validation, and foundation-build no-launch behavior.
- Browser smoke at `http://127.0.0.1:1421/` confirmed the app loads and Agent Workbench UI is not visible during normal Safe Editor startup.
- `git diff --check` passed.

Agent Workbench Provider/Consent Gate on 2026-05-28:

- Preferences now shows provider selection and responsibility-boundary consent only when Agent Workbench is active for the current app session.
- Provider selection is fixed to the allowlist `codex` / `opencode`; no arbitrary command, shell, path, PTY, xterm, or process-spawn UI was added.
- Consent is stored in `localStorage` and passed to the Rust launch gate.
- The Rust `start_agent_workbench_session` gate now validates active mode, acknowledged consent, allowlisted provider, and workspace root before stopping with the existing not-implemented message.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 30 Rust tests, including disabled-mode rejection, unacknowledged-consent rejection, non-allowlisted provider rejection, invalid-workspace rejection, and foundation-build no-launch behavior.

Agent Workbench Pane Shell on 2026-05-28:

- The right pane now exposes Preview / Agent modes only when Agent Workbench is active for the app session and responsibility-boundary consent is acknowledged.
- The Agent pane shell shows the selected provider, workspace root, consent state, and backend launch-gate state.
- With no selected workspace root, the Agent pane reports launch unavailable and the gate-check button is disabled.
- The gate-check button calls the existing backend launch gate; a passed gate still reports launch not implemented instead of starting a CLI. This was later replaced by structured Host Preflight.
- No arbitrary command input, arbitrary path input, shell selector, PTY, xterm, CLI process launch, multiple sessions, session restore, auto-apply, auto-commit, or Git integration was added.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 30 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without an Agent side-pane switch.

Agent Workbench Host Preflight on 2026-05-28:

- `start_agent_workbench_session` now returns structured preflight state instead of starting a CLI process.
- The preflight validates active-session mode, responsibility-boundary consent, allowlisted provider, and workspace root before checking provider CLI availability.
- Provider CLI discovery is limited to allowlisted `codex` / `opencode` names on `PATH`; non-allowlisted command names are rejected before lookup.
- The Agent pane now shows provider availability. Missing provider CLI is reported as provider not found, while found provider CLI reports preflight passed / launch not implemented.
- No arbitrary command input, arbitrary path input, shell selector, PTY, xterm, CLI process launch, multiple sessions, session restore, auto-apply, auto-commit, or Git integration was added.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 36 Rust tests, including command-level rejection for disabled mode, unacknowledged consent, non-allowlisted providers, invalid workspace roots, and allowlist-only provider lookup.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without an Agent side-pane switch or Agent pane.
- `git diff --check` passed.

Agent Workbench Session Lifecycle Stub on 2026-05-28:

- `start_agent_workbench_session` now creates an in-memory session stub only after preflight passes and the allowlisted provider CLI is found on `PATH`.
- The session stub records provider, workspace root, provider path, `createdAtMs`, and status. It does not start a CLI process.
- Provider not found returns no session stub. Active sessions reject a second start until stopped.
- `stop_agent_workbench_session` marks the current stub stopped without process management, and `get_agent_workbench_session_state` returns the current in-memory stub state or none.
- Session stubs are not persisted, restored, or treated as process state after restart.
- The Agent pane now shows session status and exposes Start session stub / Stop session stub controls.
- No arbitrary command input, arbitrary path input, shell selector, PTY, xterm, CLI process launch, multiple sessions, session restore, auto-apply, auto-commit, or Git integration was added.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 40 Rust tests, including session-stub creation, provider-not-found no-session behavior, second-active-session rejection, stop-to-stopped behavior, and fresh-store no-restore behavior.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without Agent switch, Agent pane, or session-stub controls.
- `git diff --check` passed.

Agent Workbench Runtime Adapter Stub on 2026-05-28:

- Session stub creation now goes through an `AgentRuntimeAdapter` boundary instead of constructing future runtime state directly inside lifecycle code.
- The default adapter is a stub adapter. It receives only allowlisted provider, canonical workspace root, and resolved provider path, then returns a runtime handle with status `stubbed/not_started`.
- Runtime stub handles are stored on the session stub but do not include process IDs, stdio handles, PTY handles, or any real CLI process state.
- Provider-not-found starts do not call the adapter or create a session stub. Adapter failure also creates no session stub. A second active-session start is rejected before calling the adapter again.
- The Agent pane now shows runtime status when a session stub exists.
- No arbitrary command input, arbitrary path input, shell selector, PTY, xterm, CLI process launch, multiple sessions, session restore, auto-apply, auto-commit, or Git integration was added.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 42 Rust tests, including adapter invocation, canonical workspace/provider path handoff, provider-not-found no-adapter behavior, adapter-failure no-session behavior, no second adapter call while active, and no process-resource fields on runtime handles.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without Agent switch, Agent pane, runtime status, or session-stub controls.
- `git diff --check` passed.

Agent Workbench Runtime Stop Adapter Stub on 2026-05-28:

- Session stub stopping now goes through the `AgentRuntimeAdapter` stop boundary before the session is marked stopped.
- The stub stop returns a runtime handle with status `stubbed/stopped` while preserving the same allowlisted provider, canonical workspace root, and resolved provider path.
- Stop-adapter failure leaves the session active with its previous `stubbed/not_started` runtime handle.
- Stopping with no session does not call the runtime adapter.
- No CLI process kill, PTY close, stdio close, arbitrary command input, shell selector, session restore, auto-apply, auto-commit, or Git integration was added.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 44 Rust tests, including runtime stop adapter invocation, stop-handle handoff, stopped runtime status, no-session no-adapter behavior, stop-adapter failure preservation, and no process-resource fields on runtime handles.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without Agent switch, Agent pane, runtime status, or session-stub controls.
- `git diff --check` passed.

Agent Workbench Minimal Real Session Slice on 2026-05-28:

- `start_agent_workbench_session` now uses the runtime adapter to start exactly one allowlisted provider process from the resolved provider path, with `cwd` set to the canonical workspace root.
- Provider stdout/stderr are captured as bounded output chunks and shown in the Agent pane. The pane can send user input to the active provider stdin and stop the session.
- `get_agent_workbench_session_state` refreshes exited process state, and stopped/exited sessions are not persisted or restored after restart.
- The backend still rejects disabled mode, unacknowledged consent, non-allowlisted providers, invalid workspace roots, provider-not-found starts, launch failures, and second active sessions before process launch.
- No arbitrary command field, arbitrary path field, shell selector, multiple sessions, session restore, auto-apply, auto-commit, Git integration, or prompt auto-submit was added.
- PTY/xterm integration is still not implemented; the current real-session slice uses process pipes, so full-screen TUI behavior may be limited.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 46 Rust tests, including real provider process start, stdout capture, stdin input, stop-through-adapter behavior, no-session no-adapter behavior, stop failure preservation, and no arbitrary command handoff through the launch request.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without Agent switch, Agent pane, runtime status, session controls, or Agent input.
- `git diff --check` passed.

Agent Workbench Real Session Stabilization on 2026-05-28:

- The earlier simple Agent output view auto-scrolled and labeled stdout, stderr, input, and system chunks separately; the current xterm surface sends input directly to the provider and keeps output history focused on provider/system output.
- The output buffer is fixed at 500 chunks; when it overflows, older chunks are discarded first.
- Active sessions keep provider changes disabled and warn if the selected workspace changes while the process remains bound to its launch workspace.
- Stopped or exited sessions reject new input, and stdin failures return an error without mutating the stored session state.
- Store drop / app close cleanup kills and waits for any running provider process.
- Existing external-change/conflict handling remains the path for files changed by the launched CLI.
- No PTY, xterm, full-screen TUI support, arbitrary command field, arbitrary path field, shell selector, multiple sessions, session restore, auto-apply, auto-commit, Git integration, or prompt auto-submit was added.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 50 Rust tests, including output pruning, stream distinctions, exited state, stopped-session input rejection, stdin failure preservation, active-session second-start rejection across provider/workspace changes, and drop cleanup.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without Agent switch, Agent pane, runtime status, session controls, Agent input, or output-limit status.
- `git diff --check` passed.

Agent Workbench Automated Stabilization Harness on 2026-05-28:

- Rust-side Agent Workbench tests now use a reusable fake allowlist provider fixture for process-based lifecycle checks.
- Fake providers cover large stdout output pruning, stderr capture, immediate provider exit, abnormal provider exit, stdin/write-failure preservation, long-running stop cleanup, and store-drop cleanup without requiring real `codex` / `opencode` automation.
- The Agent pane output counter now states that the oldest chunks are pruned at the 500-chunk limit, and disabled controls expose minimal state-specific hints.
- Agent Workbench boundary docs now state that automated stabilization is fake-provider centered; real provider checks remain trusted-workspace manual smoke, not automated approval of provider-internal behavior.
- No PTY, xterm, full-screen TUI support, real provider internal automation, arbitrary command field, arbitrary path field, shell selector, multiple sessions, session restore, auto-apply, auto-commit, or Git integration was added.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 53 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Browser smoke at `http://127.0.0.1:1421/` confirmed normal Safe Editor startup loads without Agent Workbench, Agent input, or output-limit status. The known Vite-only Tauri menu-listener warning still appeared outside the Tauri runtime.

Agent Workbench Manual Smoke Readiness on 2026-05-28:

- `docs/smoke-checklist.md` now includes an Agent Workbench Trusted Workspace Manual Smoke section.
- The checklist keeps real `codex` / `opencode` checks scoped to a trusted throwaway workspace and explicitly avoids provider-internal auto-approval.
- The manual smoke path covers Safe Editor default visibility, restart-required mode gate, provider/consent setup, provider-not-found behavior, one real session start, terminal input delivery, stop/exited state, optional external-change observation, close cleanup, and a result record template.
- No real provider manual smoke is claimed by this documentation update.

Agent Workbench Provider Discovery Polish on 2026-05-28:

- Backend preflight now builds an app search path from the app process `PATH`, user bin directories, and common macOS GUI-launch gaps such as Homebrew paths before resolving the allowlisted provider.
- The same app search path is passed to the launched provider process as `PATH`, so provider scripts using shebangs such as `#!/usr/bin/env node` can resolve their runtime when installed through Homebrew or common user-bin locations.
- Provider lookup still accepts only allowlisted `codex` / `opencode` names and does not expose arbitrary command lookup.
- At the time, View menu included an `Agent Workbench...` entry that opened Preferences, making the mode gate easier to find without adding Agent UI to normal Safe Editor startup. This was later superseded by the separate Agent Workbench dialog in the macOS app menu.
- The provider-not-found UI wording now refers to the app search path instead of implying the user's Terminal `PATH`.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 56 Rust tests, including provider process `PATH` propagation for `env node` style launchers.
- `npm run build` passed and regenerated the local macOS `.app` bundle.

Agent Workbench Minimal PTY Runtime on 2026-05-28:

- The real Agent runtime now starts allowlisted providers behind a minimal macOS PTY instead of plain stdin/stdout pipes, so CLIs that require terminal stdin can pass their terminal check.
- User input still goes only to the active allowlisted provider process. The UI remains a simple output/input surface and does not implement xterm or full-screen TUI rendering.
- ANSI and terminal control sequences are stripped from displayed provider output so PTY-driven CLIs do not show raw escape codes in the simple Agent pane.
- Pipe-mode runtime tests remain available for stream-specific fake-provider coverage; the production runtime uses PTY mode.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 58 Rust tests, including a PTY fake-provider check that confirms provider stdin is a terminal, input reaches the provider, and terminal control sequences are stripped from display output.

Pre-release stabilization review intake on 2026-05-28:

- The accepted release boundary is `0.1.0 source-only developer preview`.
- Binary distribution, Developer ID signing, notarization, installer packaging, and auto-update remain out of scope.
- README build guidance now treats `npm ci` as the source-preview path; `npm install` is reserved for intentional dependency updates.
- `docs/source-release-checklist.md` now requires dependency audit checks and latest-HEAD built-app smoke evidence before tag approval.
- `docs/releases/0.1.0.md` is the release-note draft for GitHub Release copy.
- `docs/dmg-preview-checklist.md` records DMG distribution as a future, separate preview lane from the source-only release.
- Docs-only intake verification: `git diff --check` passed.
- No new latest-HEAD built-app smoke is claimed by this intake itself.

Source Release Readiness smoke on 2026-05-27:

- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 16 Rust tests, including external-change conflict rejection, binary-looking file rejection, oversized-file rejection, CRLF preservation, final-newline preservation, Save As, and workspace lazy-listing boundaries.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- Version alignment was confirmed at `0.1.0` across `package.json`, `src-tauri/tauri.conf.json`, and `src-tauri/Cargo.toml`.
- Manual built-app smoke used `/tmp/hazakura-note-release-smoke-20260527202313`.

Native File Menu UI polish on 2026-05-28:

- Top toolbar file-action buttons and the prototype subtitle were removed.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 18 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- The regenerated `.app` launched, and macOS System Events confirmed the native File menu contains New File, Open, Open Folder, Save, and Save As.

Application Completion Polish on 2026-05-28:

- Top toolbar display settings were moved into the native View menu and Preferences dialog.
- File menu Save / Save As now reflect whether the active document can be saved, and File menu Recent Files / Recent Folders are populated from local history.
- The empty editor state now shows a focused start panel instead of editable welcome copy.
- The workspace pane now has a stable workspace header, and the window title follows the active file plus unsaved state.
- `npm run build:vite`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build`, and `git diff --check` passed.
- Built-app checks confirmed the native File menu, View menu, disabled Save state, disabled Save As state with no open tab, dynamic window title for an active restored file, Preferences dialog rendering, and the no-file start panel.

Local Bundle Signature Polish checks on 2026-05-28:

- `src-tauri/tauri.conf.json` now points the bundle at `icons/icon.icns` and uses ad-hoc macOS signing for local build validation.
- `npm run build` passed and regenerated the local macOS `.app` bundle with `Contents/Resources/icon.icns`.
- `codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/hazakura-note.app` passed.
- No Developer ID signing or notarization was added.
- Later built-app smoke on 2026-05-28 successfully inspected native menus through System Events, so the earlier menu-inspection blocker is no longer current.

Local Bundle Launch Metadata Polish checks on 2026-05-28:

- `src-tauri/Info.plist` now explicitly overrides the generated bundle metadata to set `LSRequiresCarbon` to `false`.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- `plutil -p src-tauri/target/release/bundle/macos/hazakura-note.app/Contents/Info.plist` confirmed `LSRequiresCarbon => false` while preserving `CFBundleExecutable => "hazakura-note"`.
- `codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/hazakura-note.app` passed.
- This automation session still could not complete an `open -n` launch smoke; Launch Services returned `kLSNoExecutableErr`, so no fresh manual built-app UI smoke is claimed for this slice.

Runtime smoke:

- The bundled `.app` opened and displayed the editor and preview panes.
- Open Folder loaded only the throwaway root's direct children first; `notes` and `large` remained collapsed until expanded.
- The tree hid `.git`, `node_modules`, `target`, `dist`, and `.hidden`.
- Expanding `notes` loaded `beta.md` on demand.
- Expanding `large` with 2,005 files did not fail the workspace; the status reported `Folder partially loaded` and visible entries remained usable.
- New File created `created-from-app.md` as a clean 0-byte tab, refreshed the tree, and wrote it to disk.
- Choosing existing `alpha.md` through New File did not overwrite disk; the existing clean tab was focused and disk content stayed intact.
- Open -> Edit -> Save wrote `alpha.md` changes to disk and returned the tab to clean state.
- Opening and saving `crlf.md` preserved CRLF bytes on disk.
- Editing and saving `no-final-newline.md` without a trailing newline preserved the missing final newline on disk.
- A save conflict on `conflict.md` stopped overwrite, left the external disk change intact, and displayed Reopen from disk / Close without saving / Keep editing actions.
- A non-conflict save failure on `no-write/failure.md` showed permission-denied recovery, kept local edits in the editor, and saved successfully after permissions were restored and Try save again was used.
- Dirty-tab close through the tab close button showed Save / Discard / Cancel, focused Cancel, kept the tab open after Cancel, preserved the unsaved text, and returned focus to the editor.
- App/window close with one dirty tab showed Save All / Discard All / Cancel; Cancel kept the app open, preserved the unsaved text, and returned focus to the editor.
- Active-file search found `target` twice in `notes/beta.md`, showed `2 / 2`, and highlighted both matches.
- Japanese IME composition was manually exercised by a human in the built app; editor and Find-field composition confirmation did not trigger Save, Open, Find movement, or tab-close shortcuts while composing.
- System, Light, and Dark theme selections were manually exercised. Dark was restored after restart; System followed the current OS-resolved dark appearance and kept editor, preview, status, tabs, and tree readable.
- Markdown preview sanitize was manually exercised against `sanitize.md`; `script`, `iframe`, and `alert` content did not appear in the preview tree, while ordinary headings and list items remained visible.
- Reusable manual smoke steps are documented in `docs/smoke-checklist.md`.

Text Editor Usability Pack smoke on 2026-05-27:

- Built-app toolbar showed LF / CRLF line-ending control and Preview toggle without pushing core controls out of reach.
- Preview toggle hid the preview pane and expanded the editor; toggling it back restored the Markdown preview.
- Preview image policy was manually exercised in the built app: an external `https://` image rendered as `Image blocked`, while an embedded `data:image/png;base64` image remained an image element.
- A CRLF `.txt` fixture opened clean with metadata showing `16 B · 9 chars · CRLF · no final newline`.
- Converting that fixture to LF marked the tab unsaved, saving returned it to clean state, and disk bytes changed to LF.
- Save As through the built app saved the same text to a new `.log` file and switched the tab to the new path. The path normalization handles macOS Save panel double-extension output such as `.log.txt` by saving to the intended known text extension.
- Rust tests cover Save As creation with a non-Markdown text extension and existing-file overwrite rejection.

Editor Reliability / Navigation Pack smoke on 2026-05-27:

- `npm run build:vite`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build`, and `git diff --check` passed after adding cursor/selection status, search options, Go to Line, display settings, draft restore, and focus/tab external-change recheck.
- Vite browser smoke at `http://127.0.0.1:1420/` confirmed search match count, invalid regex reporting, Go to Line status/cursor movement, and persisted editor settings for Invisibles, Font 16, and Tab 4 after reload.

Source Preview Quality Polish smoke on 2026-05-27:

- Built-app smoke used `/Users/keisetsu/Projects/hazakura-note-smoke-paid-quality`.
- Opening the throwaway folder replaced the workspace tree with direct children only and continued to hide `.git`, `node_modules`, `target`, and `dist`.
- Opening `alpha.md` from the tree showed Markdown metadata, preview, active tree state, and search highlights for two `target` matches.
- Opening `crlf.txt` showed `Text`, `CRLF`, byte/character counts, and clean state without marking the tab dirty.
- Modifying `alpha.md` outside the app, switching away, then switching back to the tab detected the external change and showed Reopen from disk / Close without saving / Keep editing before Save.
- A small polish fix corrected the Go to Line button's accessible name and removed extra separator whitespace in metadata/status text.

Modal Focus Trap Polish checks on 2026-05-27:

- Dirty-tab and app/window close dialogs now keep Tab / Shift+Tab focus cycling inside the dialog while it is open.
- `docs/smoke-checklist.md` now includes Tab / Shift+Tab focus-cycling checks for both close dialogs.
- `npm run build:vite` passed.
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` passed.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed with 16 Rust tests.
- `npm run build` passed and regenerated the local macOS `.app` bundle.
- `git diff --check` passed.

App Close Save All Failure Focus Polish checks on 2026-05-28:

- Failed or conflicted Save All from the app/window close dialog now stops the close, selects the failed tab, dismisses the dialog, returns focus to the editor, and leaves the normal save-failure or conflict recovery actions visible.
- `docs/smoke-checklist.md` now includes the failed-tab selection and editor-focus check for app/window close Save All failure.
- Automated local gates passed after this change; no fresh built-app manual smoke was claimed.

Dirty Tab Close Failure Focus Polish checks on 2026-05-28:

- Failed or conflicted Save from the dirty-tab close dialog now stops the close, selects the failed tab even when the close was requested from an inactive tab, dismisses the dialog, returns focus to the editor, and leaves the normal save-failure or conflict recovery actions visible.
- `docs/smoke-checklist.md` now includes the inactive-tab failed-selection check for dirty-tab close Save failure.
- Automated local gates passed after this change; no fresh built-app manual smoke was claimed.

Atomic Save Cleanup Polish checks on 2026-05-28:

- Atomic save now removes its hidden `.filename.hazakura-note.tmp` file if the final replace step fails.
- Rust tests cover both successful atomic replacement and cleanup after a failed replace attempt.
- Automated local gates passed after this change; no fresh built-app manual smoke was claimed.

Atomic Save Temp Collision Polish checks on 2026-05-28:

- Atomic save now creates its hidden `.filename.hazakura-note.tmp` file with exclusive creation, so a pre-existing temp file is not overwritten or removed by a failed save attempt.
- Rust tests cover successful atomic replacement, cleanup after a failed replace attempt, and pre-existing temp-file preservation.
- Automated local gates passed after this change; no fresh built-app manual smoke was claimed.

Discard All Draft Cleanup Polish checks on 2026-05-28:

- Discard All from the app/window close dialog now suppresses draft persistence during the requested close and removes stored recovery drafts for the discarded dirty tabs.
- `docs/smoke-checklist.md` now includes a restart check that discarded dirty tabs are not offered as recoverable drafts.
- Automated local gates passed after this change; no fresh built-app manual smoke was claimed.

Editor Keyboard Editing Polish checks on 2026-05-28:

- Editor-local Tab now inserts indentation instead of moving focus away from the editor.
- Shift+Tab outdents selected or current lines.
- Shift+Arrow selection is explicitly bound for character and line selection.
- Selection highlight colors were strengthened after browser smoke showed selection state moving while the visible highlight was too subtle.
- `npm run build:vite` passed.
- Vite browser smoke at `http://127.0.0.1:1421/` confirmed collapsed-cursor Tab indentation, selected-line Tab / Shift+Tab indent and outdent, Shift+Arrow character selection status, and visible selection highlight rendering.
- No fresh built-app manual smoke was claimed.

UI Brush-up Search Overlay checks on 2026-05-28:

- Cmd+F now opens the app's floating Find overlay without also opening CodeMirror's built-in search panel.
- Escape from the Find field closes the overlay, clears highlights, and returns focus to the editor.
- `npm run build:vite` passed.
- Vite browser smoke at `http://127.0.0.1:1421/` confirmed search count, highlights, no CodeMirror search panel, no horizontal overflow, and editor focus restoration.
- No fresh built-app manual smoke was claimed.

Find Close Polish checks on 2026-05-28:

- The Find close button now uses the same close path as Escape, clearing the query and search highlights before returning focus to the editor.
- `docs/smoke-checklist.md` now includes the close-button highlight-clear check in Active File Search.
- Automated local gates passed after this change; no fresh built-app manual smoke was claimed.

Workspace Image Preview / Quality Automation checks on 2026-05-28:

- Workspace tree image selection now opens a read-only local PNG/JPEG/GIF/WebP preview in the work area.
- Image preview reads the selected image through a Rust command constrained to the current workspace root and returns a `data:` URL, avoiding broad asset-protocol filesystem access.
- Markdown preview image safety remains unchanged: local and external Markdown image references stay blocked, while embedded `data:image` references remain allowed.
- `docs/development-automation.md` and the saved `hazakura-note-quality-loop` automation were updated at that time to prioritize quality-hardening slices that begin from built-app smoke when practical.
- Automated local gates passed after this change; no fresh built-app manual smoke was claimed.

Workspace Image Content Validation checks on 2026-05-28:

- Workspace image preview now requires the selected PNG/JPEG/GIF/WebP extension to match a lightweight file-content signature before returning a `data:` URL.
- Rust tests cover accepting supported PNG/JPEG/GIF/WebP previews, rejecting paths outside the workspace root, rejecting a `.png` file whose bytes are not image content, rejecting extension/signature mismatches, and rejecting images above the 20 MB preview limit.
- Automated local gates passed after this change; no fresh built-app manual smoke was claimed.

Workspace Image Size Limit Coverage checks on 2026-05-28:

- Added a Rust regression test that rejects workspace image previews above the 20 MB preview limit before producing a preview `data:` URL.
- `README.md`, `docs/roadmap.md`, and `docs/smoke-checklist.md` now state the same 20 MB workspace image preview boundary.
- `npm run build:vite`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build`, and `git diff --check` passed.
- No fresh built-app manual smoke was claimed.

Local Bundle Minimum System Version Polish checks on 2026-05-28:

- `src-tauri/tauri.conf.json` now sets the macOS bundle minimum system version to `11.0`, matching the Rust binary's `LC_BUILD_VERSION` minimum OS.
- `npm run build` regenerated the local `.app` bundle.
- `plutil -p src-tauri/target/release/bundle/macos/hazakura-note.app/Contents/Info.plist` confirmed `LSMinimumSystemVersion => "11.0"` while preserving `CFBundleExecutable => "hazakura-note"`.
- `otool -l src-tauri/target/release/bundle/macos/hazakura-note.app/Contents/MacOS/hazakura-note` confirmed the executable still reports `minos 11.0`.
- `codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/hazakura-note.app` passed.
- `open -n src-tauri/target/release/bundle/macos/hazakura-note.app` still returned `kLSNoExecutableErr` in this automation session, so no fresh built-app manual UI smoke is claimed.

Workspace Image Close Return Polish checks on 2026-05-28:

- Closing a selected workspace image preview now restores the text tab that was active before the image was opened when that tab is still open, falling back to the first open text tab if needed.
- `docs/smoke-checklist.md` now asks the image-preview smoke to confirm Cmd+W returns to the prior text tab.
- Automated local gates passed after this change. `open -n src-tauri/target/release/bundle/macos/hazakura-note.app` still returned `kLSNoExecutableErr` in this automation session, so no fresh built-app manual smoke was claimed.

Warning-expected DMG preview local check on 2026-05-28:

- Lane: binary DMG preview only; this does not change the source-only developer preview release boundary.
- This was a local preview artifact check only. No tag, push, GitHub Release publication, or DMG attachment was performed.
- `npm ci`, `npm run build:vite`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build`, `npm run build:dmg-preview`, `npm audit`, `cargo audit`, and `git diff --check` passed or completed with triaged warnings.
- `cargo audit` completed with 17 warnings and no pasted vulnerability findings. The warnings are transitive Tauri/wry dependency advisories: unmaintained GTK3/gtk-rs Linux stack crates, `proc-macro-error`, `unic-*` crates through `tauri-utils` / `urlpattern`, and one `glib` unsoundness warning through the Linux GTK/WebKit dependency path. These are not treated as a macOS warning-expected DMG preview blocker, but should remain visible for Linux support and future Tauri dependency update review.
- `npm run build -- --bundles app,dmg` confirmed Tauri can reach the app bundle step, but the Tauri DMG bundler stalled in Finder/`osascript` layout handling and was stopped before publication. The warning-expected lane now uses `scripts/build-warning-dmg-preview.sh` to avoid that Finder layout dependency.
- A plain local preview DMG was generated from the freshly built app with `npm run build:dmg-preview`: `src-tauri/target/release/bundle/dmg/hazakura-note_0.1.0_aarch64-warning-expected.dmg`.
- DMG SHA-256: `68d45b3699e4f3f3c529448bdbdbe6e24bb541586be4e3cbbf2a74d4104a716a`.
- Checksum file: `src-tauri/target/release/bundle/dmg/hazakura-note_0.1.0_aarch64-warning-expected.dmg.sha256`; `shasum -c` passed.
- `hdiutil verify src-tauri/target/release/bundle/dmg/hazakura-note_0.1.0_aarch64-warning-expected.dmg` passed.
- The DMG mounted read-only, contained `hazakura-note.app` and an `Applications` symlink, launched from the mounted volume, showed one window, exposed native File and View menus, and kept File > Save disabled in the no-file state.
- `codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/hazakura-note.app` passed.
- `codesign -dv --verbose=4` reported `Signature=adhoc` and `TeamIdentifier=not set`; `spctl --assess --type execute -vv` rejected the app, which matches the warning-expected, not-notarized preview boundary.
- App version was confirmed from the built bundle as `CFBundleShortVersionString => "0.1.0"` and `CFBundleVersion => "0.1.0"`.
- Release notes update proposal: `docs/releases/0.1.0-warning-expected-dmg-preview.md`.
- `scripts/build-warning-dmg-preview.sh` normalizes Apple Silicon artifact naming to `aarch64` so the generated preview filename matches the release-note convention.
- DeepSeek review feedback was incorporated by switching app bundle staging from `cp -R` to `cp -a` and making the next action explicitly require binary publication approval before DMG attachment.
- Chika review feedback was incorporated by adding macOS and required-command guards to the DMG preview script, clarifying the aarch64-only artifact identity, and tightening the release-note warning that this is not the source-only release.

Source-only 0.1.0 release gate on 2026-05-28:

- User approved proceeding with the source-only release lane first.
- Version alignment confirmed at `0.1.0` in `package.json`, `src-tauri/tauri.conf.json`, and `src-tauri/Cargo.toml`.
- No local `v0.1.0` tag or GitHub Release existed before publication work started.
- `npm ci`, `npm run build:vite`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, `npm run build`, `npm audit`, and `git diff --check` passed.
- `npm run build` regenerated `src-tauri/target/release/bundle/macos/hazakura-note.app`; notarization was skipped because no Apple notarization credentials were configured, which matches the source-only boundary.
- `codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/hazakura-note.app` passed.
- Built bundle metadata confirmed `CFBundleIdentifier => "lab.hazakura.note"`, `CFBundleShortVersionString => "0.1.0"`, `CFBundleVersion => "0.1.0"`, and `LSMinimumSystemVersion => "11.0"`.
- `cargo audit` completed with 17 allowed warnings from transitive Tauri/wry dependencies. These remain dependency-update review items and are not treated as source-only release blockers.
- Source release notes are in `docs/releases/0.1.0.md`.
- No DMG asset is approved for this source-only release.

Known verification note:

- Vite reports a production chunk-size warning because CodeMirror and preview libraries are bundled together. This is acceptable for the prototype; revisit before distribution readiness.
- The Modal Focus Trap Polish did not include a fresh built-app manual focus-cycling smoke pass; use the updated smoke checklist before treating this path as distribution-grade.
- The App Close Save All Failure Focus Polish did not include a fresh built-app manual failure/conflict smoke pass; use the updated smoke checklist before treating this path as distribution-grade.
- The Dirty Tab Close Failure Focus Polish did not include a fresh built-app manual failure/conflict smoke pass; use the updated smoke checklist before treating this path as distribution-grade.
- The Discard All Draft Cleanup Polish did not include a fresh built-app manual restart smoke pass; use the updated smoke checklist before treating this path as distribution-grade.
- The Editor Keyboard Editing Polish used Vite browser smoke only; repeat the new editor keyboard checklist in the built app before treating this path as distribution-grade.
- The UI Brush-up Search Overlay checks used Vite browser smoke only; repeat active-file search in the built app before treating this path as distribution-grade.
- The Find Close Polish did not include a fresh built-app manual active-file search pass; use the updated close-button check before treating this path as distribution-grade.
- The Workspace Image Preview / Quality Automation, content-validation, size-limit coverage, and close-return checks did not include a fresh built-app image-selection smoke pass; use the updated workspace image checklist before treating this path as distribution-grade.
- The Local Bundle Launch Metadata and Minimum System Version polish verified generated bundle metadata and ad-hoc signing, but the current automation session could not complete `open -n`; repeat built-app launch and native File menu smoke outside this automation environment before treating this path as distribution-grade.
- Agent Workbench real-provider behavior has automated fake-provider coverage only so far; use the trusted-workspace manual smoke checklist before treating real `codex` / `opencode` / `pi` sessions as product-grade.
- Long file name clipping was re-smoked in the workspace tree during Source Preview Quality Polish. A narrower-window pass is still useful before binary distribution readiness.

## Risks / Unknowns

- Unsaved draft restore is intentionally explicit and fingerprint-bound. It is a safety net, not an autosave system, and does not merge with changed disk content.
- Workspace listing is intentionally lazy and not a project index. Very large directories can still be partially listed when a single folder exceeds the per-folder cap.
- Save-conflict recovery is explicit and can show a non-Git change review before the user chooses a recovery action, but there is still no merge editor or automatic apply/resolve flow.
- Undo/redo remain CodeMirror defaults and have not received dedicated product-level controls beyond preserving the active editor session during theme changes.
- The app is ad-hoc signed for local build validation, but it is not Developer ID signed or notarized.
- The GitHub remote is configured over HTTPS. SSH access previously failed with `Permission denied (publickey)`.

## Next Actions

1. The v0.7 Review Desk Readiness Gate ran on 2026-06-01 in read-only mode. Findings are recorded in `docs/reviews/v0.7-readiness-gate.md` and classified as Fix now (F-1, F-2), Before v0.7 implementation (B-1 to B-4), v0.8+ (V-1 to V-3), Fold back together (T-1 to T-3), and Review Desk structural risk (R-1 to R-4). No code changed during the gate.
2. The B-1, B-2, and B-3 design decisions from the gate landed on 2026-06-01 as a docs-only slice in `docs/reviews/v0.7-review-desk-design-decisions.md`. B-1 introduces a top-level `ReviewSurface` state (separate from `RightPaneMode`). B-2 refactors `CompareViewState` into a `CompareCase` discriminated union plus a slimmer view state. B-3 reserves `Cmd+Shift+R` for the Review Desk entry point as a no-op handler. No code changed in this slice; the next step is the F-1 implementation slice.
3. The post-v0.8 feature-folder refactor lane landed on 2026-06-03 in 5 slices (6 commits, 0 behavior change) — `lib/tauri.ts` / `lib/locale.ts` per-feature split, `src-tauri/src/tests.rs` per-module split, Agent Workbench session state machine separation, shared CodeMirror highlight extraction, and the `format` helpers + `open_text_file` preflight dedupe. `src/App.tsx` is now a 6-line pure composition file. The 2026-06-03 Command Palette slice then landed `Cmd+Shift+P` as a thin launcher over existing safe-editor / agent / file actions. The 2026-06-03 v0.8 candidate polish round then landed three small slices on top of that: bounded Global Search (`Cmd+Shift+F`, new Rust `search_workspace_files_with_label` + 11 unit tests, modal + hook + CSS mirroring the Quick Open / Command Palette shape, Command Palette `Find in Files…` entry), pinned files in the start panel (`RecentEntry.pinnedAt` + storage normalizer + star toggle, two-group layout in `StartPanel`, en / ja / kana copy), and print-ready HTML export polish (screen + `@media print` stylesheet with serif paper body, `@page` margins, page-break hints, link URL annotations, mirrored across `exportPdf` and `exportHtml`). Verification: `npm run typecheck`, `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml` (133 tests, 0 failed, 0 ignored), `npm run build:vite`, `npm run build`, `git diff --check`. The next automation run can pick from remaining v0.8 candidate work (daily-editor polish, settings consolidation, Markdown toolbar polish), v0.9 candidate work (calm Markdown workspace, bounded file-tree operations, Frontmatter, keybinding customization, KaTeX beta), the deferred `EditorTab.line_ending` → `lineEnding` rename (would need a localStorage key migration line — discuss before reopening), or a one-off feature slice the user explicitly opens.
4. For external-agent implementation, use `docs/external-agent-review-workflow.md`; Codex should review the diff before the slice is accepted and should not patch unless asked for review-and-fix.
5. Keep v0.7 immutable unless a patch-release lane is explicitly approved. Move editable two-column Review Desk, candidate temporary-document policy, bounded Global Search, Command Palette, Settings consolidation, Frontmatter, KaTeX, and encoding conversion to v0.8+.
6. Keep Agent Workbench explicit, allowlisted, one-session, no-restore, and no-auto-apply. Do not add Pi RPC, Pi SDK, provider-add UI, arbitrary provider config, multi-agent orchestration, a general terminal, or Git client behavior.
7. Treat v0.4 Markdown Review Navigation and v0.3 Diff / Change Review regressions as patch follow-up only; do not expand them into project-wide indexing, prediction, merge editing, or Git-aware behavior.
8. Keep the published `v0.7.0` tag and GitHub Release immutable. If a release-critical issue is found, prepare a transparent patch release rather than moving the tag.
9. Re-smoke long file name / constrained-width layout before binary distribution readiness.
10. For follow-up warning-expected DMG previews, keep the release marked as a prerelease and use the current version-specific release note as the GitHub Release body.
11. Keep signing, notarization, updater, and installer packaging separate until the v1.x distribution-quality lane or an explicit earlier distribution-lane approval.

## Avoid

- Do not add Git operations, arbitrary terminal/shell access, LSP, plugin execution, or AI rewrite/apply flows during the next Review Desk slices.
- Do not expand workspace persistence into project indexing or background scanning.
- Do not treat the current build as distribution-ready.
