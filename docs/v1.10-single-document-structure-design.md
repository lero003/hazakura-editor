# v1.10 Single-document Structure Foundation

Status: Source complete; representative packaged interaction smoke passed
Scope: One Markdown source; read-only structure plus one bounded edit proof
Authority: High for v1.10 implementation
Last reviewed: 2026-07-14

## Goal

一つの Markdown source にある frontmatter、ATX heading、明示的 page-break、
EPUB navigation候補を同じ解釈で扱う。Markdown source は保存の正本のままとし、
構造は再生成可能な presentation metadata とする。

## Invariants

- 編集可能な buffer は一つだけ。
- source offset / line を保持し、解析のために本文を正規化・再構成しない。
- fenced code、leading YAML frontmatter、LF / CRLF を同じ規則で扱う。
- 空見出しや level skip は将来の助言候補であり、解析エラーや自動修正にしない。
- structure edit は後続スライスでも明示操作、単一 transaction、Undo可能、dirtyの対象。
- manifest、独自database、workspace scan、複数ファイル章順、auto-saveを追加しない。

## Slices

| Slice | Status | Acceptance |
|---|---|---|
| **S1 — Shared read-only model** | **Done / source** | `parseMarkdownStructure` が frontmatter、ATX heading、page-break、navigation候補を source位置付きで返す。Outline、e-book、EPUBが同じ解析を使い、異なる長さ/種類の fence と CRLF を回帰テストで固定する。 |
| **S2 — Structure overview** | **Done / source** | 既存 Outline surface 内で hierarchy と page-break を読み取り専用表示し、選択でEditorへ移動する。新しい pane / chromeは作らない。 |
| **S3 — Advisory checks** | **Done / source** | skipped level、空見出し、重複navigation label、極端に長いsectionを助言として示す。保存やexportを妨げない。 |
| **S4 — One bounded edit proof** | **Done / source** | heading level changeだけを、明示的な単一-buffer transactionとして実証する。Undo、dirty、Save As / recoveryが読む同じbuffer、IME中の拒否をテスト固定する。 |

## S1 Model

`src/features/editor/markdownStructure.ts` は次だけを返す。

- closed leading YAML frontmatter range
- ATX heading level、line、source offsets、表示text、navigation label候補
- standalone page-break markerのsource offsetsと render / drop role

空見出しは heading として残すが navigation候補にはしない。これにより後続の助言は
構造を見失わず、EPUB navigationは空labelを生成しない。

## Overview And Advisories

Outlineは見出しlevelに応じた階層、通常 / 末尾のpage-break、空見出しをsource順に
最大200項目表示する。項目選択は既存のEditor line jumpだけを使う。

助言は表示専用で、保存・Save As・exportを止めない。対象は次の4種類。

- 直前の見出しから2段以上飛ぶlevel
- 空見出し
- 大文字小文字を区別しない重複navigation label（2件目以降）
- 800行以上、または40,000文字以上の見出しsection

## Bounded Edit Proof

Outlineの明示ボタンはATX heading levelを1段だけ上げ下げする。実行直前に現在の
Editor sourceとoffsetを再照合し、`#` marker以外は変更しない。CodeMirrorへ1回だけ
dispatchするため通常のUndo historyとdirty判定を使い、別bufferや自動保存を作らない。
Assist生成lock、read-only、IME composition中、H1/H6境界、stale offsetでは実行しない。

## Deferred

- section move、複数見出しの一括変更、automatic correction
- Setext heading の構造扱い
- Markdown以外の文書構造
- multi-file Book Scope / Book manifest / hidden model
- v1.10 version bump、package、TestFlight、release

## Verification

- focused Vitest: shared model、Outline、e-book chapter/page-break、EPUB navigation
- focused Vitest: advisory thresholds、heading marker transaction、Undo、dirty、IME
- full `npm run test`
- `npm run typecheck`
- `npm run build:vite`
- `git diff --check`
- `npm run smoke:fixtures:v1.10-structure`
- packaged interaction: `docs/smoke-checklist.md` の v1.10 section

2026-07-14 の fresh local bundle では、temporary fixture workspaceを使い、
frontmatterを除いたOutline hierarchy、通常 / 末尾page-break、重複label・空見出し・
level skipの3助言、803行sectionの長さ助言を確認した。`第一場面` のH3→H2変更は
同じtabをdirtyにし、`Cmd+Z` 1回でsourceとclean stateへ戻った。これは代表的な
Developer packaged proofであり、source jump全種、IME、Save As、recovery、e-book / EPUB、
signed TestFlightは引き続き個別smokeを要する。
