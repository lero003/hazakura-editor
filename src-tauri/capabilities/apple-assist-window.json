{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "apple-assist-window",
  "description": "Minimal capability for the detached Apple Assist writing companion window. Scoped to the apple-assist window label only. It can listen for main-window target updates and call custom IPC commands, while the server-side label gates restrict mutating commands to the Apple Assist apply request and theme sync paths.",
  "windows": ["apple-assist"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
