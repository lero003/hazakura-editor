import { describe, expect, it, vi } from "vitest";
import { renderHook } from "@testing-library/react";
import { useAppShellSideEffectsController } from "./useAppShellSideEffectsController";

describe("useAppShellSideEffectsController", () => {
  it("returns nothing (side-effect-only surface)", () => {
    const { result } = renderHook(() =>
      useAppShellSideEffectsController({
        actions: {
          createNewFile: vi.fn(),
          createOkfScaffold: vi.fn(),
          importSourceAsMarkdownDraft: vi.fn(),
          exportEpubBeta: vi.fn(),
          exportHtml: vi.fn(),
          exportPdf: vi.fn(),
          openAgentWindow: vi.fn(),
          openAppleAssistWindow: vi.fn(),
          openFile: vi.fn(),
          openReferenceFile: vi.fn(),
          openWorkspace: vi.fn(),
          openWorkspacePath: vi.fn(),
          requestAppQuit: vi.fn(),
          requestWindowClose: vi.fn(),
          saveActiveTab: vi.fn(),
          saveActiveTabAs: vi.fn(),
        },
        listener: {
          onOpenRecentFile: vi.fn(),
          recentFilesRef: { current: [] },
          recentFoldersRef: { current: [] },
          setEditorSettings: vi.fn(),
          setPreferencesDialogMode: vi.fn(),
          setPreviewVisible: vi.fn(),
          setThemePreference: vi.fn(),
        },
        activity: {
          onResumeAgentUiRefresh: vi.fn(),
          onSuspendAgentUiRefresh: vi.fn(),
          onToggleQuickOpen: vi.fn(),
        },
        agentSessionSync: {
          activeAgentSession: false,
          agentSession: null,
          agentUiSuspendedRef: { current: false },
          agentWorkbenchAvailable: false,
          onRefreshAgentSessionState: vi.fn(),
          onResetAgentOutput: vi.fn(),
          setAgentLaunchGate: vi.fn(),
          setAgentSession: vi.fn(),
          workspaceRootPath: null,
        },
        appShellSync: {
          activeDirty: false,
          activeTab: null,
          agentWorkbenchActive: false,
          agentWorkbenchConsent: false,
          assistSurfaceActive: "none",
          discardingWindowCloseRef: { current: false },
          editorSettings: {
            wrapLines: true,
            showInvisibles: false,
            editorFontSize: 14,
            previewFontSize: 15,
            workspaceFontSize: 13,
            lModeFontSize: 15,
            tabSize: 2,
            spellcheckEnabled: true,
            autoBackupEnabled: true,
            ambientIntensity: "subtle",
            appleAssistDiffInitiallyOpen: true,
  outsideImages: "ask",
  loadRemoteImages: false,
  materializeImagesOnExport: true,
            lModeEnabled: false,
            lModeTypewriter: false,
          },
          menuLanguage: "en",
          pendingDrafts: [],
          previewVisible: true,
          recentFiles: [],
          recentFolders: [],
          restoreComplete: false,
          selectedImage: null,
          tabs: [],
          themePreference: "light",
          workspaceRootPath: null,
        },
        workspaceRuntime: {
          activeTabPath: null,
          closeWorkspaceContextMenu: vi.fn(),
          insertMarkdownAtCursor: vi.fn(),
          openExternalFilePaths: vi.fn(),
          restoreComplete: false,
          setActiveTabId: vi.fn(),
          setGlobalError: vi.fn(),
          setPendingDrafts: vi.fn(),
          setRestoreComplete: vi.fn(),
          setStatus: vi.fn(),
          setTabs: vi.fn(),
          setWorkspaceRootPath: vi.fn(),
          setWorkspaceTree: vi.fn(),
          workspaceContextMenuOpen: false,
          workspaceRootPath: null,
        },
        keyboardFocus: {
          activeAgentSession: false,
          activeTabId: null,
          agentUiSuspendedRef: { current: false },
          allowWindowCloseRef: { current: false },
          appCloseCancelButtonRef: { current: null },
          appCloseDialogRef: { current: null },
          assistDiscardCancelButtonRef: { current: null },
          closeTabCancelButtonRef: { current: null },
          closeTabDialogRef: { current: null },
          epubExportDialogRef: { current: null },
          epubExportSettingsOpen: false,
          pdfExportDialogRef: { current: null },
          pdfExportSettingsOpen: false,
          moveTrashCancelButtonRef: { current: null },
          moveTrashDialogRef: { current: null },
          assistDiscardDialogRef: { current: null },
          commandPaletteVisible: false,
          dirtyTabCount: 0,
          editorPaneRef: { current: null },
          findInputRef: { current: null },
          findVisible: false,
          globalSearchVisible: false,
          okfReviewVisible: false,
          modalOpen: false,
          onApplyMarkdownFormat: vi.fn(),
          onCancelAppClose: vi.fn(),
          onCancelAssistDiscard: vi.fn(),
          onCancelEpubBetaExport: vi.fn(),
          onCancelPdfExport: vi.fn(),
          onCancelPendingTrash: vi.fn(),
          onCancelTabClose: vi.fn(),
          onCheckTabForExternalChange: vi.fn(),
          onCloseCommandPalette: vi.fn(),
          onCloseFindAndFocusEditor: vi.fn(),
          onCloseGlobalSearch: vi.fn(),
          onCloseOkfReview: vi.fn(),
          onClosePreferences: vi.fn(),
          onCloseSelectedImagePreview: vi.fn(),
          onCreateNewFile: vi.fn(),
          onFocusAdjacentTab: vi.fn(),
          onFocusEditorSoon: vi.fn(),
          onNeedsWindowCloseConfirmation: vi.fn(),
          onOpenCommandPalette: vi.fn(),
          onOpenFile: vi.fn(),
          onOpenGlobalSearch: vi.fn(),
          onOpenWorkspace: vi.fn(),
          onRequestCloseTab: vi.fn(),
          onRequestWindowClose: vi.fn(),
          onSaveActiveTab: vi.fn(),
          onSaveActiveTabAs: vi.fn(),
          pendingAppClose: false,
          pendingAssistDiscardOpen: false,
          pendingCloseTabOpen: false,
          pendingTrashOpen: false,
          preferencesCloseButtonRef: { current: null },
          preferencesDialogRef: { current: null },
          preferencesOpen: false,
          selectedImageOpen: false,
          setEditorSettings: vi.fn(),
          setFindVisible: vi.fn(),
          setPreferencesDialogMode: vi.fn(),
          setPreviewVisible: vi.fn(),
          setStatus: vi.fn(),
        },
      }),
    );

    // Side-effect-only surface: returns nothing.
    expect(result.current).toBeUndefined();
  });
});
